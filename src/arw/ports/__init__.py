"""ARW v2 capability ports.

The kernel depends on these Protocols, never on concrete adapters. Method
signatures are extracted from existing strict contracts — the port IS the
current behavior, formalized (see openspec/changes/ports-and-adapters).
"""
