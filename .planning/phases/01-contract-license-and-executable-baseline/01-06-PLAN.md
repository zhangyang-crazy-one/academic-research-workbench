---
phase: 01-contract-license-and-executable-baseline
plan: 06
type: execute
wave: 4
depends_on: [01-03, 01-04]
files_modified:
  - schemas/v1/mcp-read-*.schema.json
  - tests/integration/test_mcp_confinement.py
  - tests/fixtures/confinement/**
  - vendor/patches/file-base/0002-phase1-confined-read.patch
  - vendor/source-manifest.json
  - scripts/build-file-base
  - MODIFICATIONS.md
  - .mcp.json
  - scripts/file-base-mcp
  - scripts/stage-plugin
  - scripts/smoke-staged-plugin
  - tests/staged/test_mcp_launcher.py
autonomous: true
requirements: [PKG-03, FILE-05]
must_haves:
  truths:
    - "Confinement schemas and executed red denial tests exist before patch 0002 is implemented."
    - "The native MCP rejects traversal, outside roots, symlink escape, sensitive files, and over-budget requests before content."
    - "The unchanged upstream C test suite passes after the patch under network denial."
    - "ASan+UBSan and a separate TSan build/run pass under network denial with distinct raw evidence."
    - "The installed plugin launches the rebuilt MCP stage-relatively and completes one bounded fixture read without source/network access."
  artifacts:
    - path: "tests/integration/test_mcp_confinement.py"
      provides: "Red-first direct-native denial matrix and native safety evidence assertions"
    - path: "vendor/patches/file-base/0002-phase1-confined-read.patch"
      provides: "MCP-internal descriptor-safe confinement implementation"
    - path: "scripts/file-base-mcp"
      provides: "Stage-relative installed native launcher"
  key_links:
    - from: "tests/integration/test_mcp_confinement.py"
      to: "vendor/patches/file-base/0002-phase1-confined-read.patch"
      via: "executed red tests precede patch implementation"
      pattern: "access_denied"
    - from: "scripts/build-file-base"
      to: "build/evidence/phase-01/native"
      via: "unchanged upstream, ASan+UBSan, and separate TSan network-denied runs"
      pattern: "sanitizer"
    - from: ".mcp.json"
      to: "scripts/file-base-mcp"
      via: "plugin-root-relative command proven after install"
      pattern: "file-base-mcp"
---

<objective>
Implement and deeply verify the confined native MCP, then converge its installed launcher to success.

Purpose: Satisfy FILE-05/PKG-03 with MCP-internal enforcement, unchanged upstream regression coverage, sanitizer evidence, and no source-path assumptions.
Output: Schemas, adversarial fixtures/tests, patch 0002, network-denied upstream/sanitizer evidence, extended legal inventory, rebuilt binary, installed launcher/config, and successful smoke evidence.
</objective>

## Phase Goal

**As a** workbench operator, **I want to** install and exercise a legally classified, reproducibly sourced headless plugin, **so that** its authority and filesystem boundaries are proven before feature expansion.

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-contract-license-and-executable-baseline/01-CONTEXT.md
@.planning/phases/01-contract-license-and-executable-baseline/01-RESEARCH.md
@.planning/phases/01-contract-license-and-executable-baseline/01-VALIDATION.md
@.planning/phases/01-contract-license-and-executable-baseline/01-03-SUMMARY.md
@.planning/phases/01-contract-license-and-executable-baseline/01-04-SUMMARY.md

<interfaces>
The request contains schema_version, allowed_root capability identifier, relative_path, max_bytes &lt;=4096, and max_lines &lt;=200. A result is bounded UTF-8 content with byte/line/truncation metadata or a typed denial with no content. The launcher resolves PLUGIN_ROOT from its installed path, execs `$PLUGIN_ROOT/libexec/file-base-mcp`, and accepts only explicit root/cache settings. Native evidence directories are `build/evidence/phase-01/native/upstream`, `native/asan-ubsan`, and `native/tsan`; each retains commands, compiler/link flags, streams, statuses, test inventory/hash, and network syscall audit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute red direct-native confinement contracts</name>
  <files>schemas/v1/mcp-read-request.schema.json, schemas/v1/mcp-read-result.schema.json, tests/integration/test_mcp_confinement.py, tests/fixtures/confinement/**</files>
  <read_first>Plan 03 source manifest, build script, offline runner, and digest tests; Plan 04 stage/private tests; 01-VALIDATION.md Confinement Fixture and Native Safety Gate; 01-CONTEXT.md D-06, D-07, D-09</read_first>
  <behavior>CJK/LaTeX content succeeds within 4096 bytes/200 lines; traversal, absolute outside path, symlink escape, unconfigured root, `.env`, and over-budget requests return typed no-content denials; outside secret appears nowhere.</behavior>
  <action>Before creating 0002, write strict Draft 2020-12 schemas, fixtures, and parameterized tests. Require policy evaluation before content, absent content on denial, no outside canary in any stream/evidence, explicit Linux claim, case-specific raw evidence, and a test that talks directly to the native process. Execute the tests against the 0001-only binary and explicitly invert the expected RED behavior result; collection/import/framing failure is invalid RED unless the test pinpoints the missing confinement capability per D-06.</action>
  <acceptance_criteria>Tests collect and execute red against the unpatched native binary for the expected confinement assertions; every attack, bounded success, no-content check, and direct-native proof is present.</acceptance_criteria>
  <verify><automated>uv run pytest --collect-only -q tests/integration/test_mcp_confinement.py &amp;&amp; ! uv run pytest -q tests/integration/test_mcp_confinement.py</automated></verify>
  <done>Confinement contracts and observed RED tests precede patch 0002.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement patch 0002 and pass upstream plus sanitizer suites offline</name>
  <files>vendor/patches/file-base/0002-phase1-confined-read.patch, vendor/source-manifest.json, scripts/build-file-base, MODIFICATIONS.md, tests/integration/test_mcp_confinement.py</files>
  <read_first>Task 1 schemas/tests/fixtures and RED evidence; Plan 03 0001/source manifest/build/offline runner/digest tests and preserved upstream test/legal assets; Plan 04 extended license gate and dependency inventory; AGENTS.md unchanged upstream C suite and sanitizer requirements</read_first>
  <behavior>0002 applies after 0001; native path enforcement makes confinement tests green; the byte-identical pinned upstream C tests pass normally, under combined ASan+UBSan, and under a separate TSan build/run, all with network denied.</behavior>
  <action>Implement 0002 only after valid RED evidence. Enforce descriptor-relative component traversal under an opened allowed-root, no symlink following, regular-file final target, sensitive-name policy, and request ceilings before content. Do not modify upstream C test sources; verify their manifest hash before every run. Use existing libc/platform primitives without adding a native package dependency. Extend manifest/patch order, post-patch tree/binary digests, drift coverage, and MODIFICATIONS. Under `scripts/offline-exec`, run (1) the unchanged upstream suite on a clean normal build, (2) a clean ASan+UBSan build and unchanged suite with sanitizer failures fatal, and (3) a distinct clean TSan build and unchanged suite with race reports fatal. Never combine TSan with ASan. Retain separate raw evidence directories, flags, compiler identity, test inventory/hash, streams/status, and network audit. Rerun `scripts/license-gate` and notice generation over 0001+0002, all bundled C/Python dependencies, policy/checkers, generated notices, and current artifacts; preserve release BLOCKED semantics.</action>
  <acceptance_criteria>0002 is ordered/hashed after 0001; direct-native tests pass; unchanged upstream, ASan+UBSan, and separate TSan runs all pass offline; full extended legal/digest inventory includes 0002 and all dependencies.</acceptance_criteria>
  <verify><automated>./scripts/offline-exec --evidence-root build/evidence/phase-01/native/upstream ./scripts/build-file-base --clean --run-upstream-tests &amp;&amp; ./scripts/offline-exec --evidence-root build/evidence/phase-01/native/asan-ubsan ./scripts/build-file-base --clean --sanitizers asan,ubsan --run-upstream-tests &amp;&amp; ./scripts/offline-exec --evidence-root build/evidence/phase-01/native/tsan ./scripts/build-file-base --clean --sanitizers tsan --run-upstream-tests &amp;&amp; ./scripts/license-gate --source-manifest vendor/source-manifest.json --pre-vendor-evidence build/evidence/phase-01/pre-vendor-license --evidence-root build/evidence/phase-01/license-0002 &amp;&amp; uv run pytest -q tests/integration/test_digest_drift.py tests/integration/test_mcp_confinement.py</automated></verify>
  <done>FILE-05 lives inside a digest-verified native MCP with unchanged upstream and separate sanitizer evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Converge the installed MCP launcher to a bounded read</name>
  <files>.mcp.json, scripts/file-base-mcp, scripts/stage-plugin, scripts/smoke-staged-plugin, tests/staged/test_mcp_launcher.py</files>
  <read_first>Plan 02 stage/smoke convergence pattern; Plan 04 current private-safe stage/inventory tests; Task 2 source manifest, binary, native/legal evidence; 01-RESEARCH.md resolved launcher strategy</read_first>
  <behavior>The exact stage includes libexec/file-base-mcp and self-locating launcher; isolated installed plugin starts it outside the repository with explicit fixture root/cache and succeeds on one bounded read.</behavior>
  <action>Write and execute the installed launcher test before implementation, expecting a behavior RED result. Configure only plugin-root-relative `.mcp.json` paths accepted by the host and implement the launcher to exec the staged binary. Run probe, classify, adapt owned config/launcher/stage files, clean rebuild/restage, cachebuster/reinstall, and fresh-process retry until negotiation and bounded read succeed. All denials and native safety evidence remain green. Authentication is the only external pause. Preserve every attempt and scan for source/home paths and canaries per D-08/D-09.</action>
  <acceptance_criteria>Final installed attempt launches/reads successfully outside the repository; denials and native suites remain green; no source path/PYTHONPATH/network/implicit root is used.</acceptance_criteria>
  <verify><automated>./scripts/smoke-staged-plugin --mcp --fresh-home build/isolated/mcp-home --evidence-root build/evidence/phase-01/mcp-launcher &amp;&amp; uv run pytest -q tests/staged/test_mcp_launcher.py tests/integration/test_mcp_confinement.py</automated></verify>
  <done>PKG-03/FILE-05 have successful installed-launch, direct-native confinement, upstream-regression, and sanitizer evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| MCP client to native process | Untrusted root/path/budget fields cross into filesystem access. |
| source patch to native binary | Memory/race defects or changed tests could hide unsafe behavior. |
| installed config/launcher to staged binary | Host cwd/cache behavior must not redirect execution to source bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-TRAV | Information Disclosure | native read | mitigate | Reject absolute/parent traversal before open. |
| T-06-SYM | Information Disclosure | native read | mitigate | Descriptor-relative no-follow walk and regular-file final check. |
| T-06-DOS | Denial of Service | native output | mitigate | Enforce ceilings before read and bound process capture. |
| T-06-MEM | Tampering/Denial of Service | C patch | mitigate | Unchanged upstream suite plus ASan+UBSan and separate TSan runs. |
| T-06-STALE | Tampering | installed cache | mitigate | Clean rebuild/restage, cachebuster/reinstall, fresh-process retry. |
| T-06-SC | Tampering | patch/native build | mitigate | Ordered patch hash, network-denied builds, full legal/dependency rerun. |
</threat_model>

<verification>
Run direct-native confinement, unchanged upstream, ASan+UBSan, separate TSan, legal rerun, and installed launcher commands. Confirm all native runs are network-denied with distinct raw evidence and unchanged test hashes.
</verification>

<success_criteria>
PKG-03/FILE-05 pass through source-independent installed launch, internal no-content confinement, unchanged upstream regression, and clean sanitizer evidence.
</success_criteria>

<output>Create `.planning/phases/01-contract-license-and-executable-baseline/01-06-SUMMARY.md` when done.</output>
