---
phase: 01-contract-license-and-executable-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .python-version
  - pyproject.toml
  - uv.lock
  - .codex-plugin/plugin.json
  - skills/academic-research-workbench/SKILL.md
  - bin/arw
  - vendor/python/wheelhouse/**
  - vendor/python/wheelhouse.lock.json
  - scripts/stage-plugin
  - scripts/smoke-staged-plugin
  - tests/staged/test_manifest_install.py
  - tests/staged/test_cli_launcher.py
autonomous: true
requirements: [PKG-01]
must_haves:
  truths:
    - "An operator can build, validate, and install an allowlisted plugin stage from a clean checkout."
    - "The installed Python command runs from an unrelated directory with isolated HOME/CODEX_HOME, no repository import, and no network."
    - "The installed runtime is reconstructed from a hash-locked wheelhouse and reports deterministic bootstrap failures."
  artifacts:
    - path: "pyproject.toml"
      provides: "Frozen Python package, test, and build contract"
    - path: "bin/arw"
      provides: "Stage-relative offline Python launcher"
    - path: "scripts/stage-plugin"
      provides: "Positive-allowlist stage builder"
    - path: "tests/staged/test_manifest_install.py"
      provides: "Clean stage validation and isolated install proof"
  key_links:
    - from: "bin/arw"
      to: "vendor/python/wheelhouse.lock.json"
      via: "hash-verified no-index cache-local environment bootstrap"
      pattern: "wheelhouse"
    - from: "scripts/smoke-staged-plugin"
      to: "scripts/stage-plugin"
      via: "install and execute the exact generated stage"
      pattern: "stage-plugin"
---

<objective>
Establish the Python/package/test bootstrap and prove clean, source-independent plugin installation.

Purpose: Satisfy PKG-01 through the real staged installation boundary while keeping route/host convergence in Plan 02.
Output: Frozen Python metadata and wheelhouse, valid plugin shell, offline launcher, allowlisted stage/install scripts, red-first acceptance tests, and raw install evidence.
</objective>

## Phase Goal

**As a** workbench operator, **I want to** install and exercise a legally classified, reproducibly sourced headless plugin, **so that** its authority and filesystem boundaries are proven before feature expansion.

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contract-license-and-executable-baseline/01-CONTEXT.md
@.planning/phases/01-contract-license-and-executable-baseline/01-RESEARCH.md
@.planning/phases/01-contract-license-and-executable-baseline/01-VALIDATION.md
@.planning/phases/01-contract-license-and-executable-baseline/01-PATTERNS.md

<interfaces>
`bin/arw` resolves PLUGIN_ROOT from its installed path, selects only ARW_PYTHON or PATH Python within &gt;=3.13,&lt;3.15, clears PYTHONPATH/user site, verifies the checked-in wheelhouse, and creates `$CODEX_HOME/arw/runtime/&lt;wheelhouse-sha256&gt;/venv` under a lock using pip `--no-index --require-hashes --find-links`. The stage is assembled from an explicit allowlist and installed into a repository-owned isolated marketplace.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze package inputs and execute red installed-path tests</name>
  <files>.python-version, pyproject.toml, uv.lock, vendor/python/wheelhouse/**, vendor/python/wheelhouse.lock.json, tests/staged/test_manifest_install.py, tests/staged/test_cli_launcher.py</files>
  <read_first>01-RESEARCH.md Standard Stack, Package Legitimacy Audit, Staged Plugin Smoke Path, and Open Questions (RESOLVED); 01-VALIDATION.md Plan 01 rows and Installed Runtime Isolation Gate; 01-CONTEXT.md D-01, D-02, D-07, D-09</read_first>
  <behavior>Clean stage validation/install and outside-repository CLI execution must fail before plugin, launcher, and stage scripts exist; failure must be an expected behavior assertion, not collection/import/syntax failure.</behavior>
  <action>Per D-01/D-02/D-07/D-09, pin Python 3.14.6 with support for &gt;=3.13,&lt;3.15 and create the frozen uv/hatchling/pytest project using only PASS entries from the Package Legitimacy Audit. Check in every build/runtime wheel with package/version, registry, SHA-256, and uv.lock relationship. Write behavior tests for validator-backed exact stage install and installed CLI execution from unrelated cwd with isolated HOME/CODEX_HOME, cleared PYTHONPATH, denied repository source/network, and raw command/stream/status evidence. Run collection, then execute the tests and explicitly invert the expected nonzero RED result; inspect that failures name absent plugin/launcher/stage behavior.</action>
  <acceptance_criteria>The lock and wheel hashes are complete; behavior tests collect, execute, and fail for the expected missing artifacts; no test passes through source-tree/static inspection.</acceptance_criteria>
  <verify><automated>uv sync --frozen &amp;&amp; uv run pytest --collect-only -q tests/staged/test_manifest_install.py tests/staged/test_cli_launcher.py &amp;&amp; ! uv run pytest -q tests/staged/test_manifest_install.py tests/staged/test_cli_launcher.py</automated></verify>
  <done>The package environment and executable PKG-01 boundary exist as an observed RED test contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the valid plugin shell and offline installed launcher</name>
  <files>.codex-plugin/plugin.json, skills/academic-research-workbench/SKILL.md, bin/arw</files>
  <read_first>Task 1 pyproject.toml, uv.lock, wheelhouse lock, and both staged tests; 01-PATTERNS.md plugin/skill/launcher assignments; 01-CONTEXT.md D-01, D-02, D-03, D-05</read_first>
  <behavior>The plugin manifest and thin model-invocable skill validate; bin/arw bootstraps only from installed bytes and exposes a package health command without canonical mutation.</behavior>
  <action>Create plugin identity `academic-research-workbench` with relative skills, mixed-license LicenseRef, and the interface fields required by plugin-creator. Create the final thin installed skill declaration targeting `bin/arw route --json`; this plan proves package/install presence, while PKG-02 owns execution of that route contract. Implement `bin/arw` exactly to the interface above with a bootstrap lock, atomic completion marker, wheel/hash verification, stderr diagnostics, typed incompatible-interpreter failure, and no repository/home fallback. Keep this shell read-only per D-03 and preserve separate mixed-license identity per D-05.</action>
  <acceptance_criteria>The source plugin validates; launcher contains no Paper4Master, Examination, source-checkout, or developer-home path and cannot import repository source.</acceptance_criteria>
  <verify><automated>uv run python /home/zhangyangrui/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py . &amp;&amp; env -u PYTHONPATH PYTHONNOUSERSITE=1 ./bin/arw --help</automated></verify>
  <done>A valid plugin/package shell and stage-relative offline launcher exist without canonical-write behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build and install the exact allowlisted stage</name>
  <files>scripts/stage-plugin, scripts/smoke-staged-plugin, tests/staged/test_manifest_install.py, tests/staged/test_cli_launcher.py</files>
  <read_first>Task 1 staged tests; Task 2 plugin manifest, skill, and bin/arw; plugin-creator validator/install/update references; 01-CONTEXT.md D-01, D-02, D-05, D-09</read_first>
  <behavior>The positive allowlist builds the first-party wheel and exact stage; isolated marketplace install and CLI execution succeed without repository source, network, or inherited Python state.</behavior>
  <action>Implement strict-shell stage and smoke scripts. Build the first-party wheel, assemble only declared package/plugin/wheelhouse files, reject symlinks/extras/private classes, validate the exact stage, install it into isolated HOME/CODEX_HOME, and execute the installed CLI from an unrelated cwd with repository source inaccessible and network denied. Retain stage inventory, digests, validator/install commands, streams, statuses, and launcher provenance per D-09. Do not populate `vendor/sources/**`; Plan 03 owns all upstream source-tree copying after its pre-vendoring gate.</action>
  <acceptance_criteria>The exact clean stage validates and installs; installed CLI succeeds from isolated bytes; evidence contains no source/home prefixes or secrets; vendor/sources remains untouched.</acceptance_criteria>
  <verify><automated>./scripts/stage-plugin --clean --stage-root build/stage/academic-research-workbench &amp;&amp; ./scripts/smoke-staged-plugin --install-cli --fresh-home build/isolated/install-home --evidence-root build/evidence/phase-01/install &amp;&amp; uv run pytest -q tests/staged/test_manifest_install.py tests/staged/test_cli_launcher.py</automated></verify>
  <done>PKG-01 passes from a clean checkout through exact staged installation and installed CLI execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository to installed stage | Only declared, hash-covered files may enter plugin bytes. |
| installed launcher to host Python/cache | Interpreter and wheel inputs are accepted only after version/hash checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-SUB | Tampering | wheelhouse/stage | mitigate | Frozen lock, wheel hashes, positive inventory, no-index install. |
| T-01-PRIV | Information Disclosure | stage/evidence | mitigate | Allowlist assembly and forbidden path/private-class scans. |
| T-01-REP | Repudiation | install smoke | mitigate | Raw validator/install/launcher commands, streams, statuses, and exact stage digests. |
| T-01-SC | Tampering | package installs | mitigate | Package Legitimacy Audit PASS packages only; checked-in wheels and hashes. |
</threat_model>

<verification>
Run the installed behavior tests and inspect that the final process executes exact installed bytes outside the repository with isolated HOME/CODEX_HOME, no PYTHONPATH/network/source access, and retained raw evidence.
</verification>

<success_criteria>
PKG-01 is satisfied by a validated, source-independent exact installation backed by a frozen offline Python runtime.
</success_criteria>

<output>Create `.planning/phases/01-contract-license-and-executable-baseline/01-01-SUMMARY.md` when done.</output>
