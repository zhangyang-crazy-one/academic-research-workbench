---
phase: 01-contract-license-and-executable-baseline
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - LICENSES/**
  - THIRD_PARTY_NOTICES.md
  - MODIFICATIONS.md
  - supply-chain/use-distribution.json
  - supply-chain/license-verdict.json
  - scripts/license-gate
  - scripts/stage-plugin
  - tests/integration/test_license_gate.py
  - tests/staged/test_supply_chain_inventory.py
  - tests/staged/test_private_exclusions.py
  - tests/fixtures/private-canaries/**
autonomous: true
requirements: [SUP-03, SUP-04, SUP-05]
must_haves:
  truths:
    - "The staged plugin separately identifies ARS/experiment-agent CC BY-NC 4.0, file-base MIT, every bundled C/Python dependency, and every patch."
    - "The post-materialization gate extends the preserved upstream policy/checkers/notice generator and retains raw output, including the pre-vendor receipt link."
    - "Technical qualification may pass while release qualification remains BLOCKED until use/distribution and permission evidence is complete."
    - "Papers, extracted text, run data, credentials, indexes, symlinks, and private canaries never enter the stage."
  artifacts:
    - path: "scripts/license-gate"
      provides: "Extended post-patch/dependency legal inventory and classifier"
    - path: "supply-chain/license-verdict.json"
      provides: "Separate technical and release qualification"
    - path: "tests/staged/test_private_exclusions.py"
      provides: "Private-safe allowlist proof"
  key_links:
    - from: "scripts/license-gate"
      to: "vendor/sources/file-base/scripts/license-gate.sh"
      via: "wrap and execute preserved policy/checkers/notice generator"
      pattern: "license-gate.sh"
    - from: "scripts/license-gate"
      to: "build/evidence/phase-01/pre-vendor-license"
      via: "verify and retain the pre-vendoring receipt"
      pattern: "pre-vendor-license"
    - from: "scripts/stage-plugin"
      to: "supply-chain/license-verdict.json"
      via: "stage requires complete inventory while preserving explicit release BLOCKED"
      pattern: "license-verdict"
---

<objective>
Build the post-materialization legal inventory, private-safe stage, and honest release classifier.

Purpose: Satisfy SUP-03/SUP-04/SUP-05 while preserving the pre-vendor gate and extending the upstream legal baseline over patches and all C/Python dependencies.
Output: Component licenses, generated notices, modifications, SBOM, extended gate, use/distribution record, verdict, stage tests, and raw legal/privacy evidence.
</objective>

## Phase Goal

**As a** workbench operator, **I want to** install and exercise a legally classified, reproducibly sourced headless plugin, **so that** its authority and filesystem boundaries are proven before feature expansion.

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-contract-license-and-executable-baseline/01-CONTEXT.md
@.planning/phases/01-contract-license-and-executable-baseline/01-RESEARCH.md
@.planning/phases/01-contract-license-and-executable-baseline/01-VALIDATION.md
@.planning/phases/01-contract-license-and-executable-baseline/01-01-SUMMARY.md
@.planning/phases/01-contract-license-and-executable-baseline/01-02-SUMMARY.md
@.planning/phases/01-contract-license-and-executable-baseline/01-03-SUMMARY.md

<interfaces>
Canonical staged licenses are `LICENSES/academic-research-skills-CC-BY-NC-4.0.txt`, `LICENSES/experiment-agent-CC-BY-NC-4.0.txt`, and `LICENSES/file-base-MIT.txt`, linked to source paths/digests. `license-verdict.json` has independent `technical_qualification` and `release_qualification`; unresolved SUP-04 means technical PASS and release BLOCKED with reason/evidence-needed fields. The extended gate consumes the current source manifest, patch list, C dependency inventory, Python lock/wheelhouse, native/first-party artifacts, upstream policy/checkers, generated notices, and pre-vendor receipt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute red legal inventory, classifier, and private-stage tests</name>
  <files>tests/integration/test_license_gate.py, tests/staged/test_supply_chain_inventory.py, tests/staged/test_private_exclusions.py, tests/fixtures/private-canaries/**</files>
  <read_first>Plan 01 stage/install scripts; Plan 03 source manifest, pre-vendor receipt, complete file-base legal assets, and drift tests; 01-CONTEXT.md D-04, D-05, D-09; 01-VALIDATION.md legal/supply-chain evidence contract</read_first>
  <behavior>Tests require pre-vendor receipt, component licenses, generated notices, modifications, source manifest, lock-bound CycloneDX SBOM, every patch/C/Python dependency, upstream/extended raw logs, release BLOCKED semantics, and private exclusions.</behavior>
  <action>Write all tests and unique canary fixtures before legal/stage implementation. Cover papers, extracted text, runs, credentials, indexes, VCS metadata, caches, absolute symlinks, and undeclared files. Assert exact source-to-stage license mappings, native policy/checkers/generator execution, generated notices, all current patches and C/Python/new dependencies, and the Task 2 pre-vendor receipt. Assert private repository status never implies noncommercial use per D-05. Execute and explicitly invert the expected RED behavior result.</action>
  <acceptance_criteria>Tests collect and execute red because legal outputs/classifier are absent; every inventory, release-status, preflight, and private class is explicit.</acceptance_criteria>
  <verify><automated>uv run pytest --collect-only -q tests/integration/test_license_gate.py tests/staged/test_supply_chain_inventory.py tests/staged/test_private_exclusions.py &amp;&amp; ! uv run pytest -q tests/integration/test_license_gate.py tests/staged/test_supply_chain_inventory.py tests/staged/test_private_exclusions.py</automated></verify>
  <done>The legal/privacy boundary is observed RED before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend and execute the complete post-materialization license gate</name>
  <files>LICENSES/**, THIRD_PARTY_NOTICES.md, MODIFICATIONS.md, scripts/license-gate, tests/integration/test_license_gate.py, tests/staged/test_supply_chain_inventory.py</files>
  <read_first>Task 1 legal tests; Plan 03 source manifest, pre-vendor receipt, and complete preserved upstream legal gate/assets; Plan 01 pyproject.toml, uv.lock, wheelhouse lock, and first-party wheel; 01-CONTEXT.md D-04/D-05</read_first>
  <behavior>The wrapper verifies the pre-vendor receipt, executes rather than replaces the upstream gate/policy/checkers/notice generator, extends inventory over current patches and C/Python/new dependencies, and retains generated/raw outputs.</behavior>
  <action>Copy canonical component licenses to exact staged paths and preserve attribution/modification markings. Wrap the preserved upstream gate and execute its policy, both checkers, and notice generator against the exact materialized/current patched inputs. Extend inventory for every ordered patch, bundled C dependency, Phase 1 Python dependency/build wheel, first-party wheel, native artifact, schema, and any new dependency. Emit CycloneDX 1.5 from the frozen lock. Verify/link the pre-vendor receipt and retain generated notices, policy/checker outputs, stdout/stderr/status, and inventory diffs. Design the wrapper to consume the current manifest so Plan 06 can rerun it after patch 0002 and any native dependency is added.</action>
  <acceptance_criteria>Every current component/dependency/patch appears in licenses/notices/SBOM/manifests; native and extended gates pass technically; pre-vendor and post-materialization raw evidence is retained.</acceptance_criteria>
  <verify><automated>./scripts/license-gate --source-manifest vendor/source-manifest.json --pre-vendor-evidence build/evidence/phase-01/pre-vendor-license --evidence-root build/evidence/phase-01/license &amp;&amp; uv run pytest -q tests/integration/test_license_gate.py tests/staged/test_supply_chain_inventory.py</automated></verify>
  <done>The exact source/build inventory is attributable through both mandatory gate stages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce private-safe staging and release BLOCKED semantics</name>
  <files>supply-chain/use-distribution.json, supply-chain/license-verdict.json, scripts/stage-plugin, tests/staged/test_supply_chain_inventory.py, tests/staged/test_private_exclusions.py</files>
  <read_first>Plan 01 current stage builder; Task 1 stage tests/canaries; Task 2 licenses/notices/modifications/gate outputs; Plan 03 source manifest and pre-vendor receipt; 01-CONTEXT.md D-05/D-09</read_first>
  <behavior>The stage contains only inventory-covered runtime/legal artifacts, rejects private/undeclared material, and reports technical PASS/release BLOCKED when use/distribution evidence is absent or incompatible.</behavior>
  <action>Create use-distribution.json with intended use, distribution class, permission references, accountable approval, and evidence hashes. Implement the SUP-04 classifier so missing/unknown/incompatible evidence yields technical PASS and release BLOCKED, never false approval. Extend the positive stage allowlist only with verified runtime/wheelhouse/native/schema/legal/SBOM/source-manifest artifacts. Reject symlinks/extras and require every file to be covered by source/build/legal inventories. Preserve raw inventory/diff/canary scan evidence per D-05/D-09.</action>
  <acceptance_criteria>The exact stage contains complete legal/SBOM evidence and no private class; any canary/undeclared file fails; unresolved SUP-04 remains explicitly release BLOCKED.</acceptance_criteria>
  <verify><automated>./scripts/stage-plugin --clean --stage-root build/stage/academic-research-workbench &amp;&amp; uv run pytest -q tests/staged/test_supply_chain_inventory.py tests/staged/test_private_exclusions.py</automated></verify>
  <done>SUP-03/SUP-04/SUP-05 pass with complete attribution, honest release blocking, and private-safe staging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/build inventories to legal outputs | Missing identities can misstate obligations. |
| repository to distributable stage | Private and undeclared files must not cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-LIC | Tampering/Repudiation | license verdict | mitigate | Component identities, two-stage raw gates, and default release BLOCKED. |
| T-04-PRIV | Information Disclosure | stage | mitigate | Positive allowlist, unique canaries, no-symlink rule, full inventory diff. |
| T-04-SUB | Tampering | notices/SBOM | mitigate | Execute preserved native tooling and bind outputs to current manifests. |
| T-04-SC | Tampering | dependency inventory | mitigate | Inventory every patch and C/Python/new dependency; retain raw gate output. |
</threat_model>

<verification>
Run the extended gate and stage tests. Confirm pre-vendor evidence is linked, native tooling actually executed, every current patch/dependency is inventoried, and unresolved SUP-04 remains technical PASS/release BLOCKED.
</verification>

<success_criteria>
SUP-03 through SUP-05 are satisfied without mixed-license collapse, preflight loss, or private leakage.
</success_criteria>

<output>Create `.planning/phases/01-contract-license-and-executable-baseline/01-04-SUMMARY.md` when done.</output>
