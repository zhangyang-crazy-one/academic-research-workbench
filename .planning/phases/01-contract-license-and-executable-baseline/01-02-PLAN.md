---
phase: 01-contract-license-and-executable-baseline
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - hooks/hooks.json
  - skills/academic-research-workbench/SKILL.md
  - src/arw/cli.py
  - src/arw/contracts.py
  - schemas/v1/route-result.schema.json
  - scripts/stage-plugin
  - scripts/smoke-staged-plugin
  - tests/staged/test_skill_route.py
  - tests/staged/test_compatibility_probes.py
autonomous: true
requirements: [PKG-02]
must_haves:
  truths:
    - "A fresh installed Codex process returns a declared ARS workflow family and execution mode."
    - "Every non-authentication route or compatibility failure is corrected, cleanly restaged/reinstalled, and retried."
    - "Hook/custom-agent/disabled-experiment behavior is reported from host probes, with required fallbacks and raw evidence."
  artifacts:
    - path: "schemas/v1/route-result.schema.json"
      provides: "Strict installed route contract"
    - path: "hooks/hooks.json"
      provides: "Supported observational hook companion"
    - path: "scripts/smoke-staged-plugin"
      provides: "Fresh-host probe-adapt-reinstall convergence runner"
  key_links:
    - from: "skills/academic-research-workbench/SKILL.md"
      to: "src/arw/cli.py"
      via: "installed arw route --json command"
      pattern: "arw route"
    - from: "scripts/smoke-staged-plugin"
      to: "scripts/stage-plugin"
      via: "clean restage and reinstall after every owned defect"
      pattern: "stage-plugin"
---

<objective>
Complete the skill route and converge installed Codex host behavior to a successful declared ARS route.

Purpose: Satisfy PKG-02 against fresh installed bytes while treating authentication as the only possible external pause.
Output: Strict route contract, CLI route, observational hook companion, installed skill, compatibility probes, and successful host evidence.
</objective>

## Phase Goal

**As a** workbench operator, **I want to** install and exercise a legally classified, reproducibly sourced headless plugin, **so that** its authority and filesystem boundaries are proven before feature expansion.

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contract-license-and-executable-baseline/01-CONTEXT.md
@.planning/phases/01-contract-license-and-executable-baseline/01-RESEARCH.md
@.planning/phases/01-contract-license-and-executable-baseline/01-VALIDATION.md
@.planning/phases/01-contract-license-and-executable-baseline/01-PATTERNS.md
@.planning/phases/01-contract-license-and-executable-baseline/01-01-SUMMARY.md

<interfaces>
`arw route --json` returns strict schema_version, workflow_family, execution_mode, source_adapter_version=`0.1.19`, and experiment_execution=`disabled`. Native Codex subagents plus immutable assignment-injected roles are the required fallback when custom-agent distribution is not proven. Hooks are observational and cannot write canonical state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute red route and compatibility behavior tests</name>
  <files>tests/staged/test_skill_route.py, tests/staged/test_compatibility_probes.py</files>
  <read_first>Plan 01 exact stage/install scripts and installed launcher tests; 01-VALIDATION.md PKG-02 and host-canary rows; 01-CONTEXT.md D-01, D-02, D-08, D-09; 01-RESEARCH.md Staged Plugin Smoke Path and Open Questions (RESOLVED)</read_first>
  <behavior>A fresh installed host must return schema-valid route JSON and classify hooks, custom-agent support/fallback, and disabled experiment ownership; before implementation both behavior files execute and fail for missing route/compatibility outputs.</behavior>
  <action>Write installed-host tests before route implementation. Require exact installed bytes, fresh process, isolated HOME/CODEX_HOME, no repository/PYTHONPATH/network access, and raw command/stream/status evidence for every attempt. Treat only explicit unauthenticated host output as an external gate. Run collection and then execute both behavior files under an explicit inverted RED assertion; reject collection/import/syntax failures as invalid RED results per D-08/D-09.</action>
  <acceptance_criteria>Both tests collect and execute red for missing route/compatibility behavior, with no source/static substitute and no ambiguous blocking classification.</acceptance_criteria>
  <verify><automated>uv run pytest --collect-only -q tests/staged/test_skill_route.py tests/staged/test_compatibility_probes.py &amp;&amp; ! uv run pytest -q tests/staged/test_skill_route.py tests/staged/test_compatibility_probes.py</automated></verify>
  <done>The installed-host route and compatibility boundary is observed RED before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict route, skill, and observational hook contracts</name>
  <files>hooks/hooks.json, skills/academic-research-workbench/SKILL.md, src/arw/cli.py, src/arw/contracts.py, schemas/v1/route-result.schema.json</files>
  <read_first>Task 1 tests; Plan 01 plugin manifest, bin/arw, and installed skill shell; 01-PATTERNS.md skill/hook/CLI assignments; 01-CONTEXT.md D-01, D-03, D-08</read_first>
  <behavior>The installed skill invokes route JSON; the CLI emits the strict ARS 0.1.19 family/mode contract; experiments remain disabled; hooks remain supported and observational.</behavior>
  <action>Per D-01/D-03/D-08, define strict route models/schema and argparse route command, connect the model-invocable skill to installed `bin/arw route --json`, and create only hook declarations proven by the supported host contract. Hooks and route remain read-only. Record native subagent plus immutable assignment-injected role fallback when custom-agent distribution is unproven; never claim custom-agent support or controlled experiment execution without passing evidence.</action>
  <acceptance_criteria>Route output validates, the installed skill names a declared family/mode, experiment execution is disabled, and no hook/route path can append canonical state.</acceptance_criteria>
  <verify><automated>uv run python -m arw.cli route --json | uv run python -c 'import json,sys; d=json.load(sys.stdin); assert d["workflow_family"] and d["execution_mode"] and d["experiment_execution"] == "disabled"'</automated></verify>
  <done>The package exposes final route, skill, and observational compatibility contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Converge the fresh installed host canary to success</name>
  <files>scripts/stage-plugin, scripts/smoke-staged-plugin, hooks/hooks.json, skills/academic-research-workbench/SKILL.md, tests/staged/test_skill_route.py, tests/staged/test_compatibility_probes.py</files>
  <read_first>Task 1 host tests; Task 2 route/schema/skill/hooks; Plan 01 stage/install scripts and exact-stage tests; plugin-creator install/update/cachebuster references</read_first>
  <behavior>Probe, classification, owned adaptation, clean restage/reinstall, and fresh-process retry continue until final installed route and compatibility probes succeed.</behavior>
  <action>Extend the existing stage/smoke runners with a bounded convergence loop. For each non-authentication defect, adapt only owned skill/hook/route/stage files, cleanly restage, apply the supported cachebuster/reinstall flow, and start a fresh process; do not finish on a blocking non-authentication result. If authentication is absent, emit the precise dynamic gate and resume the same loop afterward. Retain all attempts and identify the final passing installed identity per D-08/D-09.</action>
  <acceptance_criteria>The final fresh installed host returns schema-valid route and compatibility results; every earlier non-auth defect is resolved; raw evidence identifies exact installed bytes.</acceptance_criteria>
  <verify><automated>./scripts/stage-plugin --clean --stage-root build/stage/academic-research-workbench &amp;&amp; ./scripts/smoke-staged-plugin --route --fresh-home build/isolated/route-home --evidence-root build/evidence/phase-01/route &amp;&amp; uv run pytest -q -m codex_host tests/staged/test_skill_route.py tests/staged/test_compatibility_probes.py</automated></verify>
  <done>PKG-02 passes through a successful installed ARS route and evidence-backed compatibility classification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| model request to installed skill | Untrusted text may select only a declared route. |
| stage to host cache | Stale installed bytes could invalidate probe conclusions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-STALE | Tampering | plugin cache | mitigate | Cachebuster/reinstall and fresh process after every adaptation. |
| T-02-REP | Repudiation | host probes | mitigate | Retain every attempt and exact final passing installed identity. |
| T-02-ELEV | Elevation of Privilege | hooks/route | mitigate | Observational read-only behavior; sole writer remains outside this plan. |
| T-02-SC | Tampering | package inputs | mitigate | Reuse Plan 01 audited frozen wheelhouse; no new package source. |
</threat_model>

<verification>
Run installed route/compatibility tests and inspect final evidence for a successful fresh process, exact installed identity, no source/network/PYTHONPATH access, and no unresolved non-authentication result.
</verification>

<success_criteria>
PKG-02 is satisfied by a successful fresh-host ARS route with honest compatibility/fallback evidence.
</success_criteria>

<output>Create `.planning/phases/01-contract-license-and-executable-baseline/01-02-SUMMARY.md` when done.</output>
