---
phase: 01-contract-license-and-executable-baseline
plan: 05
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/arw/cli.py
  - src/arw/models.py
  - src/arw/canonical.py
  - src/arw/journal.py
  - src/arw/evidence.py
  - schemas/v1/run-manifest.schema.json
  - schemas/v1/event.schema.json
  - tests/unit/test_canonical.py
  - tests/integration/test_run_init.py
  - tests/integration/test_journal_replay.py
  - tests/fixtures/recovery/seed/**
autonomous: true
requirements: [RUN-01, RUN-02]
must_haves:
  truths:
    - "An operator initializes a strict run manifest with immutable input identity, workflow mode, schema, and declared capabilities."
    - "Only the short-lived Python CLI appends deterministic revision/sequence/hash-chained canonical JSONL under an inter-process lock."
    - "After SIGKILL immediately after journal fsync, a fresh process replays the durable last revision/hash with no duplicate event."
  artifacts:
    - path: "src/arw/journal.py"
      provides: "Sole-writer lock, append, fsync, and replay path"
    - path: "schemas/v1/event.schema.json"
      provides: "Strict two-event canonical contract"
    - path: "tests/fixtures/recovery/seed/"
      provides: "Deterministic init/append/forced-stop/replay fixture"
  key_links:
    - from: "src/arw/cli.py"
      to: "src/arw/journal.py"
      via: "only init/append/replay mutation commands"
      pattern: "journal"
    - from: "src/arw/journal.py"
      to: "src/arw/canonical.py"
      via: "hash exact appended bytes"
      pattern: "canonical_event_bytes"
---

<objective>
Implement the canonical run writer and forced-stop replay proof.

Purpose: Satisfy RUN-01/RUN-02 while keeping projections, hooks, workers, transcripts, and evidence summaries non-authoritative.
Output: Strict run/event schemas, deterministic serializer, sole-writer journal, CLI commands, recovery fixtures, tests, and raw evidence.
</objective>

## Phase Goal

**As a** workbench operator, **I want to** install and exercise a legally classified, reproducibly sourced headless plugin, **so that** its authority and filesystem boundaries are proven before feature expansion.

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contract-license-and-executable-baseline/01-CONTEXT.md
@.planning/phases/01-contract-license-and-executable-baseline/01-RESEARCH.md
@.planning/phases/01-contract-license-and-executable-baseline/01-VALIDATION.md
@.planning/phases/01-contract-license-and-executable-baseline/01-02-SUMMARY.md

<interfaces>
Phase 1 admits only `run.initialized` and `baseline.probe_recorded`. Canonical JSON is sorted compact UTF-8 with ensure_ascii=false, allow_nan=false, and exactly one trailing newline. The first prev_event_sha256 is 64 zeroes. Hashing excludes event_sha256 and covers the exact bytes appended.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict run/event contracts and execute red canonical tests</name>
  <files>schemas/v1/run-manifest.schema.json, schemas/v1/event.schema.json, tests/unit/test_canonical.py, tests/integration/test_run_init.py, tests/fixtures/recovery/seed/**</files>
  <read_first>Plan 02 src/arw/cli.py, route contract, and installed launcher boundary; 01-VALIDATION.md Recovery Fixture and RUN rows; 01-CONTEXT.md D-02, D-03, D-07, D-09; 01-RESEARCH.md deterministic event and sole-writer patterns</read_first>
  <behavior>Strict schemas reject coercion, unknown fields, NaN, invalid IDs/hashes/paths; fixed fixtures produce exact canonical bytes/hash; init requires immutable input digest, mode, capabilities, and schema version.</behavior>
  <action>Write checked-in Draft 2020-12 run/event schemas, fixed recovery fixtures, and model/canonical/init tests before production runtime files. Assert only two event discriminators, fixed IDs/timestamps for byte fixtures, relative immutable input paths, the zero previous hash, and no canonical writes from route/hook/evidence code per D-03. Execute and explicitly invert the expected RED tests; reject collection/import/syntax failure as invalid RED.</action>
  <acceptance_criteria>Tests collect and execute red because runtime models/serializer/init are absent; expected byte/hash fixtures and authority exclusions are explicit.</acceptance_criteria>
  <verify><automated>uv run pytest --collect-only -q tests/unit/test_canonical.py tests/integration/test_run_init.py &amp;&amp; ! uv run pytest -q tests/unit/test_canonical.py tests/integration/test_run_init.py</automated></verify>
  <done>The run/event authority contract is observed RED before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict init and sole-writer append/replay</name>
  <files>src/arw/cli.py, src/arw/models.py, src/arw/canonical.py, src/arw/journal.py</files>
  <read_first>Plan 02 src/arw/cli.py; Task 1 schemas/tests/fixtures; 01-CONTEXT.md D-03; 01-RESEARCH.md Sole-Writer Append Protocol</read_first>
  <behavior>Init writes immutable manifest and first event; append locks, fully replays/validates, checks expected revision, writes one canonical line, flushes/fsyncs, and releases; replay requires only manifest and JSONL.</behavior>
  <action>Implement strict Pydantic models with ConfigDict(strict=True, extra='forbid'), canonical serialization, and portalocker-protected journal operations. Extend the existing CLI without changing route behavior. Validate sequence, expected revision, previous hash, and complete tail before writing; append once and fsync; contention/stale revision/malformed tail fail without mutation. Keep derived outputs outside authority per D-03.</action>
  <acceptance_criteria>Canonical fixtures are byte-identical; init/append/replay emit schema-valid JSON; invalid writes leave bytes unchanged; only the CLI journal path appends.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/unit/test_canonical.py tests/integration/test_run_init.py</automated></verify>
  <done>The Python CLI is the sole deterministic canonical writer for both Phase 1 event types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove post-fsync kill and duplicate-free fresh-process replay</name>
  <files>src/arw/cli.py, src/arw/journal.py, src/arw/evidence.py, tests/integration/test_journal_replay.py, tests/fixtures/recovery/seed/**</files>
  <read_first>Task 1 recovery fixtures; Task 2 CLI/journal/canonical implementation and tests; 01-VALIDATION.md Recovery Fixture and Evidence Contract; 01-CONTEXT.md D-09</read_first>
  <behavior>A test-only failpoint sends SIGKILL after journal fsync and before derived output; a fresh process replays manifest+JSONL, returns last revision/hash, leaves bytes unchanged, and appends no duplicate.</behavior>
  <action>Write and execute the replay test before the failpoint implementation, require the expected RED assertion, then add the narrowly scoped post-fsync failpoint and allowlisted evidence capture. Preserve init/append/kill/replay argv, relative cwd, selected environment keys, streams, signal/status, journal bytes/hashes before/after, replay JSON, and verdict. Replay starts fresh without projection/chat state and rejects duplicate or changed accepted bytes per D-09.</action>
  <acceptance_criteria>SIGKILL occurs at the prescribed boundary; replay finds the durable event exactly once; raw evidence proves unchanged journal bytes and last revision/hash.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/integration/test_journal_replay.py</automated></verify>
  <done>RUN-01/RUN-02 include inspectable forced-stop and duplicate-free replay evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI request to canonical journal | Untrusted command data attempts durable mutation. |
| competing process to run lock | Multiple writers contend for authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-WRITER | Tampering/Repudiation | journal | mitigate | Strict models, expected revision, lock, sequence/hash validation, fsync. |
| T-05-CRASH | Tampering | append/replay | mitigate | Post-fsync SIGKILL fixture and duplicate-free fresh-process replay. |
| T-05-PRIV | Information Disclosure | evidence | mitigate | Allowlist environment fields and repository-owned fixtures only. |
| T-05-SC | Tampering | runtime packages | mitigate | Reuse Plan 01 audited wheelhouse; no new install source. |
</threat_model>

<verification>
Run canonical/init/replay tests and inspect kill/replay evidence for the signal boundary, unchanged journal bytes, one durable event, and last valid revision/hash.
</verification>

<success_criteria>
RUN-01/RUN-02 pass through strict initialization, sole-writer deterministic append, and fresh-process replay after forced termination.
</success_criteria>

<output>Create `.planning/phases/01-contract-license-and-executable-baseline/01-05-SUMMARY.md` when done.</output>
