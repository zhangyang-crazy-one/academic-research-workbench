---
phase: 01-contract-license-and-executable-baseline
plan: 07
type: execute
wave: 5
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05, 01-06]
files_modified:
  - src/arw/schema_registry.py
  - schemas/v1/build-identity.schema.json
  - schemas/v1/version-report.schema.json
  - src/arw/cli.py
  - bin/arw
  - scripts/stage-plugin
  - tests/schema/test_schema_drift.py
  - tests/schema/test_cross_language.py
  - tests/integration/test_version_report.py
  - tests/integration/test_phase1_walking_skeleton.py
  - scripts/verify-phase-1
autonomous: true
requirements: [PKG-04, VER-01]
must_haves:
  truths:
    - "Checked-in Draft 2020-12 schemas regenerate deterministically and independently validate Python and native MCP fixtures."
    - "A packaged build identity is generated only after source/native build, native safety suites, legal gates, and schema generation complete."
    - "One installed version command reports plugin, runtime, ARS, experiment-agent, file-base, patches, schemas, wheels, and Linux claim from packaged identity."
    - "A clean integrated evidence run includes pre-vendor legal, source, upstream C, ASan+UBSan, separate TSan, install, route, writer, confinement, schema, and private-stage evidence."
    - "Technical qualification is separate from release qualification and release remains BLOCKED while SUP-04 evidence is absent."
  artifacts:
    - path: "schemas/v1/build-identity.schema.json"
      provides: "Final packaged build identity contract"
    - path: "src/arw/schema_registry.py"
      provides: "Deterministic schema generation and aggregate digest"
    - path: "scripts/verify-phase-1"
      provides: "Clean integrated evidence gate including native safety runs"
  key_links:
    - from: "scripts/stage-plugin"
      to: "share/arw/build-identity.json"
      via: "generate only after all source/schema/native/legal inputs pass"
      pattern: "build-identity"
    - from: "src/arw/cli.py"
      to: "share/arw/build-identity.json"
      via: "stage-relative ARW_BUILD_IDENTITY packaged resource"
      pattern: "version"
    - from: "scripts/verify-phase-1"
      to: "build/evidence/phase-01"
      via: "raw domain evidence and separate technical/release summaries"
      pattern: "native/tsan"
---

<objective>
Finalize independent schemas, packaged build identity, installed version reporting, and the clean integrated evidence gate.

Purpose: Satisfy VER-01 and PKG-04 only after every identity-bearing source, legal, native-safety, schema, package, and stage input exists and passes.
Output: Schema registry/tests, final build identity, installed version command, integrated phase gate, and technical/release summaries.
</objective>

## Phase Goal

**As a** workbench operator, **I want to** install and exercise a legally classified, reproducibly sourced headless plugin, **so that** its authority and filesystem boundaries are proven before feature expansion.

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-contract-license-and-executable-baseline/01-CONTEXT.md
@.planning/phases/01-contract-license-and-executable-baseline/01-RESEARCH.md
@.planning/phases/01-contract-license-and-executable-baseline/01-VALIDATION.md
@.planning/phases/01-contract-license-and-executable-baseline/01-01-SUMMARY.md
@.planning/phases/01-contract-license-and-executable-baseline/01-02-SUMMARY.md
@.planning/phases/01-contract-license-and-executable-baseline/01-03-SUMMARY.md
@.planning/phases/01-contract-license-and-executable-baseline/01-04-SUMMARY.md
@.planning/phases/01-contract-license-and-executable-baseline/01-05-SUMMARY.md
@.planning/phases/01-contract-license-and-executable-baseline/01-06-SUMMARY.md

<interfaces>
The build identity is generated at `share/arw/build-identity.json` after pre-vendor/source/legal/native/schema checks. It includes plugin/runtime versions, Python range/build interpreter, ARS/experiment/file-base revisions and trees, ordered patch hashes, native binary hash, wheelhouse/lock hashes, per-schema and aggregate schema hashes, native upstream/ASan+UBSan/TSan evidence digests, and every staged payload digest except itself. `bin/arw` exports stage-relative `ARW_BUILD_IDENTITY`; `arw version --json` rejects absent, outside-root, or inconsistent resources and never queries source checkouts. A final inventory records the identity digest without self-reference.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce deterministic schemas and independent Python/native validation</name>
  <files>src/arw/schema_registry.py, schemas/v1/build-identity.schema.json, schemas/v1/version-report.schema.json, tests/schema/test_schema_drift.py, tests/schema/test_cross_language.py</files>
  <read_first>Plan 02 route schema/tests; Plan 03 source-manifest schema/manifest; Plan 05 run/event schemas and fixtures; Plan 06 MCP schemas, direct-native success/denial fixtures, and native safety evidence contracts; 01-CONTEXT.md D-07</read_first>
  <behavior>All eight Phase 1 schemas pass Draft202012Validator.check_schema; regeneration is byte-stable; Python and native fixtures validate independently; incompatible edits fail.</behavior>
  <action>Write strict build-identity/version-report contracts and drift/cross-language tests before the registry. Execute and invert the expected RED result. Then implement deterministic generation of all eight schemas and per-file/aggregate identity, normalizing only proven unstable generator metadata. Validate fixtures with jsonschema rather than Pydantic and include native upstream/sanitizer evidence digests in build identity. Do not permit unknown fields or deferred contracts per D-07/VER-01.</action>
  <acceptance_criteria>All schemas are valid Draft 2020-12, regeneration has no diff, Python/native fixtures validate independently, and deliberate schema/fixture drift fails.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/schema/test_schema_drift.py tests/schema/test_cross_language.py</automated></verify>
  <done>VER-01 blocks incompatible Python/native contract drift through an independent validator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate final packaged build identity and installed version JSON</name>
  <files>src/arw/cli.py, bin/arw, scripts/stage-plugin, tests/integration/test_version_report.py</files>
  <read_first>Plan 01 launcher/stage/wheelhouse; Plan 02 route CLI; Plan 03 source manifest and pre-vendor receipt; Plan 04 legal verdict; Plan 05 runtime; Plan 06 native binary/patch/safety evidence; Task 1 schema registry and aggregate digest</read_first>
  <behavior>Stage generation fails unless all upstream, legal, native-suite, and schema identities exist; installed version reads only packaged identity and reports every required identity.</behavior>
  <action>Write and execute installed version tests before implementation and require expected RED. Generate build identity only after source/native build, unchanged upstream suite, ASan+UBSan, separate TSan, post-patch legal inventory, wheelhouse verification, and deterministic schemas pass. Bind all evidence digests and staged payload entries except identity, then write the final inventory containing identity digest. Export only stage-relative identity and use its digest as launcher cache key. Do not add fallback constants or source queries. Test from isolated installed bytes per D-02/D-07/D-09.</action>
  <acceptance_criteria>Identity validates and contains all required versions/digests including three native evidence domains; installed version matches it; any missing/mutated input fails before PKG-04.</acceptance_criteria>
  <verify><automated>./scripts/stage-plugin --clean --stage-root build/stage/academic-research-workbench &amp;&amp; uv run pytest -q tests/integration/test_version_report.py</automated></verify>
  <done>PKG-04 is claimed only from the complete final packaged identity.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the clean integrated walking-skeleton evidence gate</name>
  <files>tests/integration/test_phase1_walking_skeleton.py, scripts/verify-phase-1</files>
  <read_first>All Plan 01-06 SUMMARYs; current pre-vendor/source/offline/build/license/stage/smoke/launcher scripts; current route/version/runtime/confinement/schema/native/stage tests; 01-VALIDATION.md full Evidence Contract and Native Safety Gate</read_first>
  <action>Implement the clean orchestrator and end-to-end assertion. In a fresh verification workspace, run the exact pinned pre-vendor license gate before temporary source materialization, then network-denied source verification/build, post-patch extended legal gate, unchanged upstream C suite, clean ASan+UBSan suite, distinct clean TSan suite, exact stage/install, successful fresh-host route, installed version, runtime init/append/post-fsync kill/replay, installed MCP bounded read/denial matrix, schema/digest/inventory/private scans, and raw evidence checks. Every native suite runs through offline-exec, preserves unchanged test hashes and distinct flags/streams/status/network audits, and is a hard technical-PASS prerequisite. Preserve all domains under `build/evidence/phase-01/&lt;build-identity-sha256&gt;`. Emit summary.json/SUMMARY.md with independent technical/release fields; unresolved SUP-04 remains release BLOCKED with exact evidence needed.</action>
  <acceptance_criteria>The clean installed skeleton exercises every domain; technical PASS requires pre-vendor, upstream C, ASan+UBSan, TSan, and all other automated evidence; unresolved legal evidence remains release BLOCKED; forbidden paths/secrets are absent.</acceptance_criteria>
  <verify><automated>./scripts/verify-phase-1 --clean --evidence-root build/evidence/phase-01</automated><human-check>Only accountable review of use-distribution.json and authentic permission evidence may change release BLOCKED; absent that evidence, BLOCKED is the correct completed result.</human-check></verify>
  <done>The clean phase gate emits complete raw evidence and honest technical PASS/release BLOCKED semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| model generators to checked-in schemas | Self-validation could hide contract drift. |
| source/legal/native/schema outputs to identity | Missing or stale inputs could be misreported. |
| raw evidence to summaries | Aggregation could hide a failed or absent native/security gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-SCHEMA | Tampering | schema registry | mitigate | Independent jsonschema validation and byte-stable drift tests. |
| T-07-ID | Spoofing/Tampering | build identity | mitigate | Generate after all gates and bind every input/evidence digest. |
| T-07-REP | Repudiation | summary | mitigate | Require raw domain evidence and separate technical/release fields. |
| T-07-PRIV | Information Disclosure | evidence/stage | mitigate | Re-run allowlist/canary/forbidden-path scans before technical PASS. |
| T-07-SC | Tampering | integrated build | mitigate | Pre-vendor gate, network denial, frozen wheelhouse, extended legal gate, and all native suites. |
</threat_model>

<verification>
Run schema/version tests and the clean phase gate. Confirm build identity follows every input gate, all three native evidence domains are present and passing, and release BLOCKED persists when SUP-04 evidence is unresolved.
</verification>

<success_criteria>
VER-01/PKG-04 pass from independent schemas and complete packaged identity, and the clean installed phase emits all mandatory technical/release evidence.
</success_criteria>

<output>Create `.planning/phases/01-contract-license-and-executable-baseline/01-07-SUMMARY.md` when done.</output>
