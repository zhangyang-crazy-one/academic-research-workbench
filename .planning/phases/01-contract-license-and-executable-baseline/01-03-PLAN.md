---
phase: 01-contract-license-and-executable-baseline
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - schemas/v1/source-manifest.schema.json
  - scripts/pre-vendor-license-gate
  - scripts/verify-sources
  - scripts/offline-exec
  - scripts/build-file-base
  - vendor/sources/academic-research-skills/**
  - vendor/sources/experiment-agent/**
  - vendor/sources/file-base/**
  - vendor/patches/file-base/0001-file-base-server-name.patch
  - vendor/source-manifest.json
  - tests/integration/test_pre_vendor_license_gate.py
  - tests/integration/test_source_materialization.py
  - tests/integration/test_digest_drift.py
autonomous: true
requirements: [SUP-01, SUP-02]
must_haves:
  truths:
    - "Before vendor/sources is populated, the exact pinned clean upstream sources pass their native license gate, policy, checkers, and notice generator with raw evidence retained."
    - "A maintainer reconstructs exact ARS, experiment-agent, and file-base trees from checked-in inputs and ordered patches without network access."
    - "Any source tree, patch, lock, generated binary, legal-input, or declared artifact digest mutation fails before staging."
  artifacts:
    - path: "scripts/pre-vendor-license-gate"
      provides: "Mandatory exact-pin legal preflight before source-tree copy"
    - path: "vendor/source-manifest.json"
      provides: "Exact source, patch-order, legal-input, lock, and artifact identities"
    - path: "scripts/offline-exec"
      provides: "Linux network-denied and network-syscall-audited runner"
    - path: "scripts/build-file-base"
      provides: "Clean temporary source reconstruction and build"
  key_links:
    - from: "scripts/pre-vendor-license-gate"
      to: "vendor/sources/**"
      via: "passing raw-evidence receipt required before materialization"
      pattern: "pre-vendor-license"
    - from: "scripts/build-file-base"
      to: "vendor/source-manifest.json"
      via: "pre/post tree and ordered patch digest verification"
      pattern: "source-manifest"
---

<objective>
Execute the mandatory pinned upstream license preflight before vendoring, then materialize and verify the offline digest-closed source chain.

Purpose: Satisfy SUP-01/SUP-02 while enforcing AGENTS.md ordering: no upstream source tree enters vendor/sources until the exact clean upstream legal gate passes and evidence is retained.
Output: Pre-vendoring legal receipt, source manifest/schema, exact snapshots and patch, offline verification/build scripts, mutation tests, and raw source evidence.
</objective>

## Phase Goal

**As a** workbench operator, **I want to** install and exercise a legally classified, reproducibly sourced headless plugin, **so that** its authority and filesystem boundaries are proven before feature expansion.

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-contract-license-and-executable-baseline/01-CONTEXT.md
@.planning/phases/01-contract-license-and-executable-baseline/01-RESEARCH.md
@.planning/phases/01-contract-license-and-executable-baseline/01-VALIDATION.md
@.planning/phases/01-contract-license-and-executable-baseline/01-PATTERNS.md
@.planning/phases/01-contract-license-and-executable-baseline/01-01-SUMMARY.md

<interfaces>
The pre-vendor receipt binds exact upstream URL/revision/tree identities, native file-base `scripts/license-gate.sh`, `scripts/license-policy.json`, both checkers, `gen-third-party-notices.sh`, root/dependency license inputs, generated notices, argv/streams/status, and confirms `vendor/sources/**` was absent throughout. The source manifest records ARS `c22c17eed8a5753aa60681be9734919f2e2f5b42`, experiment-agent `9b063fa895eaf1f63ac99ac03f924f8d31aa8d26`, file-base `ee68144af5453addda995a27cce8142999f318fb`, tree hashes, legal receipt digest, ordered patch hashes, lock/wheelhouse hashes, and built artifact hashes. Patch 0001 SHA-256 is `dd6022c69819804db015019058feaecebf0ee9c31e5cc55eb8bad6b47003da1a`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define pre-vendor, source, drift, and offline behavior contracts</name>
  <files>schemas/v1/source-manifest.schema.json, tests/integration/test_pre_vendor_license_gate.py, tests/integration/test_source_materialization.py, tests/integration/test_digest_drift.py</files>
  <read_first>AGENTS.md License gate — mandatory before vendoring and file-base test evidence; Plan 01 pyproject.toml, uv.lock, and wheelhouse lock; 01-CONTEXT.md D-04, D-05, D-07, D-09; 01-RESEARCH.md Source/Patch/License Manifest and Package Legitimacy Audit; 01-VALIDATION.md SUP-01/SUP-02 rows</read_first>
  <behavior>The preflight test requires exact clean pins, native gate/policy/checkers/notice generation, absent vendor/sources, and raw evidence; source tests require later network-denied reconstruction and independent drift failures.</behavior>
  <action>Write the strict source-manifest schema and all behavior tests before any source copy. Assert the pre-vendor receipt is created from exact clean temporary trees, includes every native legal tool/input/output, and proves `vendor/sources/**` was not present before or during execution. Require Linux network namespace denial plus strace audit for later verification/build. Mutation fixtures alter source, patch, legal receipt/input, lock, wheelhouse, binary, and artifact classes one at a time and require failure before stage creation per D-04/D-09. Execute the tests and invert the expected RED result; collection/import/syntax errors are not valid RED.</action>
  <acceptance_criteria>Tests collect and execute red because the preflight/materialization scripts and sources are absent; every ordering, digest, legal-input, and network-denial assertion is explicit.</acceptance_criteria>
  <verify><automated>uv run pytest --collect-only -q tests/integration/test_pre_vendor_license_gate.py tests/integration/test_source_materialization.py tests/integration/test_digest_drift.py &amp;&amp; ! uv run pytest -q tests/integration/test_pre_vendor_license_gate.py tests/integration/test_source_materialization.py tests/integration/test_digest_drift.py</automated></verify>
  <done>The pre-vendoring and reproducibility boundaries are observed RED before source materialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute and retain the exact pinned pre-vendoring license gate</name>
  <files>scripts/pre-vendor-license-gate, tests/integration/test_pre_vendor_license_gate.py</files>
  <read_first>Task 1 pre-vendor test and source-manifest schema; AGENTS.md mandatory-before-vendoring table; exact clean upstream repositories at the three locked revisions; file-base root LICENSE, scripts/license-gate.sh, scripts/license-policy.json, scripts/gen-third-party-notices.sh, scripts/license-gate-check.py, scripts/license-gate-check-npm.py, vendored/*/LICENSE*, and vendored/nomic/NOTICE; canonical ARS/experiment license inputs from 01-RESEARCH.md Open Questions (RESOLVED)</read_first>
  <behavior>The script reconstructs exact clean temporary source trees outside vendor/sources, verifies revisions/trees, runs the unmodified native file-base gate/policy/checkers/notice generator, verifies ARS/experiment licenses, and emits a digest-bound raw receipt.</behavior>
  <action>Implement `scripts/pre-vendor-license-gate` as the hard predecessor to all source copying. Fail if `vendor/sources/**` already exists. Reconstruct the three exact pinned clean trees in a temporary location from the registered upstream inputs, verify upstream URL/revision/tree identity, and run file-base's unmodified `license-gate.sh` so its policy, Python/npm checkers, and notice generator actually execute. Validate canonical ARS/experiment license files and modification/attribution duties. Capture argv, tool identities, source trees, every legal input digest, generated notices, stdout/stderr/status, and the absence of vendor/sources before deleting the temporary trees. Emit an immutable receipt under `build/evidence/phase-01/pre-vendor-license/`; do not copy any source tree in this task.</action>
  <acceptance_criteria>The exact clean upstream legal gate passes with complete raw evidence, vendor/sources remains absent, and the receipt binds all required policy/checker/generator/license identities.</acceptance_criteria>
  <verify><automated>test ! -d vendor/sources &amp;&amp; ./scripts/pre-vendor-license-gate --clean --evidence-root build/evidence/phase-01/pre-vendor-license &amp;&amp; test ! -d vendor/sources &amp;&amp; uv run pytest -q tests/integration/test_pre_vendor_license_gate.py</automated></verify>
  <done>AGENTS.md's mandatory pre-vendoring legal gate has executed and its exact-pin evidence is retained before source copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Materialize exact sources and enforce offline digest drift failure</name>
  <files>vendor/sources/academic-research-skills/**, vendor/sources/experiment-agent/**, vendor/sources/file-base/**, vendor/patches/file-base/0001-file-base-server-name.patch, vendor/source-manifest.json, scripts/verify-sources, scripts/offline-exec, scripts/build-file-base, tests/integration/test_source_materialization.py, tests/integration/test_digest_drift.py</files>
  <read_first>Task 2 passing pre-vendor receipt and test; Task 1 schema/source/drift tests; exact upstream inputs named in Task 2; Plan 01 uv.lock and wheelhouse lock; Paper4Master patch and build wrapper; 01-CONTEXT.md D-04/D-05/D-09</read_first>
  <behavior>Only a valid pre-vendor receipt unlocks source copy; exact snapshots and 0001 are materialized; verify/build run from checked-in inputs under network denial; each digest mutation fails before output promotion.</behavior>
  <action>Require and verify the Task 2 receipt digest before creating `vendor/sources/**`. Materialize immutable ARS, experiment-agent, and file-base snapshots at locked revisions; use canonical component license paths and preserve the complete upstream legal baseline. Copy the Paper4Master patch byte-for-byte as 0001 and populate exact tree/file/legal-receipt digests. Implement strict-shell verification and clean temporary reconstruction with ordered `git apply --check`/`git apply`. Implement offline-exec to fail closed unless bwrap/unshare network denial and strace network auditing are active, reject AF_INET/AF_INET6 attempts, and retain raw evidence. Build only through this runner, atomically promote the verified binary, and make every independent drift mutation fail per SUP-01/SUP-02.</action>
  <acceptance_criteria>Preflight evidence precedes vendor creation; exact sources/0001 match pins; a clean network-denied build succeeds; all mutation classes fail before staging; raw source/build evidence is retained.</acceptance_criteria>
  <verify><automated>./scripts/offline-exec --evidence-root build/evidence/phase-01/source ./scripts/verify-sources &amp;&amp; ./scripts/offline-exec --evidence-root build/evidence/phase-01/build ./scripts/build-file-base --clean &amp;&amp; uv run pytest -q tests/integration/test_source_materialization.py tests/integration/test_digest_drift.py</automated></verify>
  <done>SUP-01/SUP-02 pass through preflight-gated, checked-in-only, network-denied source reconstruction and drift checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| external upstream bytes to pre-vendor receipt | Exact clean sources become eligible only after native legal tooling passes. |
| pre-vendor receipt to checked-in vendor trees | No source copy is allowed without the bound passing receipt. |
| vendor trees to native build | Clean ordered reconstruction produces the artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-SUB | Spoofing/Tampering | upstream/source/patch identity | mitigate | Exact URL/revision/tree/patch/receipt hashes and mutation tests. |
| T-03-LIC | Tampering/Repudiation | pre-vendor gate | mitigate | Execute unmodified native policy/checkers/generator and retain raw outputs before copy. |
| T-03-NET | Information Disclosure/Tampering | offline reconstruction | mitigate | Network namespace denial plus strace AF_INET/AF_INET6 audit. |
| T-03-SC | Tampering | source/build dependencies | mitigate | Frozen checked-in inputs, no runtime clone, and digest-closed artifact promotion. |
</threat_model>

<verification>
Confirm the pre-vendor receipt timestamp/digest precedes vendor/sources creation, then run network-denied verify/build and all three integration tests; inspect native legal and syscall evidence.
</verification>

<success_criteria>
SUP-01/SUP-02 are proven without violating the mandatory license-before-vendoring gate.
</success_criteria>

<output>Create `.planning/phases/01-contract-license-and-executable-baseline/01-03-SUMMARY.md` when done.</output>
