---
phase: 04-subagent-orchestration-hooks-and-human-gates
plan: 07
type: execute
wave: 5
depends_on: [04-05, 04-06]
files_modified:
  - src/arw/execution.py
  - scripts/stage-plugin
  - scripts/smoke-staged-plugin
  - tests/staged/test_phase4_host_qualification.py
autonomous: true
requirements: [PKG-05, AGT-01, AGT-02, AGT-04, AGT-05, AGT-06, AGT-07]
user_setup:
  - service: Codex CLI
    why: "Exact-host qualification requires an authenticated Codex process in three fresh isolated homes."
    env_vars:
      - name: CODEX_HOME
        source: "The staged canary creates fresh temporary homes; do not point it at a research runtime or canonical control root."
    dashboard_config:
      - task: "Authenticate the local Codex CLI with `codex login` before the required fresh-host canary."
        location: "Local Codex CLI authentication flow"
must_haves:
  truths:
    - "A thin native Codex adapter dispatches an immutable assignment into an isolated attempt, returns an observation and direct proposal path, and never promotes transcript text to canonical evidence."
    - "Formal host identity/assignment mapping is proven by a fresh staged canary or the workflow records `blocked`; assignment injection or inline output is explicitly `degraded_inline`."
    - "The exact staged plugin includes Phase 4 schemas and the observational hook executable, and host qualification keeps authentication material/private runtime state out of staged artifacts and retained evidence."
  artifacts:
    - path: src/arw/execution.py
      provides: "Codex-native adapter implementing the previously defined host-neutral protocol"
    - path: tests/staged/test_phase4_host_qualification.py
      provides: "Three-fresh-home formal/degraded/blocked host qualification matrix"
    - path: scripts/smoke-staged-plugin
      provides: "Exact staged host canary with redacted raw evidence"
  key_links:
    - from: src/arw/execution.py
      to: attempts/<attempt-id>/result/proposal.json
      via: "adapter reports a direct path; parent validates bytes in OrchestrationService"
      pattern: "proposal.json"
    - from: scripts/smoke-staged-plugin
      to: scripts/stage-plugin
      via: "three fresh homes install byte-identical stage before host canary"
      pattern: "installed_from_exact_stage"
    - from: scripts/stage-plugin
      to: hooks/arw_hook.py
      via: "positive allowlist includes the observation executable and Phase 4 schemas"
      pattern: "arw_hook.py"
---

<objective>
Add the thin Codex host adapter and exact staged host qualification that distinguishes verified native independence from degraded fallback or blocked operation without allowing host transcripts to become canonical evidence.

Purpose: Native-host behavior is an external trust boundary. The project must prove it from exact installed bytes and fresh processes, or preserve an honest blocked/degraded result.
Output: `CodexNativeExecutionAdapter`, staged allowlist updates, three-home host canary, and qualification tests/evidence classification.
</objective>

## Phase Goal

**As a** research-workflow owner, **I want to** operate specialized subagents, independent reviews, hooks, and human decisions through immutable proposals under a sole-writer canonical runtime, **so that** every result is auditable, replayable, deterministic, and accountable.

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-subagent-orchestration-hooks-and-human-gates/04-CONTEXT.md
@.planning/phases/04-subagent-orchestration-hooks-and-human-gates/04-AI-SPEC.md
@.planning/phases/04-subagent-orchestration-hooks-and-human-gates/04-RESEARCH.md
@.planning/phases/04-subagent-orchestration-hooks-and-human-gates/04-05-SUMMARY.md
@.planning/phases/04-subagent-orchestration-hooks-and-human-gates/04-06-SUMMARY.md

<interfaces>
`ExecutionAdapter.dispatch`, `request_cancel`, and `force_terminate` already exist from 04-03; `OrchestrationService` from 04-04 owns result validation and journal append. The native adapter returns `HostResult` with a proposed direct path and observation only. It must never parse `codex exec --json` or an output-last-message response as a `WorkerProposal`; only the parent can read and validate `attempts/<attempt-id>/result/proposal.json`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>P04-07-T01 — Implement the thin Codex adapter with honest formal/degraded/blocked classification</name>
  <files>src/arw/execution.py, tests/staged/test_phase4_host_qualification.py</files>
  <read_first>src/arw/execution.py; src/arw/orchestration.py; src/arw/orchestration_models.py; tests/staged/test_phase4_host_qualification.py; pyproject.toml `codex_host` marker; `codex exec --help`; 04-AI-SPEC.md sections 3, 5, 12, and 14; 04-RESEARCH.md "Codex-host proof"; 04-CONTEXT.md D-02, D-05, D-08, and D-09 through D-10</read_first>
  <behavior>
    - The adapter starts a separate `codex exec` process in the attempt scratch directory with `--ephemeral`, configured profile/permission digest, one-depth/four-thread limits, and only the immutable assignment protocol prompt/path.
    - The adapter returns process/host/profile/permission observations plus the expected direct proposal path; stdout/stderr/JSON events are retained as observations and cannot supply a proposal body.
    - Missing stable host identity, missing assignment-to-host mapping, unavailable configured profile, or a non-native fallback returns `blocked` or `degraded_inline`; neither may satisfy formal panel identity requirements.
    - Cooperative cancellation signals the isolated process group; force termination occurs only after the frozen grace and an observed qualified termination outcome.
  </behavior>
  <action>Implement `CodexNativeExecutionAdapter` behind the existing protocol, with an explicit immutable native-host configuration containing profile name, sandbox/permission digest, hook configuration digest, and the frozen `agents.max_depth=1`/`agents.max_threads=4` settings. Launch a fresh `codex exec` process in the assignment's scratch root using `--ephemeral`, the configured profile, a workspace-write sandbox limited to that attempt, and the assignment-injected protocol that tells the host to write only `result/proposal.json`; pass no canonical control root and no worker credential that can append events. Record command/version/profile/permission/process observations, but do not use transcript text, `--output-last-message`, or generated UUIDs as proposal/identity authority. Require a documented stable host agent identity and assignment mapping for `native_formal`; if the installed host cannot expose both, emit an evidence-bearing `blocked` qualification. Support the D-02 assignment-injected fallback only as `degraded_inline`, which cannot satisfy D-09/D-10 formal independence. Implement process-group cooperative cancel then qualified force termination after the frozen grace; return any late file as observation evidence for parent stale rejection.</action>
  <acceptance_criteria>The adapter conforms to the host-neutral interface, keeps the proposal/file boundary parent-validated, has no transcript-to-canonical route, and mechanically refuses a formal claim unless stable host identity and assignment mapping are demonstrated.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 uv run --frozen pytest -q tests/staged/test_phase4_host_qualification.py -m 'not codex_host'</automated></verify>
  <done>Native Codex execution is represented as a narrow, testable external adapter with truthful qualification states.</done>
</task>

<task type="auto" tdd="true">
  <name>P04-07-T02 — Qualify exact staged bytes in three fresh Codex homes and retain redacted evidence</name>
  <files>scripts/stage-plugin, scripts/smoke-staged-plugin, tests/staged/test_phase4_host_qualification.py</files>
  <read_first>scripts/stage-plugin; scripts/smoke-staged-plugin; tests/staged/test_phase4_host_qualification.py; hooks/hooks.json; hooks/arw_hook.py; schemas/v1/host-qualification.schema.json; schemas/v1/hook-observation.schema.json; 04-AI-SPEC.md sections 12 through 14; 04-RESEARCH.md "Staged host probe"</read_first>
  <behavior>
    - The positive stage allowlist includes every Phase 4 schema, the hook executable/configuration, and the first-party runtime needed by the native adapter; inventory/digest checks reject omissions, extras, symlinks, and private runtime material.
    - Three independent fresh homes install the same exact staged bytes, run the host canary, and emit a tuple binding installed identity, Codex version, adapter/profile/permission/hook digests, host identity/mapping result, and raw-evidence digests.
    - Authentication absence or unsupported native mapping produces a machine-readable `BLOCKED` qualification and exit status 77 for required-host execution, never a skipped or passing formal result.
  </behavior>
  <action>Extend `scripts/stage-plugin`'s explicit copy list, generated-schema share list, static allowlist, inventory, private-path rejection, and build identity so the exact staged plugin contains all ten Phase 4 schemas and `hooks/arw_hook.py` with executable-safe mode in addition to `hooks/hooks.json`. Extend `scripts/smoke-staged-plugin` with a dedicated Phase 4 host-canary mode that creates three separate fresh `CODEX_HOME` roots, stages/installs byte-identical plugin copies, invokes the adapter's immutable-assignment canary in each, and retains redacted command/stdout/stderr/observation/result-path evidence. Bind each canary tuple to exact installed build identity, Codex version, adapter implementation digest, selected profile/permission/hook digests, host identity/mapping classification, and evidence digests. Strip authentication material and absolute private paths from retained evidence. When authentication is absent, profile/mapping is unavailable, process limits/hook parity cannot be proven, or a canary disagrees, write a `BLOCKED` result and return 77 for required-host mode; do not use pytest skip or a successful technical verdict. Add host-marked tests for all three homes plus non-host tests that assert the blocked/degraded classification contract.</action>
  <acceptance_criteria>Exact-stage tests prove every required Phase 4 runtime file is present and private state is absent; three fresh authenticated canaries are required for a formal qualification, while every nonqualifying condition is retained as explicit BLOCKED/degraded evidence without a false pass.</acceptance_criteria>
  <verify><automated>ARW_REQUIRE_CODEX_HOST=1 UV_OFFLINE=1 uv run --frozen pytest -q tests/staged/test_phase4_host_qualification.py -m codex_host</automated></verify>
  <done>The project can prove a real native host from exact installed bytes or accurately record that formal native execution remains unavailable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| parent adapter → Codex host process | The external process can misreport identity, ignore limits, emit transcript-only output, or survive cancellation. |
| source stage → fresh installed plugin | Repository files and an operator home must not be mistaken for exact installed bytes. |
| authentication material → retained evidence | Fresh-host qualification must not package or record credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-37 | Spoofing | host identity/mapping | mitigate | Accept formal mode only with stable observed host identity and assignment mapping in all three fresh canaries; otherwise BLOCKED. |
| T-04-38 | Tampering | proposal/transcript boundary | mitigate | Parent reads only the direct result file; adapter transcript is observation evidence and cannot become canonical proposal bytes. |
| T-04-39 | Repudiation | host qualification | mitigate | Bind exact installed identity, config digests, command observations, host result, and raw evidence digests in a schema-validated tuple. |
| T-04-40 | Information disclosure | auth/private paths | mitigate | Fresh isolated homes, stage allowlist/private scans, auth deletion, and evidence redaction before retention. |
| T-04-41 | Denial of service | host process | mitigate | Frozen depth/thread/concurrency limits, process group cancellation, grace, qualified force termination, and canary assertions. |
| T-04-42 | Elevation of privilege | host adapter/profile | mitigate | Give host only the immutable assignment/scratch capability; no journal/control root, no canonical writer, and no automatic formal fallback. |
| T-04-SC | Tampering | package-manager installs | mitigate | No package-manager install is permitted; stage the existing locked wheelhouse and use offline verification. |
</threat_model>

<verification>
Run the non-host test command on every machine. In an authenticated qualification environment, run the required-host command and inspect all three fresh-home evidence tuples. Then run `UV_OFFLINE=1 ./scripts/stage-plugin --clean --stage-root build/stage/phase-04-host --evidence-root build/evidence/phase-04/stage` and verify its inventory contains the Phase 4 schemas/hook executable but no private runtime or credentials.
</verification>

<success_criteria>
Native host use is safe and honest: only a three-home exact-stage proof can support `native_formal`; otherwise the deterministic parent workflow records `degraded_inline` or `BLOCKED`, retains evidence, and never asserts nonexistent independence.
</success_criteria>

<output>Create `.planning/phases/04-subagent-orchestration-hooks-and-human-gates/04-07-SUMMARY.md` when done.</output>
