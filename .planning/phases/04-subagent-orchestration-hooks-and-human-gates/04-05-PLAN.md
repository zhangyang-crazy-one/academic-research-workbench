---
phase: 04-subagent-orchestration-hooks-and-human-gates
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - tests/evals/test_phase4_corpus.py
  - tests/integration/test_orchestration_replay.py
  - tests/integration/test_orchestration_panels.py
  - tests/integration/test_orchestration_hook_parity.py
  - tests/integration/test_human_gates.py
  - tests/staged/test_phase4_host_qualification.py
  - .planning/phases/04-subagent-orchestration-hooks-and-human-gates/04-VALIDATION.md
autonomous: true
requirements: [PKG-05, AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, SCI-02, SCI-03]
must_haves:
  truths:
    - "All 48 corpus cases run against the completed deterministic runtime with expected events, terminal states, redaction, and replay digests."
    - "The full no-host suite is green; exact-host canaries are separately marked PASS only when run successfully in three fresh homes."
    - "The validation contract maps every implementation task to real automated evidence and does not confuse a skipped host test with success."
  artifacts:
    - path: tests/staged/test_phase4_host_qualification.py
      provides: Exact-version staged admission evidence and explicit unqualified status when credentials are absent.
    - path: 04-VALIDATION.md
      provides: Final Nyquist task map and sign-off evidence.
---

<objective>
Close Phase 4 with end-to-end deterministic evidence, cold replay, corpus coverage, and a separately honest host qualification path. This plan validates the implemented system rather than adding a new fallback behavior.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Run corpus-driven end-to-end, replay, panel, hook, and gate verification</name>
  <files>tests/evals/test_phase4_corpus.py, tests/integration/test_orchestration_replay.py, tests/integration/test_orchestration_panels.py, tests/integration/test_orchestration_hook_parity.py, tests/integration/test_human_gates.py</files>
  <read_first>04-01 through 04-04 summaries; 04-AI-SPEC.md Evaluation Strategy and Guardrails; 04-RESEARCH.md Validation Architecture; corpus manifest and all Phase 4 tests</read_first>
  <action>Replace Wave 0 scaffolding with executable corpus parametrization and expected-oracle assertions. For every case validate event sequence, accepted/rejected artifact references, blocker/terminal state, redaction, authoritative-tree behavior, and authority-normalized cold replay after deleting transcripts, host cache/context, status output, and projections. Exercise every formal-panel order, injected stale/cancel/retry/orphan outcome, hook mode, scoped human decision, restricted capability/root denial, and experiment-designer routing while ensuring experiment execution remains disabled.</action>
  <acceptance_criteria>All 48 cases run; all development cases pass in ordinary CI; sealed labels never reach worker-visible roots/prompts; all formal completion orders have equal authoritative digest; accepted stale/unauthorized/invalid count is zero; every required critical dissent or unresolved required gate is BLOCKED; no test relies on live Codex output.</acceptance_criteria>
  <verify><automated>uv run pytest -q -m "not codex_host" tests/unit tests/schema tests/integration tests/staged tests/evals</automated></verify>
  <done>The phase has a complete deterministic release gate that exercises its declared reference corpus and cold-replay invariants.</done>
</task>
<task type="auto">
  <name>Task 2: Qualify exact Codex host tuples honestly and finalize the validation contract</name>
  <files>tests/staged/test_phase4_host_qualification.py, .planning/phases/04-subagent-orchestration-hooks-and-human-gates/04-VALIDATION.md</files>
  <read_first>04-AI-SPEC.md Exact-Codex-version staged qualification; scripts/stage-plugin; tests/staged/test_compatibility_probes.py; tests/staged/test_mcp_launcher.py; 04-VALIDATION.md</read_first>
  <action>Implement staged tests that require explicit `ARW_EXPECT_CODEX_VERSION`, isolated fresh HOME/CODEX_HOME, stage digest, adapter/profile/permission/hook digest, and credential availability. Cover fresh child dispatch, host identity binding, result isolation, max-depth/thread limits, cooperative/qualified force cancel mapping, hook trust modes, one continuation, no peer/sealed-label access, and no private source-path leakage. When prerequisites are absent, report `unqualified` and skip only the host execution assertion; never emit PASS. Update validation task rows with actual task IDs, test commands, wave status, corpus and host admission conditions. Mark Nyquist compliant only after all task rows and deterministic sign-off are demonstrably green.</action>
  <acceptance_criteria>Host tests have an explicit guard and distinguish unqualified from pass; a successful admission requires 12 host canaries in three fresh homes for every exact tuple; validation contains no placeholders, every task has an automated command, and all 10 requirements map to deterministic or human/staged evidence without falsely claiming host qualification.</acceptance_criteria>
  <verify><automated>uv run pytest -q -m "not codex_host" tests/unit tests/schema tests/integration tests/staged tests/evals &amp;&amp; ./scripts/stage-plugin --clean --stage-root build/stage/academic-research-workbench</automated></verify>
  <done>Phase verification is complete for deterministic behavior and candid about any host environment not yet qualified.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation | Evidence |
|---|---|---|
| Narrow unit tests miss cross-boundary failure | 48-case end-to-end corpus plus deletion-based cold replay | full deterministic suite |
| Skipped authenticated test is reported as success | explicit unqualified host tuple status | staged qualification test |
| Validation documentation drifts from actual tasks | task-ID/command map updated after implementation | validation review |
</threat_model>

<verification>Run the full deterministic suite, stage the plugin, verify corpus counts/digests and `git diff --check`. Run authenticated host canaries only with explicit credentials; otherwise retain BLOCKED/unqualified host provenance.</verification>
<success_criteria>Every Phase 4 requirement and roadmap success criterion has real deterministic evidence, and host-specific claims are only made when exact staged canaries pass.</success_criteria>
<output>Create `04-05-SUMMARY.md` after execution.</output>
