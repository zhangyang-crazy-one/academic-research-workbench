---
phase: 04-subagent-orchestration-hooks-and-human-gates
plan: "01"
subsystem: orchestration-contracts
tags: [pydantic, json-schema, canonical-json, immutable-records]

requires:
  - phase: 01-contract-license-and-executable-baseline
    provides: StrictModel, canonical JSON serialization, and schema-registry conventions
provides:
  - Frozen, extra-forbidden Phase 4 orchestration records with canonical serialization
  - Checked-in JSON Schemas regenerated through the sole schema registry
  - Formal/degraded/blocked provenance vocabulary that preserves parent-only authority
affects:
  - 04-02 canonical events and workflow authority
  - 04-03 scheduler, panel, and hook contracts
  - 04-04 parent lifecycle and replay
  - 04-10 sealed evaluation corpus
  - 04-11 deterministic behavior-test matrix

tech-stack:
  added: []
  patterns:
    - Strict frozen Pydantic models with tuple-backed JSON-array normalization
    - Schema drift is rejected by registering every Phase 4 model in schema_registry.py

key-files:
  created:
    - src/arw/orchestration_models.py
    - schemas/v1/role-catalog.schema.json
    - schemas/v1/assignment.schema.json
    - schemas/v1/worker-proposal.schema.json
    - schemas/v1/review-finding-matrix.schema.json
    - schemas/v1/gate-decision.schema.json
    - schemas/v1/hook-observation.schema.json
    - schemas/v1/host-qualification.schema.json
    - schemas/v1/phase4-evaluation-verdict.schema.json
  modified:
    - src/arw/schema_registry.py
    - tests/unit/test_orchestration_models.py
    - tests/schema/test_phase4_contracts.py

key-decisions:
  - "Normalize wire-format JSON arrays to tuples so strict in-memory records remain immutable."
  - "Permit formal execution provenance only with retained host identity, assignment mapping, and isolation evidence; inline work is never independent."
  - "Use the existing schema registry as the sole regeneration path for all Phase 4 contracts."

patterns-established:
  - "Parent authority stays outside contract records: workers submit immutable proposals and cannot write canonical state."
  - "Versioned schemas and canonical bytes are checked together to make authority-shape drift visible."

requirements-completed: [PKG-05, AGT-01, AGT-02, AGT-03, AGT-04, AGT-05, AGT-06, AGT-07, SCI-02, SCI-03]

duration: 17m 51s
completed: 2026-07-14
---

# Phase 04 Plan 01: Strict Orchestration Contracts Summary

**Frozen Phase 4 orchestration records and regenerated JSON Schemas establish truthful execution provenance without adding a canonical worker writer.**

## Performance

- **Duration:** 17m 51s
- **Started:** 2026-07-14T19:55:34Z
- **Completed:** 2026-07-14T20:16:45Z
- **Tasks:** 1/1 complete
- **Files modified:** 12

## Accomplishments

- Added strict, immutable contracts for role catalogs, assignments, proposals, reviews, gates, human decisions, hooks, host qualification, and evaluation verdicts.
- Registered and checked in eight deterministic Phase 4 JSON Schemas through the repository's existing schema registry.
- Preserved the sole-writer boundary: the contracts define worker proposals only and add no canonical event writer, scheduler, mutating hook, or experiment executor.

## Task Commits

Each TDD stage for the completed task was committed atomically:

1. **Task 1 RED: strict orchestration contract tests** — `0b50adc` (`test`)
2. **Task 1 GREEN: immutable orchestration contracts and schemas** — `77502f1` (`feat`)

## Files Created/Modified

- `src/arw/orchestration_models.py` — Versioned frozen contract vocabulary, canonical helpers, and validation rules.
- `src/arw/schema_registry.py` — Registers the Phase 4 schema-bearing contracts and verifies generated bytes.
- `schemas/v1/*.schema.json` — Checked-in strict JSON Schemas for the eight declared Phase 4 records.
- `tests/unit/test_orchestration_models.py` — Contract invariants, including provenance and immutable serialization checks.
- `tests/schema/test_phase4_contracts.py` — Schema regeneration and representative invalid-instance coverage.

## Decisions Made

- JSON-compatible sequence inputs are normalized into tuples before validation completes, preventing mutable containers from entering frozen records.
- `native_profile` and `assignment_injected_subagent` require retained identity, mapping, and isolation evidence for formal claims; `degraded_inline` cannot claim independence, and unavailable required roles are `blocked`.
- The contracts reject `code_runner` and `study_manager`; `experiment_designer` remains proposal-only.

## TDD Gate Compliance

- RED: `0b50adc` added tests that failed before `arw.orchestration_models` existed.
- GREEN: `77502f1` implemented the minimum strict contracts and schemas, after which the focused suite passed.

## Verification

- `UV_OFFLINE=1 uv run --frozen pytest -q tests/unit/test_orchestration_models.py tests/schema/test_phase4_contracts.py` — 6 passed.
- `UV_OFFLINE=1 uv run --frozen pytest -q tests/schema/test_schema_drift.py` — 3 passed.
- `git diff --check` — passed.

## Deviations from Plan

### Scope Reconciliation

**1. Removed corpus work that the planner moved to Plan 04-10.**

- **Found during:** Execution reconciliation after Task 1.
- **Issue:** The prior execution context still described former P04-01-T02 corpus work, but `514c075` split that work into `04-10-PLAN.md`; retaining it would violate the requested Plan 04-01-only scope.
- **Fix:** Reverted the five corpus artifacts and test so they remain exclusively owned by Plan 04-10, and corrected the Roadmap's stale 9-plan/combined-04-01 entry to its current 11-plan split.
- **Files affected:** `tests/evals/test_phase4_corpus.py`, `tests/evals/phase4/corpus/v1/*`, and `tests/fixtures/orchestration/v1/phase4-fixtures.json`.
- **Verification:** The actual 04-01 contract and schema-drift commands pass after the reconciliation.
- **Committed in:** `61af8fc`.

**2. Used the eight declared schema artifacts rather than inventing two unnamed schemas.**

- **Found during:** Task 1 schema generation.
- **Issue:** The task action says "ten listed schemas," while its `<files>` contract names eight schema files.
- **Fix:** Generated exactly the eight named records and registered them as the complete `PHASE4_SCHEMA_NAMES` set; adding unspecified schemas would expand the public contract without a plan owner.
- **Verification:** The focused schema tests and schema-drift suite both pass.

**Total deviations:** 2 planning-scope/text reconciliations.

**Impact on plan:** No product scope was added. Plan 04-01 now matches its current single-task contract-only definition.

## Known Stubs

None. The future corpus and behavior-test work was deliberately left to Plans 04-10 and 04-11; no placeholder implementation remains in this plan's files.

## Issues Encountered

- The plan was narrowed in commit `514c075` after an earlier execution context had been prepared. Git history confirmed the corpus moved to Plan 04-10 and the behavior matrix to Plan 04-11; the out-of-scope corpus commits were safely reverted.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plans 04-02 through 04-04 can import stable contracts and regenerate the exact checked-in schemas.
- Plan 04-10 still owns the sealed corpus, and Plan 04-11 still owns the expected-failure behavior matrix; neither is claimed complete here.

---
*Phase: 04-subagent-orchestration-hooks-and-human-gates*
*Completed: 2026-07-14*

## Self-Check: PASSED

All listed contract/schema artifacts exist, the task and scope-reconciliation commits are reachable, and `git diff --check` passed after the final metadata reconciliation.
