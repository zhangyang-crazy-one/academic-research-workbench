---
phase: 04-subagent-orchestration-hooks-and-human-gates
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/arw/models.py
  - src/arw/reducer.py
  - src/arw/runtime.py
  - src/arw/scheduler.py
  - src/arw/orchestration.py
  - src/arw/status.py
  - src/arw/cli.py
  - tests/unit/test_scheduler.py
  - tests/integration/test_orchestration_lifecycle.py
  - tests/integration/test_orchestration_replay.py
autonomous: true
requirements: [AGT-01, AGT-02, AGT-03, AGT-05, SCI-02]
must_haves:
  truths:
    - "Only RuntimeCommandService accepts, rejects, retries, cancels, supersedes, or records proposals."
    - "Frozen layer/task ordinal, not completion timing, determines canonical acceptance; every 2-5 item permutation has the same authoritative digest."
    - "Retry is bounded to two attempts; stale, cancelled, superseded, and orphaned results never become accepted evidence."
  artifacts:
    - path: src/arw/scheduler.py
      provides: Frozen DAG, cursor, retry classification, deterministic buffering and acceptance order.
    - path: src/arw/orchestration.py
      provides: Parent-only prepare, seal, validate, accept, reject, retry, cancellation and restart commands.
  key_links:
    - from: src/arw/orchestration.py
      to: src/arw/runtime.py
      via: sole-writer RuntimeCommandService transactions
      pattern: RuntimeCommandService
---

<objective>
Extend the durable Phase 2 runtime into an executable parent/worker lifecycle while preserving sole-writer authority, replay compatibility, deterministic order, bounded recovery, and explicit BLOCKED gates.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add replay-safe orchestration events, state, and status projection</name>
  <files>src/arw/models.py, src/arw/reducer.py, src/arw/runtime.py, src/arw/status.py, src/arw/cli.py, tests/integration/test_orchestration_lifecycle.py</files>
  <read_first>04-01-SUMMARY.md; src/arw/models.py attempt and human-decision payloads; src/arw/reducer.py; src/arw/runtime.py; src/arw/status.py; tests/integration/test_runtime_attempts.py; tests/integration/test_runtime_transitions.py; tests/integration/test_recovery.py</read_first>
  <action>Add versioned canonical event payloads and reducer state for assignment preparation, attempt dispatch, proposal sealing/validation, acceptance/rejection, cancellation request/force interruption, supersession, retry exhaustion, and orphan interruption. Expose assignment ID, attempt ID, frozen acceptance key, execution provenance, proposal disposition, active cursor, and blocker/pending decision state in status. Implement parent-only RuntimeCommandService methods that lock, validate expected revision/authority/assignment bindings, append exactly one event, and leave the authoritative tree byte-identical on rejection. Route new parent commands through CLI without granting worker/hook actor categories mutation authority.</action>
  <acceptance_criteria>Worker, reviewer, hook, adapter, MCP, and direct-file attempts cannot append accepted state; a valid parent command replays identically in a fresh process; every invalid/stale/duplicate command leaves journal/manifests/pointers/status-input tree unchanged; status distinguishes active, cancelled, interrupted, stale, blocked, and accepted outcomes.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/integration/test_orchestration_lifecycle.py tests/integration/test_runtime_attempts.py tests/integration/test_runtime_transitions.py tests/unit/test_reducer.py tests/unit/test_status.py</automated></verify>
  <done>Lifecycle state is journaled and replayable without granting a worker canonical authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement frozen scheduling, bounded retry/cancel policy, and cold recovery</name>
  <files>src/arw/scheduler.py, src/arw/orchestration.py, tests/unit/test_scheduler.py, tests/integration/test_orchestration_lifecycle.py, tests/integration/test_orchestration_replay.py</files>
  <read_first>04-CONTEXT.md D-05 through D-08; 04-AI-SPEC.md Core Pattern and State Management; src/arw/recovery.py; tests/integration/test_recovery_crash.py; tests/integration/test_segmented_journal.py; Task 1 changes</read_first>
  <action>Implement a frozen acyclic DAG with unique `(topological_layer, task_ordinal)` keys, bounded concurrency metadata, buffered completion records, and cursor-only acceptance. Classify failures so timeout/process/repairable envelope errors receive at most one new attempt; permission/stale/supersession/cancellation/policy/identity/scientific disagreement do not retry. Record cooperative cancellation before adapter action, then qualified force/interruption after grace; restart active attempts as interrupted and requeue only within budget. Preserve late bytes as immutable `rejected_stale` attempt evidence and open a scoped BLOCKED decision when required work has no legal replacement.</action>
  <acceptance_criteria>All 2-5 task completion permutations, including 24 four-reviewer orders, produce equal canonical/replay digests; no third attempt is possible; later buffered result cannot pass an unresolved earlier cursor; crash recovery does not duplicate accepted work; deleting host/transcript/cache/status projections still permits cold replay from canonical files.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/unit/test_scheduler.py tests/integration/test_orchestration_lifecycle.py tests/integration/test_orchestration_replay.py tests/integration/test_recovery.py tests/integration/test_recovery_crash.py</automated></verify>
  <done>Parent scheduling is deterministic across timing, failure, cancellation, and restart conditions.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation | Evidence |
|---|---|---|
| Completion timing changes history | Frozen key cursor and exhaustive permutation oracle | scheduler tests |
| Retry or cancellation loop consumes unbounded work | two-attempt budget, deadline/grace state, terminal BLOCKED decision | lifecycle/recovery tests |
| Late proposal is accepted after a state change | immutable stale evidence plus parent-only acceptance | lifecycle/replay tests |
</threat_model>

<verification>Run all Plan 02 tests plus the existing runtime/recovery suites. Inspect a fresh-process replay after removing host/transcript/projection inputs.</verification>
<success_criteria>AGT-01/02/03/05 and SCI-02 have deterministic parent-controlled lifecycle evidence and no stale or orphan bypass.</success_criteria>
<output>Create `04-02-SUMMARY.md` after execution.</output>

