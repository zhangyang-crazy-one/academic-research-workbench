---
phase: 04-subagent-orchestration-hooks-and-human-gates
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - src/arw/execution.py
  - src/arw/hook_contracts.py
  - src/arw/orchestration.py
  - hooks/hooks.json
  - hooks/arw_hook.py
  - .codex-plugin/plugin.json
  - tests/unit/test_hook_contracts.py
  - tests/integration/test_orchestration_hook_parity.py
  - tests/integration/test_orchestration_lifecycle.py
  - tests/staged/test_phase4_host_qualification.py
autonomous: true
requirements: [PKG-05, AGT-05, AGT-06, AGT-07]
must_haves:
  truths:
    - "The Codex adapter is a non-authoritative transport seam; only the parent accepts proposal bytes."
    - "Hook modes trusted_enabled, disabled, untrusted, timeout, and failed preserve identical runtime/MCP/gate/provenance authority."
    - "Hooks request at most one continuation and never append canonical events or authorize an operation."
  artifacts:
    - path: src/arw/execution.py
      provides: Qualified async adapter protocol, fake adapter, and explicit unknown/unqualified host outcomes.
    - path: hooks/arw_hook.py
      provides: Strict bounded hook observation/continuation process with no canonical writes.
---

<objective>
Add a thin, testable Codex execution adapter and non-authoritative hooks without creating a second state machine or allowing host/hook behavior to weaken canonical runtime, MCP confinement, or human gates.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the qualified execution-adapter seam and deterministic fake adapter</name>
  <files>src/arw/execution.py, src/arw/orchestration.py, tests/integration/test_orchestration_lifecycle.py, tests/staged/test_phase4_host_qualification.py</files>
  <read_first>04-AI-SPEC.md Entry Point Pattern, Core Pattern, Tool Use, and State Management; src/arw/orchestration.py; src/arw/runtime.py; src/arw/files_mcp.py; tests/integration/test_phase2_durable_runtime.py; tests/staged/test_compatibility_probes.py</read_first>
  <action>Define an async `ExecutionAdapter` protocol with dispatch, cooperative cancel, qualified force termination, and observation collection; define `CodexNativeExecutionAdapter` as an adapter shell that returns explicit unqualified/BLOCKED outcomes unless exact host evidence proves each mapping. Add a deterministic fake adapter used by all ordinary tests to deliver isolated attempt proposal paths and controlled completion/cancel/failure observations. Keep host session/transcript fields observational; bind any observed host ID to the parent-created worker identity and attempt only after validation. Enforce direct-child depth one, finite concurrency, per-attempt scratch/result isolation, and fresh child/attempt on retry.</action>
  <acceptance_criteria>Deterministic tests do not require Codex, network, transcript parsing, or credentials; host result text is never accepted directly; missing/reused host identity blocks formal independence; unqualified cancel/force behavior becomes interrupted/BLOCKED rather than silently succeeding; fake adapter supports completion-order and timeout injection.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/integration/test_orchestration_lifecycle.py tests/integration/test_orchestration_replay.py tests/unit/test_scheduler.py</automated></verify>
  <done>Host integration is a narrow, replaceable transport boundary and cannot become canonical authority.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement bounded hook observations and five-mode parity tests</name>
  <files>src/arw/hook_contracts.py, hooks/hooks.json, hooks/arw_hook.py, .codex-plugin/plugin.json, tests/unit/test_hook_contracts.py, tests/integration/test_orchestration_hook_parity.py, tests/staged/test_phase4_host_qualification.py</files>
  <read_first>04-CONTEXT.md D-13 and D-14; 04-AI-SPEC.md Hook sections; hooks/hooks.json; .codex-plugin/plugin.json; src/arw/files_mcp.py; tests/integration/test_files_security.py; tests/staged/test_skill_route.py</read_first>
  <action>Define strict stdin/stdout hook envelopes and idempotent observation records for `SubagentStart`, `SubagentStop`, and `Stop`. Implement hydration/preflight/policy-warning behavior with finite command timeout and no canonical runtime invocation. Persist continuation count by attempt/deliverable and allow at most one directed continuation; repeated/concurrent hook signals are observational and cannot create a second continuation. Configure plugin hooks with explicit trust-aware status handling. Write paired tests across trusted_enabled, disabled, untrusted, timeout, and failed modes that compare authority-normalized replay digests and prove runtime, MCP roots/capabilities, gates, and provenance make identical decisions.</action>
  <acceptance_criteria>Hook payloads reject malformed/oversized/redaction-breaking input; hooks cannot accept proposals, mutate journal, release blockers, or expand roots/capabilities; disabled/bypassed hooks leave the same accepted state and denials as trusted mode; only allowlisted observation receipts differ; continuation count never exceeds one.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/unit/test_hook_contracts.py tests/integration/test_orchestration_hook_parity.py tests/integration/test_files_security.py</automated></verify>
  <done>AGT-06 and AGT-07 are proven by paired authority tests, not trust in hook configuration.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation | Evidence |
|---|---|---|
| Host transcript/result becomes state authority | adapter exposes only isolated proposal-path observation | fake-adapter and staged tests |
| Hook trust disables security | five-mode parity with runtime/MCP/gate enforcement outside hooks | parity integration tests |
| Repeated stop hook loops work indefinitely | persisted per-target continuation cap | hook contract tests |
</threat_model>

<verification>Run deterministic adapter/hook suites without Codex credentials. Stage the plugin and collect exact-host tests only when credentials and tuple configuration are explicitly provided; skipped host tests remain unqualified.</verification>
<success_criteria>AGT-05/06/07 and PKG-05 have bounded adapter/hook behavior and formal proof that host convenience cannot bypass authority.</success_criteria>
<output>Create `04-04-SUMMARY.md` after execution.</output>

