---
phase: 05-rebuildable-research-graph-and-evidence-queries
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/arw/graph_projection.py
  - src/arw/graph_store.py
  - src/arw/files.py
  - src/arw/runtime.py
  - src/arw/evidence.py
  - tests/integration/test_graph_rebuild.py
  - tests/integration/test_graph_authority.py
  - tests/property/test_graph_replay.py
  - tests/fixtures/research-graph/mutations/
autonomous: true
requirements: [GRAPH-03, GRAPH-04, GRAPH-06, VER-05]
must_haves:
  truths:
    - "Clean full, delete/rebuild and incremental projections produce equivalent normalized query results under the declared oracle."
    - "Modify, rename, delete, correction, migration and supersession preserve explicit lineage and never retain stale evidence."
    - "Projection crash, corruption, staleness or unavailability leaves canonical events, state, gate verdicts and next legal transition byte-identical."
  artifacts:
    - path: src/arw/graph_store.py
      provides: "Parent-only generation lifecycle, atomic pointer, receipt and integrity checks"
    - path: tests/integration/test_graph_rebuild.py
      provides: "VER-05 mutation/equivalence matrix and normalized diff evidence"
    - path: tests/integration/test_graph_authority.py
      provides: "Before/after canonical authority and corruption/unavailable probes"
    - path: tests/property/test_graph_replay.py
      provides: "Generated command/mutation replay and duplicate/crash coverage"
  key_links:
    - from: src/arw/graph_projection.py
      to: src/arw/graph_store.py
      via: "canonical input bundle -> closed generation -> receipt -> atomic selection"
  - from: tests/integration/test_graph_authority.py
      to: src/arw/runtime.py
      via: "same canonical replay and legal transition with projection present/absent/corrupt"
---

<objective>
Prove rebuildability and authority isolation with a retained mutation corpus,
oracle comparisons, crash/tamper probes and exact canonical before/after
snapshots.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>05-03-T01 — Implement full/incremental/delete generation equivalence</name>
  <files>src/arw/graph_projection.py, src/arw/graph_store.py, src/arw/files.py, tests/integration/test_graph_rebuild.py, tests/property/test_graph_replay.py, tests/fixtures/research-graph/mutations/</files>
  <read_first>05-RESEARCH.md Rebuild and equivalence oracle; src/arw/files.py; src/arw/graph_projection.py; src/arw/graph_store.py; Phase 2 recovery tests; Phase 3 generation tests</read_first>
  <action>Implement explicit parent commands for clean full build, incremental build from the prior canonical watermark, delete/rebuild, migration and repair. Use immutable sibling generations and atomic selection; each runtime append/result is checked before the next step. Build the mutation matrix for modify, unambiguous rename, delete/tombstone, correction, schema migration and supersession, including combinations and repeated/idempotent runs. Query each generation through the fixed operations, normalize with oracle v1, compare exact semantic rows/digests/watermarks, and retain receipts, manifests, diff output and raw transcripts.</action>
  <acceptance_criteria>Every required mutation has a passing clean-vs-incremental and delete/rebuild comparison; ambiguous lineage fails closed; stale/deleted entities cannot yield old body/evidence results; repeated build is idempotent and never creates a second canonical event for the same command.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 .venv/bin/python -m pytest -q tests/integration/test_graph_rebuild.py tests/property/test_graph_replay.py</automated></verify>
  <done>GRAPH-03, GRAPH-04 and VER-05 have executable equivalence evidence across the complete mutation corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>05-03-T02 — Prove projection failures cannot change canonical authority</name>
  <files>src/arw/runtime.py, src/arw/evidence.py, tests/integration/test_graph_authority.py</files>
  <read_first>src/arw/runtime.py; src/arw/reducer.py; src/arw/evidence.py; src/arw/graph_store.py; Phase 2 crash/recovery tests; Phase 4 gate/review tests</read_first>
  <action>Inject process death after input-bundle write, native build, digest verification and before/after pointer replacement; corrupt generation manifest/database/pointer; remove the selected generation; make the native builder unavailable; and present stale watermark/source digests. Assert the parent retains the last valid pointer, emits a bounded receipt, and can replay status/gate/transition from canonical events/manifests. Compare canonical event bytes, reducer state, gate verdict/hash and next legal transition before and after each fault. Query failures must be typed and no repair may occur through MCP.</action>
  <acceptance_criteria>All fault points either retain the previous selected generation or publish one fully validated replacement; no canonical bytes, accepted revision, verdict, blocker, or next legal transition differs; recovery is idempotent and raw fault evidence is retained.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 .venv/bin/python -m pytest -q tests/integration/test_graph_authority.py tests/integration/test_graph_rebuild.py -k 'crash or corrupt or unavailable or stale or authority'</automated></verify>
  <done>GRAPH-06 is demonstrated independently of graph availability and no projection failure can become hidden workflow authority.</done>
</task>

</tasks>

<threat_model>
| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T05-01 | mitigate | Generation/DB/pointer hashes and closed receipts reject tamper. |
| T05-02 | mitigate | Watermark/source digest checks make stale evidence typed and non-authoritative. |
| T05-03 | mitigate | Canonical replay and legal-transition comparisons run without selected projections. |
| T05-05 | mitigate | Complete mutation matrix plus property replay catches omitted lineage. |
| T05-06 | verify | Oracle version/diff retains semantic distinctions and exact evidence hashes. |
</threat_model>

<verification>
Run all mutation and authority modules, inspect retained normalized diff receipts,
run prior recovery suites, and verify that failure injection cannot write through
the graph MCP. No skipped mutation is acceptable.
</verification>

<success_criteria>
Clean/full/incremental/delete/migration/supersession equivalence and canonical
authority isolation are proven from retained raw evidence.
</success_criteria>

<output>Create `05-03-SUMMARY.md` after execution.</output>
