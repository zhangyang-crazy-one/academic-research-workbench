---
phase: 05-rebuildable-research-graph-and-evidence-queries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arw/graph_models.py
  - src/arw/graph_projection.py
  - src/arw/graph_oracle.py
  - src/arw/schema_registry.py
  - schemas/v1/graph-node.schema.json
  - schemas/v1/graph-edge.schema.json
  - schemas/v1/graph-projection-manifest.schema.json
  - schemas/v1/graph-projection-receipt.schema.json
  - schemas/v1/graph-query-request.schema.json
  - schemas/v1/graph-query-result.schema.json
  - schemas/v1/graph-oracle.schema.json
  - tests/schema/test_graph_contracts.py
  - tests/unit/test_graph_projection.py
  - tests/fixtures/research-graph/
autonomous: true
requirements: [GRAPH-01, GRAPH-02]
must_haves:
  truths:
    - "Validated canonical manifests deterministically produce all ten required research node types and explicit evidence/supersession edges."
    - "Every node and edge binds a stable identity, source digest, schema version, supersession state, and accepted ledger watermark; caller-supplied identity or watermark is rejected."
    - "The normalization oracle compares semantic graph/query records while retaining exact evidence digests and provenance fields."
  artifacts:
    - path: src/arw/graph_models.py
      provides: "Strict node, edge, generation, receipt, query, result and oracle models"
    - path: src/arw/graph_projection.py
      provides: "Parent-owned canonical-manifest projector and deterministic input bundle"
    - path: src/arw/graph_oracle.py
      provides: "Versioned normalized comparison and diff receipt"
    - path: schemas/v1/graph-projection-receipt.schema.json
      provides: "Cross-language receipt binding source watermark and generation digest"
    - path: tests/fixtures/research-graph/
      provides: "Small ten-entity corpus with corrections, dissent and supersession"
  key_links:
    - from: src/arw/graph_projection.py
      to: src/arw/runtime.py
      via: "read-only replay of accepted events/manifests; no graph write advances runtime state"
  - from: src/arw/graph_oracle.py
    to: schemas/v1/graph-oracle.schema.json
    via: "oracle version and canonical bytes are evidence-bound"
---

<objective>
Define the Phase 5 authority boundary and canonical projection contracts before
building a database. Make the Python parent produce deterministic, validated
graph input and a reusable equivalence oracle from canonical run/manifests.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>05-01-T01 — Add strict graph contracts and registry-derived schemas</name>
  <files>src/arw/graph_models.py, src/arw/schema_registry.py, schemas/v1/graph-*.schema.json, tests/schema/test_graph_contracts.py</files>
  <read_first>AGENTS.md; .planning/REQUIREMENTS.md; 05-RESEARCH.md; src/arw/schema_registry.py; src/arw/file_models.py; src/arw/integration_lock.py; schemas/v1; tests/schema/test_schema_registry.py</read_first>
  <action>Implement strict Pydantic models for the ten node types, explicit edge types, projection input/generation manifest, receipt, allowlisted query request/result, and oracle comparison. Derive `PHASE5_SCHEMA_NAMES` from the model registry, extend schema validation/regeneration without a static count, require canonical IDs/digests/watermarks and reject extra fields, caller-supplied event hashes, noncanonical ordering, invalid supersession, and unknown query operations. Generate checked-in Draft 2020-12 schemas and independent jsonschema fixtures.</action>
  <acceptance_criteria>All Phase 5 schema documents validate independently; unknown fields, wrong digest lengths, stale watermark bindings, arbitrary Cypher/SQL, invalid labels, and noncanonical arrays are rejected. Existing Phase 1–4 schemas and stage count tests remain green.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 .venv/bin/python -m pytest -q tests/schema/test_graph_contracts.py tests/schema/test_schema_registry.py tests/unit/test_orchestration_models.py</automated></verify>
  <done>The registry and generated schema identity describe the complete Phase 5 contract without hard-coded count drift.</done>
</task>

<task type="auto" tdd="true">
  <name>05-01-T02 — Project canonical manifests and implement the normalization oracle</name>
  <files>src/arw/graph_projection.py, src/arw/graph_oracle.py, tests/unit/test_graph_projection.py, tests/integration/test_graph_projection.py, tests/fixtures/research-graph/</files>
  <read_first>src/arw/runtime.py; src/arw/reducer.py; src/arw/manifests.py; src/arw/review.py; src/arw/integration_lock.py; 05-RESEARCH.md Projection data model and Rebuild oracle; Phase 2/4 fixtures</read_first>
  <action>Implement a parent-only projector that replays accepted ledger events and reads immutable manifests to emit sorted node/edge input records. Derive identities from authoritative namespace plus manifest identity, bind source and payload digests and the accepted watermark, preserve correction/supersession/dissent edges, and emit a content-addressed input bundle. Implement `research-graph-normalization-v1`: strip only backend/temp nondeterminism, preserve all provenance fields, sort stable records, normalize query pages/continuations, and emit a canonical diff receipt. Create a fixture corpus covering Run, Stage, Artifact, Claim, Source, Dataset, Experiment, Figure, Review and Gate with one correction, supersession and minority dissent.</action>
  <acceptance_criteria>Replaying the same canonical inputs twice produces byte-identical input bundles and oracle bytes; changing a manifest or accepted event changes the bound digest/watermark; deleting a projection input is a typed error rather than an inferred deletion; all ten node kinds and required edges appear in the fixture inventory.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 .venv/bin/python -m pytest -q tests/unit/test_graph_projection.py tests/integration/test_graph_projection.py</automated></verify>
  <done>Phase 5 has deterministic, authority-preserving graph input and an executable comparison oracle ready for native publication.</done>
</task>

</tasks>

<threat_model>
| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T05-05 | mitigate | Project from replayed canonical inputs and test correction/rename/delete/supersession lineage in fixtures. |
| T05-06 | mitigate | Oracle preserves exact IDs, digests, states, watermarks and evidence hashes; only backend nondeterminism is removed. |
| T05-08 | mitigate | Strict schemas reject caller identity/watermark/hash substitution and arbitrary query text. |
</threat_model>

<verification>
Run both task commands, `git diff --check`, schema regeneration/drift checks, and
the existing focused Phase 1–4.1 schema/unit subset before starting Wave 2.
</verification>

<success_criteria>
GRAPH-01 and GRAPH-02 are machine-verifiable from canonical bytes, all ten node
types are represented, and the oracle has a declared version and digest.
</success_criteria>

<output>Create `05-01-SUMMARY.md` after execution.</output>
