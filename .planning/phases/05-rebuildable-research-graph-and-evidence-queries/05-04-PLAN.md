---
phase: 05-rebuildable-research-graph-and-evidence-queries
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - scripts/verify-phase-5
  - scripts/stage-plugin
  - scripts/verify-sources
  - src/arw/build_identity.py
  - src/arw/evidence.py
  - src/arw/schema_registry.py
  - SBOM.cdx.json
  - supply-chain/stage-inventory.json
  - schemas/v1/build-identity.schema.json
  - tests/staged/test_graph_install.py
  - tests/staged/test_private_exclusions.py
  - tests/staged/test_supply_chain_inventory.py
  - tests/integration/test_graph_verifier.py
  - docs/runtime/research-graph.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04, GRAPH-05, GRAPH-06, VER-05]
must_haves:
  truths:
    - "A clean exact stage contains every runnable graph artifact and exact source/patch/schema/projection identity, but no graph database, private fixture or runtime index."
    - "A serial verifier retains raw commands, MCP transcripts, mutation/oracle receipts, tamper/authority snapshots and independently checked per-requirement verdicts."
    - "Roadmap, requirements and state are updated only after the technical evidence passes; SUP-04/P04-09 remain separate release blockers."
  artifacts:
    - path: scripts/verify-phase-5
      provides: "Owned-root staged qualification and GRAPH/VER-05 verdict generator"
    - path: docs/runtime/research-graph.md
      provides: "Operator contract for projection lifecycle, bounded traces and recovery"
    - path: build/evidence/phase-05/verdict.json
      provides: "Raw-evidence-bound technical verdict with release blockers separate"
  key_links:
    - from: scripts/verify-phase-5
      to: build/evidence/phase-05
      via: "commands, versions, stage tree, MCP transcripts, oracle and authority snapshots"
    - from: src/arw/build_identity.py
      to: vendor/source-manifest.json
      via: "ordered patch/native binary/contract/projection algorithm digest"
  - from: scripts/stage-plugin
      to: supply-chain/stage-inventory.json
      via: "positive allowlist excludes graph/index/run/evidence/private payloads"
---

<objective>
Close Phase 5 with an exact installed qualification, full evidence-bound
verifier, package/inventory/build identity updates and truthful planning status.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>05-04-T01 — Integrate graph artifacts into stage, SBOM and build identity</name>
  <files>scripts/stage-plugin, src/arw/build_identity.py, SBOM.cdx.json, supply-chain/stage-inventory.json, tests/staged/test_graph_install.py, tests/staged/test_private_exclusions.py</files>
  <read_first>scripts/stage-plugin; src/arw/build_identity.py; vendor/source-manifest.json; supply-chain/stage-inventory.json; tests/staged/test_supply_chain_inventory.py; Phase 4.1 final stage evidence; 05-RESEARCH.md dependency/license posture</read_first>
  <action>Extend the positive stage allowlist with only the graph query launcher, wheel modules, required schemas, native graph binary/profile and operator documentation. Bind Phase 5 schema aggregate, oracle/projection algorithm, ordered patch/tree/native digests and launcher identity into build identity/integration lock/SBOM/notices. Keep graph databases, generations, source fixtures, evidence, temporary bundles and private canaries outside the stage. Update independent inventory/private scans and relocated installed smoke tests.</action>
  <acceptance_criteria>Clean stage validation passes with exact inventory/coverage and no private graph/index/run data; staged version reports the same source/patch/schema/projection/native identities; missing or extra graph artifact, digest drift, symlink, or database causes failure.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 .venv/bin/python -m pytest -q tests/staged/test_graph_install.py tests/staged/test_private_exclusions.py tests/staged/test_supply_chain_inventory.py && UV_OFFLINE=1 ./scripts/verify-sources</automated></verify>
  <done>The installable package contains the complete runnable Phase 5 profile without mutable or private projection state.</done>
</task>

<task type="auto" tdd="true">
  <name>05-04-T02 — Run serial Phase 5 verifier and reconcile planning evidence</name>
  <files>scripts/verify-phase-5, src/arw/evidence.py, tests/integration/test_graph_verifier.py, docs/runtime/research-graph.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>05-VALIDATION.md; ROADMAP.md Phase 5 success criteria; REQUIREMENTS.md GRAPH/VER-05; scripts/verify-phase-1; scripts/verify-phase-2; scripts/verify-phase-3; Phase 4.1 verifier evidence; src/arw/evidence.py</read_first>
  <action>Implement `scripts/verify-phase-5 --clean --evidence-root` using owned temporary/control/source roots and exact staged launchers. Run schema/source/build/stage checks, all ten-node projection fixtures, clean/full/delete/incremental mutation corpus, bounded MCP transcript/error matrix, projection crash/tamper/unavailable authority comparisons, and private-canary/inventory scans. Retain raw command/environment/version/digest records, stage tree, JSON-RPC transcripts, oracle bytes/diffs, receipts, before/after canonical snapshots and per-requirement/per-roadmap verdicts. Reject missing raw evidence, skipped required checks, stale artifacts, substituted binaries, symlinked evidence roots or tampered verdict inputs. Only after technical PASS, mark GRAPH-01..06 and VER-05 complete and update Phase 5 roadmap/state/summary references; do not alter the separate SUP-04/P04-09 release blockers.</action>
  <acceptance_criteria>A clean verifier exits zero only with all seven Phase 5 technical requirements and five roadmap criteria true from exact staged bytes; tampering or deleting evidence makes it nonzero; frozen full pytest and prior technical verifiers remain green; release verdict stays blocked solely for explicit legal/accountable-human evidence.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 ./scripts/verify-phase-5 --clean --evidence-root build/evidence/phase-05 && UV_OFFLINE=1 .venv/bin/python -m pytest -q && UV_OFFLINE=1 ./scripts/verify-sources && UV_OFFLINE=1 ./scripts/verify-phase-1 --clean --evidence-root build/evidence/phase-01-regression && UV_OFFLINE=1 ./scripts/verify-phase-2 --clean --evidence-root build/evidence/phase-02-regression && UV_OFFLINE=1 ./scripts/verify-phase-3 --clean --evidence-root build/evidence/phase-03-regression</automated></verify>
  <done>Phase 5 has a reproducible technical PASS with raw evidence; planning status is reconciled without claiming release authorization.</done>
</task>

</tasks>

<threat_model>
| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T05-01 through T05-06 | verify | Serial verifier binds each raw input, fault, query, oracle and canonical before/after snapshot. |
| T05-07 | mitigate | Positive staged allowlist, SBOM/inventory/build identity and canary scan reject private projection payloads. |
| T04-legal | preserve | Technical verdict and SUP-04/P04-09 release verdict remain separate and explicit. |
</threat_model>

<verification>
The verifier command is the mandatory sign-off gate. Also run `git diff --check`,
inspect all requirement verdict rows and retain the stage/evidence digests before
updating roadmap or requirement checkboxes.
</verification>

<success_criteria>
All GRAPH-01..06 and VER-05 technical gates are machine-verifiable from an exact
stage and retained evidence; no package or projection state becomes authority;
legal/accountable-human release blockers remain truthful.
</success_criteria>

<output>Create `05-04-SUMMARY.md` and update Phase 5 roadmap/state only after execution.</output>
