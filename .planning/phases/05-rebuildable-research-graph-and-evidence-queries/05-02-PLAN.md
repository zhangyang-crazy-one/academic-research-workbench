---
phase: 05-rebuildable-research-graph-and-evidence-queries
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - vendor/patches/file-base/0004-phase5-research-graph.patch
  - vendor/source-manifest.json
  - src/arw/graph_store.py
  - src/arw/graph_mcp.py
  - src/arw/cli.py
  - scripts/build-file-base
  - scripts/file-base-graph-mcp
  - schemas/v1/graph-query-request.schema.json
  - schemas/v1/graph-query-result.schema.json
  - tests/integration/test_graph_queries.py
  - tests/integration/test_graph_mcp_profile.py
  - tests/native/test_phase5_graph_profile.c
autonomous: true
requirements: [GRAPH-03, GRAPH-05]
must_haves:
  truths:
    - "A clean materialized pinned file-base tree can build and validate a disposable research graph generation from the Python input bundle."
    - "MCP uses the official 2025-11-25 stdio/JSON-RPC lifecycle and exposes only named bounded evidence operations, never raw Cypher, SQL or administrative mutation."
    - "Generation selection, database digest, source watermark and query budget are independently verified before any body-bearing result."
  artifacts:
    - path: vendor/patches/file-base/0004-phase5-research-graph.patch
      provides: "Ordered native builder/profile patch over the exact pinned file-base tree"
    - path: src/arw/graph_mcp.py
      provides: "Strict allowlisted MCP query adapter and typed stale/corrupt/unavailable results"
    - path: scripts/file-base-graph-mcp
      provides: "Installed parent-selected graph query launcher with no source checkout fallback"
    - path: tests/native/test_phase5_graph_profile.c
      provides: "Native read-only graph profile and write-clause denial tests"
  key_links:
    - from: src/arw/graph_store.py
      to: vendor/patches/file-base/0004-phase5-research-graph.patch
      via: "input bundle/output manifest contract and native generation validator"
  - from: src/arw/graph_mcp.py
    to: src/arw/graph_models.py
    via: "strict query/result schemas and generation receipt binding"
---

<objective>
Add a reproducible native projection/build seam and a narrow MCP query profile
using the upstream protocol. Preserve the existing five-tool files profile and
keep all graph administration parent-owned.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>05-02-T01 — Build a clean pinned research-graph generation</name>
  <files>vendor/patches/file-base/0004-phase5-research-graph.patch, vendor/source-manifest.json, scripts/build-file-base, tests/native/test_phase5_graph_profile.c, src/arw/graph_store.py</files>
  <read_first>vendor/source-manifest.json; vendor/patches/file-base/0001-0003*.patch; vendor/sources/file-base/src/store; vendor/sources/file-base/src/cypher; scripts/materialize-sources; scripts/build-file-base; 05-RESEARCH.md</read_first>
  <action>Materialize the exact upstream file-base source in a clean temporary tree and add ordered patch `0004-phase5-research-graph.patch` (or the next verified order if the source manifest has advanced). The patch must add an explicit research projection/build profile that reads only the validated input bundle, writes a sibling generation, closes and verifies SQLite/schema/count/digest manifests, and has no watcher, source checkout, network, or query-side mutation. Keep upstream Cypher write/admin clauses rejected. Record pre/post trees, patch digest, compiler profile and native test inventory in `vendor/source-manifest.json`; never patch `vendor/sources/file-base` in place.</action>
  <acceptance_criteria>Clean materialization plus ordered patch builds reproducibly; malformed bundle, duplicate identity, missing watermark, output tamper, write clause, path escape, or partial generation fails closed and leaves the selected generation unchanged. Normal and sanitizer native tests pass and the release binary has no test-only barrier controls.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 ./scripts/verify-sources &amp;&amp; UV_OFFLINE=1 ./scripts/build-file-base --clean &amp;&amp; UV_OFFLINE=1 .venv/bin/python -m pytest -q tests/native/test_phase5_graph_profile.c tests/integration/test_graph_projection.py</automated></verify>
  <done>The exact pinned native tree can build a validated disposable research graph generation with retained source/patch evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>05-02-T02 — Expose bounded allowlisted graph queries over official MCP</name>
  <files>src/arw/graph_mcp.py, scripts/file-base-graph-mcp, schemas/v1/graph-query-*.schema.json, tests/integration/test_graph_queries.py, tests/integration/test_graph_mcp_profile.py</files>
  <read_first>src/arw/files_mcp.py; src/arw/file_contracts.py; src/arw/graph_models.py; vendor/sources/file-base/src/mcp/mcp.c; vendor/sources/file-base/src/cypher/cypher.c; official MCP tools/stdio references in 05-RESEARCH.md</read_first>
  <action>Implement `graph_mcp.py`/launcher using newline-delimited JSON-RPC 2.0 and the official initialize/tools/list/call lifecycle. Register only named operations (`trace_claim`, `trace_source`, `trace_experiment`, `trace_review`, `trace_gate_evidence`, and a bounded schema/health operation if needed); compile each operation to a fixed native query/template and reject arbitrary query strings, SQL, paths, generation IDs, unknown fields and write clauses. Enforce server-owned max depth, fanout, rows, bytes, cursor and deadline, verify the selected generation receipt/database digest/watermark before execution, and return typed structured results with no partial page on timeout or stale/corrupt/unavailable state. Add CLI parent dispatch that selects a validated generation without making the graph authoritative.</action>
  <acceptance_criteria>`tools/list` contains only the declared graph profile; malformed/oversized/unknown/raw/write requests fail before execution; query and restart snapshots show no writes to root/control/generation/cache; exact result envelopes bind operation, schema, generation, watermark, evidence digests and continuation.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 .venv/bin/python -m pytest -q tests/integration/test_graph_queries.py tests/integration/test_graph_mcp_profile.py</automated></verify>
  <done>Authorized clients can perform bounded evidence traces through the official MCP protocol without receiving an administrative or arbitrary Cypher surface.</done>
</task>

</tasks>

<threat_model>
| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T05-01 | mitigate | Close, hash and verify a sibling generation before selection; native and Python digest checks agree. |
| T05-03 | mitigate | Delete/unavailable selection returns typed state and never blocks canonical runtime commands. |
| T05-04 | mitigate | Fixed query templates, strict schemas, native write-clause denial and resource ceilings. |
| T05-07 | verify | Stage only launchers/binaries/contracts; graph databases and fixture data remain evidence/temp only. |
</threat_model>

<verification>
Run native source/build checks, the graph MCP integration subset, no-write tree
manifests, and `git diff --check`. Retain JSON-RPC initialize/tools/call/error
transcripts and native compiler/source identities.
</verification>

<success_criteria>
GRAPH-03's disposable generation and GRAPH-05's bounded query surface are
executable from exact staged/native bytes and cannot accept an arbitrary query.
</success_criteria>

<output>Create `05-02-SUMMARY.md` after execution.</output>
