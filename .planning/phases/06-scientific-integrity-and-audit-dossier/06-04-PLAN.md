---
phase: 06-scientific-integrity-and-audit-dossier
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/arw/audit_dossier.py
  - src/arw/schema_registry.py
  - schemas/v1/audit-dossier.schema.json
  - docs/runtime/scientific-integrity.md
  - docs/runtime/audit-dossier.md
  - tests/unit/test_audit_dossier.py
  - tests/integration/test_audit_dossier_replay.py
  - tests/property/test_audit_dossier_replay.py
  - tests/fixtures/phase6/representative-run/
autonomous: true
requirements: [SCI-01, SCI-06, SCI-07, VER-07]
must_haves:
  truths:
    - "One strict canonical audit dossier manifest deterministically renders JSON and Markdown while remaining non-authoritative references to replayed events/manifests."
    - "The dossier includes run history, manifests/Passports, integrity/provenance/access/claim/review/human/graph/test/build evidence, blockers, and separate technical/release verdicts."
    - "Cold replay after deleting disposable graph/index projections produces byte-identical dossier output or the same typed projection blocker without changing canonical state."
  artifacts:
    - path: src/arw/audit_dossier.py
      provides: "Parent-owned canonical dossier collector, content-addressed manifest, deterministic JSON/Markdown renderer, and replay checker"
    - path: schemas/v1/audit-dossier.schema.json
      provides: "Strict VER-07 dossier wire contract"
    - path: tests/integration/test_audit_dossier_replay.py
      provides: "Frozen rerender, projection-loss, secret exclusion, and authority-boundary evidence"
    - path: docs/runtime/audit-dossier.md
      provides: "Operator contract for dossier sections, replay, blockers, and verdict separation"
  key_links:
    - from: src/arw/audit_dossier.py
      to: src/arw/journal.py
      via: "replay_run/reduce_events validates canonical ledger before collecting inputs"
    - from: src/arw/audit_dossier.py
      to: src/arw/graph_projection.py
      via: "graph receipt/watermark is referenced as disposable evidence and loss yields a typed blocker"
    - from: src/arw/audit_dossier.py
      to: src/arw/review.py
      via: "exact panel/report/synthesis/matrix/minority/dissent hashes are preserved"

<threat_model>
| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T-06-10 | mitigate | Assemble only from replayed canonical records and immutable manifests; renderer never appends events or treats Markdown/SQLite as authority. |
| T-06-11 | mitigate | Canonical bytes freeze generated_at and sort every reference; content-addressed write-once manifest plus byte-identical rerender/property tests prevent nondeterminism. |
| T-06-12 | mitigate | Explicit bounds, redaction, private-path/secret scans, and unresolved-license blockers prevent dossier disclosure or legal laundering. |
| T-06-13 | mitigate | Projection loss/corruption is rebuilt or represented by a typed blocker while ledger/state/claim verdicts remain unchanged. |
</threat_model>

<objective>
Implement VER-07's canonical dossier and cold-replay boundary as a vertical
slice over Phase 4/5 records and the preceding Phase 6 evidence contracts.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>06-04-T01 — Define dossier manifest and deterministic render contracts</name>
  <files>src/arw/audit_dossier.py, src/arw/schema_registry.py, schemas/v1/audit-dossier.schema.json, tests/unit/test_audit_dossier.py, docs/runtime/audit-dossier.md</files>
  <read_first>06-01-PLAN.md; 06-02-PLAN.md; 06-03-PLAN.md; 06-RESEARCH.md section "Canonical dossier manifest and renderers"; src/arw/canonical.py; src/arw/journal.py; src/arw/runtime.py; src/arw/build_identity.py; src/arw/graph_models.py; src/arw/review.py; scripts/verify-phase-5; docs/runtime/durable-provenance.md; docs/runtime/research-graph.md</read_first>
  <action>Implement strict `arw.audit-dossier.v1` models and registry schema containing dossier/run IDs, frozen generated_at, ledger head/history, run/assignment/result/artifact/Passport hashes, integrity receipts, external provenance, access decisions and claim capabilities, review matrix/report/synthesis/minority/dissent hashes, human decisions/waivers/corrections, graph projection receipt/watermark, bounded test/benchmark logs, source/build/integration-lock identity references, technical and release qualification, and typed blockers. Implement canonical manifest sealing excluding only dossier_sha256, content-addressed write-once storage, deterministic JSON bytes, and Markdown rendering that labels non-authoritative projections and links exact hashes. Normalize/sort IDs and relative paths, bound list/output sizes, redact secrets/private full text, and preserve SUP-04/P04-09 or other legal blockers instead of changing release verdict.</action>
  <acceptance_criteria>Manifest schemas reject unknown fields, unsorted/duplicate references, secrets, unbounded paths, self-supplied hashes, and missing technical/release distinction; two renders from one frozen manifest are byte-identical; Markdown cannot be parsed back as authority; required VER-07 sections and exact digest references are present.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/unit/test_audit_dossier.py tests/schema/test_phase6_contracts.py tests/schema/test_schema_registry.py</automated></verify>
  <done>The canonical dossier contract and deterministic views exist without a second state store.</done>
</task>

<task type="auto" tdd="true">
  <name>06-04-T02 — Assemble replayed evidence and prove cold projection-loss equivalence</name>
  <files>src/arw/audit_dossier.py, tests/integration/test_audit_dossier_replay.py, tests/property/test_audit_dossier_replay.py, tests/fixtures/phase6/representative-run/</files>
  <read_first>src/arw/audit_dossier.py; src/arw/journal.py; src/arw/manifests.py; src/arw/runtime.py; src/arw/reducer.py; src/arw/graph_projection.py; src/arw/graph_store.py; src/arw/review.py; src/arw/evidence_access.py; tests/integration/test_graph_rebuild.py; tests/integration/test_graph_authority.py; tests/integration/test_orchestration_replay.py; 06-RESEARCH.md section "Cold replay and projection-loss checks"</read_first>
  <action>Implement parent-only `assemble_audit_dossier` that replays/validates ledger and accepted manifests first, discovers strict receipt/provenance/access/review/human/build/test records by explicit digest, evaluates claim capabilities with an injected clock, and includes graph generation/receipt/watermark only after validation. On missing/stale/corrupt projection, rebuild from canonical records or append a bounded `projection_unavailable` blocker; never infer body evidence from SQLite. Build a representative fixture containing run events, manifests/Passports, Phase 4 panel/review/dissent and human decision, Phase 5 projection receipt, Phase 6 records, tests/build/source identities, and legal blockers. Generate twice with frozen time, compare JSON/Markdown bytes, delete graph/index projections and replay, permute input order/property sequences, and assert canonical ledger/state/claim verdicts are unchanged and no private payload or secret leaks into outputs.</action>
  <acceptance_criteria>Cold replay produces identical canonical dossier bytes and sorted references or the same typed projection blocker; deleting projection databases cannot alter accepted state or claim statuses; stale/missing evidence remains visible as blockers; fixture and property tests reject duplicate IDs, stale references, superseded decisions, unbounded output, private full text, and Markdown/SQLite authority.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/integration/test_audit_dossier_replay.py tests/property/test_audit_dossier_replay.py tests/integration/test_graph_rebuild.py tests/integration/test_graph_authority.py</automated></verify>
  <done>VER-07 dossier assembly is replayable, projection-loss safe, deterministic, bounded, and separate from release permission.</done>
</task>
</tasks>

<verification>
Run schema/unit tests before integration/property tests. Compare retained JSON,
Markdown, canonical ledger, and claim-verdict digests after projection loss,
then rerun the existing Phase 4/5 replay and authority subsets.
</verification>

<success_criteria>
The canonical dossier contains every required audit section, rerenders
byte-identically, survives cold replay, and preserves technical PASS versus
legal/release BLOCKED without exporting private evidence.
</success_criteria>

<output>Create `06-04-SUMMARY.md` after execution.</output>
