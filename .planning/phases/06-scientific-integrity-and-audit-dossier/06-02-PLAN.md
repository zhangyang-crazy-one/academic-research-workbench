---
phase: 06-scientific-integrity-and-audit-dossier
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/arw/experiment_provenance.py
  - src/arw/schema_registry.py
  - schemas/v1/experiment-provenance.schema.json
  - tests/unit/test_experiment_provenance.py
  - tests/integration/test_experiment_provenance.py
  - tests/integration/test_controlled_execution_blocked.py
  - tests/fixtures/phase6/representative-run/experiment/provenance.json
autonomous: true
requirements: [SCI-04, SCI-05]
must_haves:
  truths:
    - "D-04: Externally executed datasets, model/configuration identity, metrics, environment, runner, and artifacts ingest through one strict digest-bound envelope."
    - "D-05: Controlled execution evaluates four independently bound fresh gates—sandbox, accountable approval, environment capture, provenance-equivalence probe—and returns BLOCKED when any is absent, stale, mismatched, or caller-supplied."
    - "D-06: Imported provenance digests and verdicts are recomputed from canonical bytes; no producer boolean can self-attest a pass."
  artifacts:
    - path: src/arw/experiment_provenance.py
      provides: "Strict external provenance envelope, digest validation, redacted environment capture, and four-gate policy evaluator"
    - path: schemas/v1/experiment-provenance.schema.json
      provides: "Cross-language schema for externally executed provenance"
    - path: tests/integration/test_controlled_execution_blocked.py
      provides: "Truth table proving missing/stale/forged qualification gates never execute"
  key_links:
    - from: src/arw/experiment_provenance.py
      to: src/arw/integrity.py
      via: "qualification receipts bind exact experiment subject/config/artifact digests and are freshness-evaluated"
    - from: src/arw/experiment_provenance.py
      to: src/arw/manifests.py
      via: "artifact/config/source links are immutable manifest digests, not producer booleans"
---

<threat_model>
ASVS L1 enforcement: block every high-severity finding before the wave can close.

| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T-06-04 | mitigate | Strict nested models, canonical digest recomputation, unique IDs, normalized paths, and no unknown fields prevent forged provenance. |
| T-06-05 | mitigate | Environment fields use an explicit redacted allowlist; secret-pattern and private-path tests reject raw credentials/full text. |
| T-06-06 | mitigate | No controlled-execution adapter exists; a pure four-gate policy returns BLOCKED and tests assert no subprocess invocation. |
</threat_model>

<asvs_l1>
Controls: V1/V4 parent authority, V5 strict nested validation, V6 digest
binding, V7 redacted bounded logs, V8 secret/private-data protection, V12
bounded process/file resources, and V14 disabled-execution configuration. High
or critical findings block technical qualification; no caller flag may clear a
finding.
</asvs_l1>

<objective>
Add SCI-04/SCI-05 as an external-evidence-only vertical slice. It records enough
identity to replay an external run while making any controlled execution claim
fail closed until a separately qualified future adapter exists.
</objective>

<decision_refs>
D-04: external experiments use one strict provenance schema. D-05: controlled
execution remains blocked until sandbox, approval, environment capture, and
provenance-equivalence gates all pass. D-06: imported digests and verdicts are
recomputed from canonical bytes rather than self-attested.
</decision_refs>

<risks_and_rollback>
Risk: imported metrics are misrepresented as ARW reproduction or environment
capture leaks secrets. Mitigate with `external_only`, redaction allowlists, and
the all-16 gate truth table. If policy tests fail, roll back only new provenance
files/fixtures and registry entries; do not enable a subprocess adapter or alter
Phase 4 execution code.
</risks_and_rollback>

<tasks>
<task type="auto" tdd="true">
  <name>06-02-T01 — Define strict external experiment provenance contracts</name>
  <files>src/arw/experiment_provenance.py, src/arw/schema_registry.py, schemas/v1/experiment-provenance.schema.json, tests/unit/test_experiment_provenance.py, tests/fixtures/phase6/representative-run/experiment/provenance.json</files>
  <read_first>06-01-PLAN.md; .planning/phases/06-scientific-integrity-and-audit-dossier/06-CONTEXT.md decisions D-04 through D-06; 06-RESEARCH.md section "External experiment provenance"; src/arw/models.py; src/arw/manifests.py; src/arw/schema_registry.py; tests/unit/test_orchestration_models.py; tests/fixtures/phase6/representative-run/</read_first>
  <action>Implement strict `arw.experiment-provenance.v1` models for provenance/run/experiment IDs, dataset source digests and access state, model revision/source digest, canonical configuration digest/content type, typed metrics with metric digest, artifact ID/media type/content and manifest digests, redacted allowlisted environment identity/version fields, runner identity/command/host digests and bounded timestamps, `execution_claim.mode=external_only` and status imported|blocked, qualification receipt references, source manifests, and derived `provenance_sha256`. Add registry-generated schema, a representative valid fixture, and parent-owned `ingest_experiment_provenance(provenance, allowed_root, authority_envelope)` that validates/recomputes bytes, writes once under a content-addressed root, and appends one canonical evidence-accepted event through the parent runtime. Recompute dataset/config/metric/artifact digests from canonical bytes or immutable manifests and reject self-attested hashes, duplicate IDs, absolute/uncontrolled paths, secrets, unknown fields, string-to-number coercion, and any mode other than external_only.</action>
  <acceptance_criteria>A valid fixture validates independently and round-trips with deterministic digest; malformed fields, duplicate IDs, unsupported execution mode, secret-looking environment values, unchecked digest, and path escapes fail before write; schema registry count remains derived and Phase 1–5 schemas remain unchanged.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/unit/test_experiment_provenance.py tests/schema/test_phase6_contracts.py tests/schema/test_schema_registry.py</automated></verify>
  <done>SCI-04 has one strict external provenance envelope with canonical source/config/metric/artifact identity.</done>
</task>

<task type="auto" tdd="true">
  <name>06-02-T02 — Enforce four-gate controlled-execution BLOCKED policy</name>
  <files>src/arw/experiment_provenance.py, tests/integration/test_experiment_provenance.py, tests/integration/test_controlled_execution_blocked.py</files>
  <read_first>src/arw/experiment_provenance.py; src/arw/integrity.py; src/arw/runtime.py; src/arw/execution.py; 06-RESEARCH.md sections "External experiment provenance" and "Integrity and provenance integration"; tests/integration/test_orchestration_lifecycle.py; tests/integration/test_human_gates.py</read_first>
  <action>Implement `evaluate_controlled_execution_policy` as a pure parent-side function requiring fresh, exact-subject qualification receipts for `sandbox_approval`, `accountable_approval`, `environment_capture`, and `provenance_equivalence_probe`. Compare receipt subject/config/artifact digests and injected clock; return typed BLOCKED reason codes for each missing/stale/mismatched/unauthorized gate. Keep the execution adapter absent or hard-disabled even when all four gates are present, reject caller booleans such as `sandbox_passed`/`reproduced`, and expose only an explicit imported/external status. Add a truth-table integration fixture covering all 16 gate combinations, stale and forged receipts, failed probe, all-four-present hard-disabled case, and valid external import; monkeypatch subprocess entry points to prove no controlled run starts and verify blocked policy survives projection deletion.</action>
  <acceptance_criteria>Every combination missing one or more of four gates is BLOCKED with exact replacement evidence and zero subprocess calls; forged caller flags do not change outcomes; a valid external import is accepted only as imported external evidence and cannot produce experiment_reproduced; all receipts bind the same subject and freshness window.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/integration/test_experiment_provenance.py tests/integration/test_controlled_execution_blocked.py</automated></verify>
  <done>SCI-04/SCI-05 policy evidence proves external provenance intake and fail-closed controlled-execution boundaries.</done>
</task>
</tasks>

<verification>
Run unit/schema tests before the integration truth table. Then run the existing
runtime/execution subset serially and retain a no-subprocess assertion artifact.
</verification>

<success_criteria>
Externally executed provenance is replayable and strict, while controlled
execution remains explicitly BLOCKED unless all four independently bound gates
pass; this phase still launches no experiment process.
</success_criteria>

<output>Create `06-02-SUMMARY.md` after execution.</output>
