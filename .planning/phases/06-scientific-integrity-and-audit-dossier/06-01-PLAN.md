---
phase: 06-scientific-integrity-and-audit-dossier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arw/integrity.py
  - src/arw/schema_registry.py
  - schemas/v1/integrity-receipt.schema.json
  - tests/schema/test_phase6_contracts.py
  - tests/unit/test_integrity_receipts.py
  - tests/integration/test_integrity_receipts.py
autonomous: true
requirements: [SCI-01]
must_haves:
  truths:
    - "Every integrity receipt is strict, versioned, content-addressed, and records subject/input digests, method/tool identity, verdict, reason codes, and explicit freshness policy."
    - "Changing any bound subject/input digest or crossing valid_until returns a deterministic fail-closed reason without mutating the old receipt."
    - "Phase 6 schemas are generated from the registry and independently validated without a new static schema-count literal."
  artifacts:
    - path: src/arw/integrity.py
      provides: "IntegrityReceipt model, canonical sealing, immutable publication helper, and digest/freshness evaluator"
    - path: schemas/v1/integrity-receipt.schema.json
      provides: "Draft 2020-12 strict cross-language receipt contract"
    - path: tests/integration/test_integrity_receipts.py
      provides: "Mutation, expiry, tamper, and cold-replay evidence for SCI-01"
  key_links:
    - from: src/arw/integrity.py
      to: src/arw/canonical.py
      via: "canonical JSON bytes excluding only receipt_sha256 are hashed before publication"
    - from: src/arw/integrity.py
      to: src/arw/manifests.py
      via: "write-once content-addressed receipt storage rejects replacement and symlink paths"

<threat_model>
| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T-06-01 | mitigate | Recompute receipt digests from canonical bytes and reject producer-supplied hash substitution, duplicate inputs, unknown fields, and noncanonical ordering. |
| T-06-02 | mitigate | Compare current subject/input inventory and injected clock to immutable receipt fields; stale or changed inputs produce explicit blockers. |
| T-06-03 | mitigate | Bound receipt size, normalized IDs, timestamps, and source references; refuse symlink/collision writes using existing manifest primitives. |
</threat_model>

<objective>
Define SCI-01 as an executable strict contract before any provenance or dossier
code consumes it. The result is a reusable receipt primitive that composes with
the existing ledger/manifests and cannot become mutable authority.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>06-01-T01 — Add strict integrity receipt models and registry-derived schema</name>
  <files>src/arw/integrity.py, src/arw/schema_registry.py, schemas/v1/integrity-receipt.schema.json, tests/schema/test_phase6_contracts.py, tests/unit/test_integrity_receipts.py</files>
  <read_first>AGENTS.md; .planning/phases/06-scientific-integrity-and-audit-dossier/06-CONTEXT.md; .planning/phases/06-scientific-integrity-and-audit-dossier/06-RESEARCH.md section "Recommended contract design"; src/arw/canonical.py; src/arw/models.py; src/arw/schema_registry.py; schemas/v1/artifact-manifest.schema.json; tests/schema/test_schema_registry.py; tests/unit/test_canonical.py</read_first>
  <action>Implement strict Pydantic models under `src/arw/integrity.py` for `arw.integrity-receipt.v1`: normalized `receipt_id`, subject kind/id and SHA-256, sorted unique `input_sha256`, method id/version, tool identity with build digest, observed timestamp, `freshness_policy.valid_until` and bounded clock skew, PASS/FAIL/BLOCKED verdict, stable reason codes/text, source-manifest digests, creator identity, and derived `receipt_sha256`. Add `PHASE6_SCHEMA_NAMES` or an equivalent registry tuple to `src/arw/schema_registry.py`, include the receipt in `SCHEMA_NAMES`, generate `schemas/v1/integrity-receipt.schema.json` from the model, and add registry-derived count/drift tests that keep all existing schemas unchanged. Reject extras, coercion, duplicate/noncanonical arrays, invalid digests, non-finite values, future timestamps beyond skew, and caller-provided digest mismatches.</action>
  <acceptance_criteria>Strict model round trips through Draft 2020-12 JSON Schema; `receipt_sha256` equals SHA-256 of canonical JSON with only that field removed; unknown fields, duplicate inputs, wrong digest length, invalid verdict/reason combinations, and noncanonical bytes fail before publication; existing schema registry tests stay green.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/schema/test_phase6_contracts.py tests/schema/test_schema_registry.py tests/unit/test_integrity_receipts.py</automated></verify>
  <done>The checked-in receipt contract is generated/validated from one registry and has deterministic canonical/hash fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>06-01-T02 — Publish immutable receipts and evaluate freshness against live inputs</name>
  <files>src/arw/integrity.py, tests/unit/test_integrity_receipts.py, tests/integration/test_integrity_receipts.py, tests/fixtures/phase6/representative-run/integrity/receipt.json</files>
  <read_first>src/arw/integrity.py; src/arw/manifests.py; src/arw/evidence.py; src/arw/canonical.py; src/arw/runtime.py; tests/unit/test_manifests.py; tests/integration/test_passport_lifecycle.py; 06-RESEARCH.md sections "Canonical authority and immutable storage" and "Integrity and provenance integration"</read_first>
  <action>Implement `seal_integrity_receipt`, write-once content-addressed publication below a caller-provided allowed root, and `evaluate_integrity_receipt(receipt, subject_sha256, input_sha256, now)` returning PASS/FAIL/BLOCKED plus exact reason codes such as `subject_digest_mismatch`, `input_digest_mismatch`, `freshness_expired`, `future_timestamp`, and `missing_source`. Recompute every digest from canonical bytes, reject symlink/collision/replacement writes, sort references, and keep stale receipts immutable. Add a small fixture with a valid receipt and tests for unchanged/fresh PASS, subject mutation, input mutation, expiry, future clock, tampered file, duplicate publication, and cold replay; ensure no caller `fresh=True` or alternate hash can override evaluation.</action>
  <acceptance_criteria>All mutation and expiry cases fail closed with deterministic replacement-evidence references; a second publication cannot replace bytes at the same content address; cold loading and reevaluation produce byte-identical receipt and verdict inputs; no ledger event or mutable projection is written by the evaluator.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/unit/test_integrity_receipts.py tests/integration/test_integrity_receipts.py</automated></verify>
  <done>SCI-01 has immutable receipt files and executable subject/input/freshness invalidation evidence.</done>
</task>
</tasks>

<verification>
Run both task commands serially, `git diff --check`, schema regeneration/drift
checks, and the existing canonical/manifest unit subset before Wave 2.
</verification>

<success_criteria>
SCI-01 is machine-verifiable from canonical receipt bytes and deterministic
freshness/mismatch reason codes, with no mutable receipt authority.
</success_criteria>

<output>Create `06-01-SUMMARY.md` after execution.</output>
