---
phase: 07-installed-e2e-recovery-and-release-qualification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arw/integration_lock.py
  - src/arw/cli.py
  - scripts/stage-plugin
  - scripts/qualify-codex-host
  - tests/unit/test_integration_lock.py
  - tests/integration/test_phase7_installed_e2e.py
  - tests/staged/test_phase7_qualification.py
autonomous: true
requirements:
  - VER-02
must_haves:
  truths:
    - "A source-hidden local-marketplace install exercises the exact staged ARW bytes rather than importing the checkout."
    - "The external local ARS adapter is explicit, versioned, and bound to manifest, upstream commits, full adapter tree, and ARS content tree digests; missing, symlinked, bundled, or drifting input fails closed."
    - "Codex CLI 0.144.4, MCP, lifecycle, hooks, positive allowlist, inventory, SBOM, and build identity are independently evidenced and share one integration-lock identity."
  artifacts:
    - path: "tests/integration/test_phase7_installed_e2e.py"
      provides: "Installed ARW plus external local ARS smoke and bounded route/handoff/result evidence"
    - path: "tests/staged/test_phase7_qualification.py"
      provides: "Source-hidden stage, compatibility, hook, MCP, and release-boundary probes"
    - path: "build/evidence/phase-07/installed-qualification.json"
      provides: "Canonical qualification receipt with stage and external ARS digests"
  key_links:
    - from: "tests/integration/test_phase7_installed_e2e.py"
      to: "src/arw/integration_lock.py"
      via: "build_integration_lock and validate_integration_lock"
      pattern: "ARW_ARS_ROOT"
    - from: "scripts/stage-plugin"
      to: "scripts/qualify-codex-host"
      via: "stage identity and locked installed package"
      pattern: "stage-inventory.json"

<threat_model>
T-07-01: a staged test accidentally imports the source checkout or silently
copies the ARS tree. T-07-02: a mutable local ARS checkout, unsupported Codex
version, changed hook, or missing build artifact is accepted as qualified.
Mitigate with source-hidden local-marketplace installation, explicit
ARW_ARS_ROOT, positive stage allowlist, exact digests, and fail-closed probes.
</threat_model>

<asvs_l1>
V1 input validation: reject missing, symlinked, absolute/escaping, and
untrusted ARS roots. V8 data protection: retain only bounded redacted route
evidence. V14 configuration: bind every installed identity to one lock and
reject unsupported host/version drift.
</asvs_l1>

<objective>
Produce the first Phase 7 installed qualification slice. Prove that the exact
local Codex ARS adapter at
`/home/zhangyangrui/.codex/skills/academic-research-suite` is an external,
lock-bound dependency of the installed ARW package, not a bundled or moving
runtime dependency, and that the exact Codex 0.144.4 host tuple is the only
technical PASS baseline.
</objective>

<decision_refs>
D-07-02, D-07-03, D-07-04, D-07-09, D-07-10, D-07-11, D-07-12, D-07-13.
</decision_refs>

<risks_and_rollback>
If the existing lock contract is already sufficient, add tests and receipts
without redesigning dependency resolution. If a contract gap is found, make
the smallest fail-closed change and keep the current stage allowlist intact.
Rollback is limited to this plan's new tests/receipts; do not remove or alter
existing Phase 4/04.1 qualification evidence.
</risks_and_rollback>

<tasks>
<task id="07-01-T1" type="implementation">
<name>Exercise exact external ARS integration lock</name>
<files>tests/unit/test_integration_lock.py, src/arw/integration_lock.py</files>
<read_first>src/arw/integration_lock.py; vendor/source-manifest.json; MODIFICATIONS.md; /home/zhangyangrui/.codex/skills/academic-research-suite/manifest.json; /home/zhangyangrui/.codex/skills/academic-research-suite/VERSION; /home/zhangyangrui/.codex/skills/academic-research-suite/SKILL.md</read_first>
<action>Build a lock using an explicit `external_ars_root`/`ARW_ARS_ROOT` pointing at the current local adapter and assert adapter version 0.1.20, both pinned upstream commit/tree identities, manifest/version/router agreement, full adapter tree digest, and `ars/` content digest. Add table-driven fail-closed cases for missing root, symlink root, changed manifest/VERSION/SKILL, changed local-only file, upstream commit drift, and a bundled `skills/academic-research-suite` stage. Do not add a moving-latest resolver or copy the external tree into the stage.</action>
<acceptance_criteria>Every identity mismatch raises the existing canonical integration-lock failure; a positive lock records `bundled: false`, explicit root provenance, and deterministic bytes; unchanged Phase 04.1 lock tests remain green.</acceptance_criteria>
<verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/unit/test_integration_lock.py tests/integration/test_version_report.py</automated></verify>
</task>

<task id="07-01-T2" type="implementation">
<name>Add installed source-hidden ARS smoke harness</name>
<files>tests/integration/test_phase7_installed_e2e.py, src/arw/cli.py</files>
<read_first>scripts/stage-plugin; scripts/qualify-codex-host; src/arw/cli.py; tests/staged/test_skill_route.py; tests/staged/test_mcp_launcher.py; /home/zhangyangrui/.codex/skills/academic-research-suite/SKILL.md; /home/zhangyangrui/.codex/skills/academic-research-suite/ars/academic-pipeline/WORKFLOW.md</read_first>
<action>Run the installed package through a local marketplace with the checkout hidden from import/path lookup, networking disabled, fresh HOME and CODEX_HOME, an explicit ARW_ARS_ROOT, and a repository-owned bounded TMPDIR. Invoke the local ARS academic-pipeline route on deterministic fixture input and accept only schema-valid bounded route/handoff/result envelopes containing workflow identity, adapter identity, input/output digests, and redacted command summaries. Assert full transcripts, private text, credentials, and absolute source paths are absent from retained receipts. Ensure CLI route/version output reports the exact adapter rather than a hard-coded older version.</action>
<acceptance_criteria>The smoke fails if ARW imports from the source checkout, ARS is bundled, networking is attempted, external root is implicit, or evidence contains secrets/absolute paths; the positive receipt hashes the installed stage and integration lock.</acceptance_criteria>
<verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 TMPDIR=$(pwd)/build/tmp/phase-07/ars-smoke .venv/bin/python -m pytest -q tests/integration/test_phase7_installed_e2e.py -k "installed or ars"</automated></verify>
</task>

<task id="07-01-T3" type="implementation">
<name>Bind staged qualification and host compatibility probes</name>
<files>tests/staged/test_phase7_qualification.py, scripts/stage-plugin, scripts/qualify-codex-host</files>
<read_first>scripts/stage-plugin; scripts/qualify-codex-host; tests/staged/test_phase4_host_qualification.py; tests/staged/test_manifest_install.py; hooks/hooks.json; SBOM.cdx.json</read_first>
<action>Extend staged probes to assert positive allowlist coverage for every runnable ARW artifact, no ARS source tree, inventory/SBOM/build identity parity, MCP negotiation, CLI prepare/dispatch/panel/gate/hook/recover exposure, official hook observation parity, and exact Codex CLI 0.144.4 host tuple. Unsupported versions must be recorded as drift and fail closed. Keep host execution serial and place receipts below a unique `build/evidence/phase-07` root.</action>
<acceptance_criteria>Locked stage plus exact host canary passes only with matching stage/lock/hook/inventory/SBOM hashes; source-hidden and unsupported-version probes fail closed; no test is deleted or xfailed.</acceptance_criteria>
<verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/staged/test_phase7_qualification.py tests/staged/test_manifest_install.py tests/staged/test_mcp_launcher.py tests/staged/test_skill_route.py</automated></verify>
</task>
</tasks>

<verification>
Run the focused lock/staged tests first, then run `scripts/stage-plugin` once and the exact `scripts/qualify-codex-host` canary once, serially, with `ARW_ARS_ROOT` and `TMPDIR` explicit. Inspect the receipt for stage, lock, ARS, Codex, hook, MCP, inventory, and SBOM hashes before proceeding to Wave 2.
</verification>

<success_criteria>
VER-02 has an installed, source-hidden, exact-version receipt; external local ARS identity is explicit and drift-sensitive; stage and host evidence are bounded, reproducible, and technically PASS or fail closed independently of the legal release verdict.
</success_criteria>

<output>
`tests/integration/test_phase7_installed_e2e.py`, `tests/staged/test_phase7_qualification.py`, updated lock probes if required, and a retained installed qualification receipt under `build/evidence/phase-07`.
</output>
