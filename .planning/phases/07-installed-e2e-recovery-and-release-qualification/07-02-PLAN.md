---
phase: 07-installed-e2e-recovery-and-release-qualification
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/arw/runtime.py
  - src/arw/journal.py
  - src/arw/recovery.py
  - src/arw/evidence.py
  - tests/integration/test_phase7_fault_matrix.py
  - tests/integration/test_recovery_crash.py
  - tests/fixtures/recovery/phase7_faults
autonomous: true
requirements:
  - VER-04
must_haves:
  truths:
    - "Canonical write, fsync, lock, and host-dispatch faults have stable IDs and are injected only through a test-controlled seam."
    - "Hard termination, torn writes, I/O/space failures, lock death, duplicate delivery, and stale completion replay to explicit recovery, bounded retry, or BLOCKED outcomes without laundering failures as cancellation."
    - "Every scenario has parent-owned sidecar evidence and a validated canonical recovery event; middle-chain/hash/manifest/lock damage never self-heals."
  artifacts:
    - path: "tests/integration/test_phase7_fault_matrix.py"
      provides: "Serial deterministic fault matrix and replay assertions"
    - path: "tests/fixtures/recovery/phase7_faults"
      provides: "Stable fault IDs and expected event/reason-code fixtures"
    - path: "build/evidence/phase-07/recovery-matrix.json"
      provides: "Bounded sidecar evidence and aggregate retry/replay verdicts"
  key_links:
    - from: "tests/integration/test_phase7_fault_matrix.py"
      to: "src/arw/recovery.py"
      via: "scan/replay/recover classification"
      pattern: "fault_id"
    - from: "src/arw/runtime.py"
      to: "src/arw/journal.py"
      via: "parent-owned append/fsync boundary"
      pattern: "canonical"

<threat_model>
T-07-03: a child or scheduler writes a retry before the parent has materialized
the canonical attempt. T-07-04: torn/middle-chain/lock damage is treated as a
recoverable tail. T-07-05: process, I/O, or disk faults disappear into
`CANCELLED`, duplicate/stale results become authority, or sidecar evidence is
not tied to the canonical run. Mitigate with stable boundary injection IDs,
parent-only event append, replay classification, and bounded serial evidence.
</threat_model>

<asvs_l1>
V5 validation: reject stale/duplicate/forged envelopes before replay admission.
V7 error handling: preserve bounded raw failure evidence and deterministic
reason codes. V8 integrity: hash sidecars and canonical recovery events without
putting sidecars in the authority path.
</asvs_l1>

<objective>
Close VER-04 with a deterministic, serial, process-level fault matrix that
proves canonical-before-host ordering, at-most-one fresh retry, max two total
attempts, crash-safe replay, and fail-closed handling of non-tail corruption.
</objective>

<decision_refs>
D-07-05, D-07-06, D-07-07, D-07-08.
</decision_refs>

<risks_and_rollback>
Prefer existing recovery seams and fixtures. Any new injection hook must be
disabled by default and unreachable from production input. If a scenario is
environment-sensitive, preserve its raw result as a blocked qualification
case rather than weakening assertions or adding xfail. Revert only new
fault-matrix files or guarded seams; preserve prior recovery behavior.
</risks_and_rollback>

<tasks>
<task id="07-02-T1" type="implementation">
<name>Add deterministic boundary fault controls</name>
<files>src/arw/runtime.py, src/arw/journal.py, src/arw/recovery.py, tests/fixtures/recovery/phase7_faults</files>
<read_first>src/arw/runtime.py; src/arw/journal.py; src/arw/recovery.py; tests/integration/test_recovery.py; tests/integration/test_recovery_crash.py; tests/fixtures/recovery</read_first>
<action>Introduce a test-only fault plan keyed by stable IDs for canonical write-before-commit, fsync, lock acquisition/owner death, host dispatch, and result acceptance boundaries. Ensure the parent coordinator materializes `attempt.prepared` before any host dispatch, appends timeout/cancel request/deadline before host cancel/force, and records terminal observation afterward. Keep fault controls opt-in and reject unknown IDs.</action>
<acceptance_criteria>Production/default runs cannot activate the injector; each named boundary has deterministic pre/post semantics and a canonical event sequence fixture; retry attempts are never scheduled before their parent preparation event.</acceptance_criteria>
<verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/integration/test_recovery.py tests/unit/test_scheduler.py tests/unit/test_reducer.py</automated></verify>
</task>

<task id="07-02-T2" type="implementation">
<name>Implement the serial fault/replay matrix</name>
<files>tests/integration/test_phase7_fault_matrix.py, tests/fixtures/recovery/phase7_faults, src/arw/evidence.py</files>
<read_first>src/arw/evidence.py; src/arw/recovery.py; tests/integration/test_recovery_crash.py; tests/unit/test_recovery_scan.py</read_first>
<action>Run one independent subprocess and run root per case for hard kill, torn final write, middle-chain corruption, I/O failure, disk exhaustion simulation, lock death, duplicate delivery, stale worker completion, timeout, and repairable/malformed proposal. Capture stable fault ID, boundary, bounded stdout/stderr, file snapshots and hashes, process status, replay classification, and reason code in parent-owned sidecars. Assert tail recovery/quarantine only when provable, middle/hash/manifest/lock damage BLOCKED, host/process failures receive at most one fresh retry and total attempts <=2, and malformed/repairable validation gets a fresh retry with exact event order.</action>
<acceptance_criteria>All required fault classes execute serially and produce deterministic expected classifications; sidecars contain no secrets or absolute paths and are hash-bound; no failure is relabeled solely as cancellation.</acceptance_criteria>
<verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 TMPDIR=$(pwd)/build/tmp/phase-07/faults .venv/bin/python -m pytest -q tests/integration/test_phase7_fault_matrix.py tests/integration/test_recovery_crash.py</automated></verify>
</task>

<task id="07-02-T3" type="implementation">
<name>Prove crash/resume and stale-result rejection</name>
<files>tests/integration/test_phase7_fault_matrix.py, src/arw/recovery.py, src/arw/runtime.py</files>
<read_first>src/arw/recovery.py; src/arw/runtime.py; src/arw/reducer.py; tests/integration/test_orchestration_replay.py; tests/integration/test_orchestration_lifecycle.py</read_first>
<action>Kill and resume the same run from fresh parent processes at every canonical write boundary, then replay from the run manifest and ledger only. Assert duplicate command IDs, stale revisions, stale worker completions, and orphaned scheduler intents are rejected; append a canonical recovery event only after validation and preserve the original fault sidecar receipt. Delete disposable projections before replay to prove they are not authority.</action>
<acceptance_criteria>Recovered runs have a deterministic event/hash chain and explicit recovery/quarantine verdict; unresolved non-tail corruption stays BLOCKED; replay does not accept child-owned or stale evidence.</acceptance_criteria>
<verify><automated>UV_OFFLINE=1 PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/integration/test_phase7_fault_matrix.py -k "resume or stale or duplicate or retry"</automated></verify>
</task>
</tasks>

<verification>
Run focused recovery tests after each task, then execute the entire matrix once with a repository-owned bounded TMPDIR and no parallel subprocesses. Check each sidecar and canonical event hash before advancing to Wave 3.
</verification>

<success_criteria>
VER-04 has raw retained evidence for every required fault class, canonical-before-host and timeout/cancel ordering are proved, retries are bounded, and crash/replay classification is deterministic and fail closed.
</success_criteria>

<output>
Guarded deterministic fault seams, `tests/integration/test_phase7_fault_matrix.py`, stable fixtures, and `build/evidence/phase-07/recovery-matrix.json` with a technical verdict independent of legal release permission.
</output>
