---
phase: 02-durable-provenance-runtime
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - src/arw/schema_registry.py
  - src/arw/build_identity.py
  - src/arw/cli.py
  - schemas/v1/**
  - scripts/stage-plugin
  - scripts/verify-phase-2
  - tests/schema/test_schema_drift.py
  - tests/schema/test_cross_language.py
  - tests/integration/test_phase2_durable_runtime.py
  - tests/integration/test_version_report.py
  - tests/staged/test_cli_launcher.py
  - tests/staged/test_manifest_install.py
  - tests/staged/test_private_exclusions.py
  - docs/runtime/durable-provenance.md
autonomous: true
requirements: [RUN-03, RUN-04, RUN-05, RUN-06, RUN-07, RUN-08]
must_haves:
  truths:
    - "D-01 through D-15 are exercised together by one fresh-process, projection-free, crash/recover/checkpoint/resume fixture."
    - "RUN-03 through RUN-08 pass from a clean staged installation with checked schemas and repository-owned evidence, not only source-tree unit tests."
    - "Deleting `passport.json` and every derived projection does not change replay/status; changing canonical manifests, accepted events, or recovery evidence fails closed."
    - "The Phase 1 install, license, confinement, exact journal, and private-exclusion regressions remain green; unresolved SUP-04 release blocking is not weakened."
  artifacts:
    - path: "scripts/verify-phase-2"
      provides: "Deterministic offline Phase 2 qualification and evidence entrypoint"
    - path: "tests/integration/test_phase2_durable_runtime.py"
      provides: "One end-to-end oracle for RUN-03 through RUN-08"
    - path: "docs/runtime/durable-provenance.md"
      provides: "Operator command, status, recovery, and forensic contract"
  key_links:
    - from: "scripts/verify-phase-2"
      to: "build/evidence/phase-02/verdict.json"
      via: "raw command/tree/hash evidence summarized without replacing it"
      pattern: "technical_qualification"
    - from: "scripts/stage-plugin"
      to: "schemas/v1/**"
      via: "all Phase 2 runtime contracts are packaged and inventory-covered"
      pattern: "schema"
    - from: "tests/integration/test_phase2_durable_runtime.py"
      to: "arw status --json"
      via: "fresh staged processes compare accepted revision/head/Passport/recovery fields"
      pattern: "legal_next_transitions"
---

<objective>
Qualify the complete durable provenance runtime from a clean staged plugin and preserve machine-checkable evidence that every Phase 2 design decision holds together.

Purpose: Prevent a collection of passing component tests from being mistaken for process-independent, recoverable runtime behavior.
Output: End-to-end fixture, staged schema/build identity updates, offline verifier/evidence bundle, operator contract, and full repository regression results.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-durable-provenance-runtime/02-CONTEXT.md
@.planning/phases/02-durable-provenance-runtime/02-RESEARCH.md
@.planning/phases/02-durable-provenance-runtime/02-VALIDATION.md
@.planning/phases/02-durable-provenance-runtime/02-04-SUMMARY.md

<interfaces>
The verifier operates with `UV_OFFLINE=1`, records exact argv/cwd/allowlisted environment/stdout/stderr/status, inventories canonical trees before/after rejection/status/replay/recovery, and writes raw evidence under `build/evidence/phase-02/`. `verdict.json` summarizes requirement booleans and hashes but never replaces raw files. The staged plugin must include every registered schema and report the resulting aggregate schema identity through `arw version --json`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close schema, build identity, staged package, and compatibility coverage</name>
  <files>src/arw/schema_registry.py, src/arw/build_identity.py, schemas/v1/**, scripts/stage-plugin, tests/schema/test_schema_drift.py, tests/schema/test_cross_language.py, tests/integration/test_version_report.py, tests/staged/test_cli_launcher.py, tests/staged/test_manifest_install.py, tests/staged/test_private_exclusions.py</files>
  <read_first>02-04-SUMMARY.md and every new schema/model; 02-RESEARCH.md Schema and Compatibility Strategy; current schema registry/build identity/stage allowlist; Phase 1 schema drift/cross-language/version/staged/private tests; supply-chain license verdict and stage inventory rules</read_first>
  <action>Register every Phase 2 schema under stable `$id` values, validate Draft 2020-12 and exact checked-in bytes independently, and add valid/invalid cross-language fixtures for every command/result/event/manifest/status/recovery contract. Update aggregate schema identity and `version --json`. Extend positive staged packaging only for source, schemas, runtime modules, tests/fixtures needed at runtime verification, documentation, and verifier; do not include run data, private papers, raw evidence, caches, ignored source mirrors, or indexes. Preserve Phase 1 schema compatibility/aliases, technical-versus-release license verdict, source digest gates, and private canary exclusions.</action>
  <acceptance_criteria>Every Pydantic canonical boundary has a checked independent schema; schema mutation changes aggregate build identity and fails drift tests; the staged plugin launches all Phase 2 CLI commands; no private/ignored/evidence file enters stage; all Phase 1 staged/version/schema tests pass.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/schema/test_schema_drift.py tests/schema/test_cross_language.py tests/integration/test_version_report.py tests/staged/test_cli_launcher.py tests/staged/test_manifest_install.py tests/staged/test_private_exclusions.py</automated></verify>
  <done>The installed artifact carries and reports the complete durable-runtime contract without weakening Phase 1 packaging gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build one projection-free end-to-end durable runtime fixture</name>
  <files>tests/integration/test_phase2_durable_runtime.py, scripts/verify-phase-2, docs/runtime/durable-provenance.md</files>
  <read_first>All Phase 2 summaries and targeted tests; 02-CONTEXT.md D-01 through D-15; 02-RESEARCH.md Validation Architecture and Required evidence assertions; Phase 1 `scripts/verify-phase-1`, recovery evidence fixture, staging/smoke scripts; current CLI help and schemas</read_first>
  <action>Create a deterministic repository-owned run that initializes a registered domain-neutral workflow, accepts legal lifecycle/decision/attempt events and one artifact, rejects invalid/duplicate/stale/out-of-order/unauthorized requests with unchanged trees, creates and supersedes a Passport, proves dynamic stale evidence blocking, resumes the exact current Passport once, injects a torn final append, observes read-only recovery-required status, explicitly quarantines/recovers, deletes `passport.json` and any derived state, and reconstructs identical accepted state in a fresh staged process. Add a separate middle-chain/manifest tamper copy that remains blocked. `scripts/verify-phase-2` must clean only its owned evidence root, run targeted and staged fixture commands offline, retain exact command/byte/hash/tree evidence, independently validate outputs, and write a concise verdict mapping RUN-03 through RUN-08 and D-01 through D-15. Document exact operator commands, exit semantics, status fields, recovery eligibility, quarantine locations, and cases requiring human forensics without describing features or shortcuts not implemented.</action>
  <acceptance_criteria>The end-to-end test proves all six RUN requirements and all 15 decisions in fresh processes; replay/status after pointer/projection deletion are identical; torn-tail recovery preserves/binds evidence; tampered accepted history blocks; verifier emits raw artifacts plus `verdict.json` with every requirement true and `technical_qualification: PASS`.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 uv run --frozen pytest -q tests/integration/test_phase2_durable_runtime.py &amp;&amp; UV_OFFLINE=1 ./scripts/verify-phase-2 --clean --evidence-root build/evidence/phase-02</automated></verify>
  <done>The complete Phase 2 design intent is reproducible from canonical files and inspectable evidence alone.</done>
</task>

<task type="auto">
  <name>Task 3: Run full offline regression and reconcile the validation contract</name>
  <files>.planning/phases/02-durable-provenance-runtime/02-VALIDATION.md, docs/runtime/durable-provenance.md</files>
  <read_first>Task 1 staged/schema results; Task 2 verdict and raw evidence; 02-VALIDATION.md every task row/Wave 0 item/sign-off; pyproject pytest config; Phase 1 verification script and current release BLOCKED verdict</read_first>
  <action>Run every targeted Phase 2 test, the full `UV_OFFLINE=1 uv run --frozen pytest -q` suite, `scripts/verify-phase-1`, source/license/stage gates as supported by the existing clean environment, and `scripts/verify-phase-2`. Diagnose and fix product/test defects within Phase 2 scope rather than weakening assertions. Update 02-VALIDATION.md task IDs/status/Wave 0 checkboxes and sign-off only from actual command evidence. Confirm Phase 1 SUP-04 release qualification remains BLOCKED unless separate permission evidence genuinely changed; Phase 2 technical PASS must not override it. Record any environment-only skipped gate precisely in the summary and do not declare Phase 2 complete while a RUN requirement or Phase 2 success criterion is unproven.</action>
  <acceptance_criteria>Full offline pytest exits 0; both phase technical verifiers pass; Phase 2 verdict has RUN-03 through RUN-08 true; validation rows are green and evidence-backed; no HIGH threat or unverified Phase 2 criterion remains; release classification is unchanged unless supported by new legal evidence.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 uv run --frozen pytest -q &amp;&amp; ./scripts/verify-phase-1 &amp;&amp; UV_OFFLINE=1 ./scripts/verify-phase-2 --clean --evidence-root build/evidence/phase-02</automated></verify>
  <done>Phase 2 passes its design-intent gate and the complete repository remains regression-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source checkout to staged plugin | Source-only success may hide missing schemas/modules or private leakage. |
| component tests to phase verdict | A summary could claim behavior not proven end to end. |
| raw evidence to verdict | Mutable or incomplete summaries could conceal failed commands/tampering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-17 | Tampering | schema/build identity | mitigate | Independent drift/cross-language validation and aggregate digest. |
| T-02-18 | Information Disclosure | staged package | mitigate | Positive allowlist plus existing canary/symlink/private exclusions. |
| T-02-19 | Repudiation | phase verdict | mitigate | Raw commands/bytes/tree hashes retained and requirement booleans cross-checked. |
| T-02-20 | Tampering/Availability | regression qualification | mitigate | Clean staged fresh-process fixture and full offline suite; no assertion weakening. |
</threat_model>

<verification>
Run staged/schema tests, Phase 2 fixture/verifier, full offline pytest, and Phase 1 verifier; inspect requirement/decision mappings and raw hashes. No unresolved HIGH threat may remain.
</verification>

<success_criteria>
RUN-03 through RUN-08 and D-01 through D-15 are jointly proven from a clean staged runtime, and all prior technical/security/privacy guarantees remain intact.
</success_criteria>

<output>Create `.planning/phases/02-durable-provenance-runtime/02-05-SUMMARY.md` when done.</output>
