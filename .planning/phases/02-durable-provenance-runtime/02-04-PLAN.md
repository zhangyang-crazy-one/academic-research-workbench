---
phase: 02-durable-provenance-runtime
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - src/arw/models.py
  - src/arw/journal.py
  - src/arw/recovery.py
  - src/arw/runtime.py
  - src/arw/reducer.py
  - src/arw/status.py
  - src/arw/cli.py
  - src/arw/evidence.py
  - schemas/v1/recovery-request.schema.json
  - schemas/v1/recovery-receipt.schema.json
  - schemas/v1/event.schema.json
  - src/arw/schema_registry.py
  - tests/unit/test_recovery_scan.py
  - tests/integration/test_recovery.py
  - tests/integration/test_recovery_crash.py
  - tests/integration/test_runtime_status.py
  - tests/fixtures/recovery/tails/**
autonomous: true
requirements: [RUN-04, RUN-06, RUN-07, RUN-08]
must_haves:
  truths:
    - "D-04: Replay and status are read-only, expose the last trustworthy prefix, and never repair canonical bytes."
    - "D-05: Only explicit locked `arw recover` creates recovery state; original damaged segment bytes, digest, fault offset, and quarantine evidence are preserved."
    - "D-06: The next segment begins with one `recovery.completed` event binding prior head/revision, original/quarantine hashes, fault, operator, and reason."
    - "D-07: Only a final incomplete/unverifiable tail is recoverable; manifest, accepted-event, prior/middle-chain, or recovery-binding damage remains BLOCKED for human forensics."
    - "D-15: A trustworthy tail-recovery-required or BLOCKED status exits zero, while no trustworthy prefix exits nonzero."
  artifacts:
    - path: "src/arw/recovery.py"
      provides: "Tail classification, quarantine receipt, and explicit next-segment recovery transaction"
    - path: "schemas/v1/recovery-receipt.schema.json"
      provides: "Digest/offset/operator/reason evidence contract"
    - path: "tests/integration/test_recovery_crash.py"
      provides: "Failpoint, exact-byte, idempotency, and fresh-process recovery evidence"
  key_links:
    - from: "src/arw/recovery.py"
      to: "src/arw/journal.py"
      via: "one shared structured scan determines tail versus blocked damage"
      pattern: "fault_offset"
    - from: "recovery.completed"
      to: "quarantine/<recovery_id>/receipt.json"
      via: "event binds original and quarantine digests plus last valid head"
      pattern: "quarantine_sha256"
    - from: "src/arw/status.py"
      to: "src/arw.recovery"
      via: "read-only health projection from scan result"
      pattern: "recovery_required"
---

<objective>
Add explicit evidence-preserving recovery that continues a validated prefix without rewriting its damaged segment and blocks every non-tail corruption.

Purpose: Prove crash/torn-tail survival while making data loss, forensic uncertainty, and recovery actions independently inspectable.
Output: Tail classifier, recovery request/receipt/event schemas, `arw recover`, recovery-aware replay/status, adversarial corruption fixtures, and crash evidence.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-durable-provenance-runtime/02-CONTEXT.md
@.planning/phases/02-durable-provenance-runtime/02-RESEARCH.md
@.planning/phases/02-durable-provenance-runtime/02-VALIDATION.md
@.planning/phases/02-durable-provenance-runtime/02-03-SUMMARY.md

<interfaces>
`SegmentScan` must retain exact segment path/digest/byte count, accepted byte end, fault offset/class/raw tail, last valid event/revision/head, and health. `RecoveryReceipt` binds those fields plus quarantine raw path/digest, operator, reason code/text, command ID, and timestamp. The original damaged final segment remains byte-for-byte at its canonical path. Replay crosses the damaged tail only when the immediately following segment's first event is a valid `recovery.completed` binding the unchanged original segment and valid quarantine receipt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the adversarial tail, middle-corruption, and recovery crash oracle</name>
  <files>tests/unit/test_recovery_scan.py, tests/integration/test_recovery.py, tests/integration/test_recovery_crash.py, tests/integration/test_runtime_status.py, tests/fixtures/recovery/tails/**</files>
  <read_first>02-03-SUMMARY.md segment/runtime/Passport interfaces; 02-CONTEXT.md D-04 through D-07 and D-15; 02-RESEARCH.md Segment scan result, Explicit recovery transaction, adversarial corruption fixtures, and required evidence assertions; Phase 1 SIGKILL/evidence fixture; src/arw/evidence.py; current journal scanner and status health models</read_first>
  <action>Create deterministic fixtures for no-newline partial JSON, newline-terminated malformed final record, truncated UTF-8, changed final accepted event, changed middle event, deleted/reordered event, chain gap, changed manifest, later valid record after corruption, changed original segment after recovery, changed quarantine raw/receipt, forged recovery event, and unexpected next segment. Unit tests must classify only the final suffix cases as `recoverable_tail`. Integration tests must assert status/replay tree immutability, exact last valid revision/head/fault offset, explicit recover success, original/quarantine byte equality, recovery-first next segment, and permanent BLOCKED results for D-07 cases. Crash tests add failpoints after partial normal append, quarantine raw fsync, receipt fsync, recovery segment publication, and Passport pointer refresh if recovery checkpoints; record argv/environment/streams/exit/filesystem hashes/oracle. Invert expected RED behavior after collection.</action>
  <acceptance_criteria>All fixtures are repository-owned and tests collect; behavior fails only because recovery APIs/failpoints are absent; assertions distinguish trusted-prefix status from no-trusted-prefix failure and cover all event/manifest/recovery tampering classes.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest --collect-only -q tests/unit/test_recovery_scan.py tests/integration/test_recovery.py tests/integration/test_recovery_crash.py &amp;&amp; ! uv run --frozen pytest -q tests/unit/test_recovery_scan.py tests/integration/test_recovery.py tests/integration/test_recovery_crash.py</automated></verify>
  <done>The exact recoverable-tail boundary and crash evidence oracle are observed RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement read-only fault classification and recovery-aware replay/status</name>
  <files>src/arw/journal.py, src/arw/recovery.py, src/arw/models.py, src/arw/reducer.py, src/arw/status.py, schemas/v1/recovery-receipt.schema.json, schemas/v1/event.schema.json, src/arw/schema_registry.py, tests/unit/test_recovery_scan.py, tests/integration/test_runtime_status.py</files>
  <read_first>Task 1 RED scanner/status tests and every corruption fixture; 02-RESEARCH.md fault classification and replay binding rules; Plan 02 structured SegmentScan; Plan 03 Passport/current state; D-04/D-06/D-07/D-15; canonical byte/hash helpers</read_first>
  <action>Extend the shared segment scanner to retain exact byte offsets and classify `healthy`, `recoverable_tail`, or `blocked` without writing. A final malformed/incomplete suffix after the last fully valid event may be recoverable; any manifest failure, changed accepted event/hash/revision, corruption with later event, non-final segment damage without a valid recovery boundary, or prior recovery binding mismatch is blocked. Teach replay to validate an existing recovery boundary by requiring the next segment's first event, previous head/revision, original full digest/count/fault, quarantine receipt/raw digest, and reason/operator fields before ignoring the prior tail. Project last trustworthy prefix, recovery health, blocker code, and legal `recover` transition into status; status/replay never create quarantine, new segments, or pointers and follow D-15 exit rules.</action>
  <acceptance_criteria>Unit classification matches every fixture; healthy and previously recovered runs replay fully; recoverable tails report exact prefix/fault with status exit 0; D-07 corruption reports BLOCKED when a trustworthy prefix exists or nonzero when none exists; all read-only tree snapshots are unchanged.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/unit/test_recovery_scan.py tests/integration/test_runtime_status.py tests/integration/test_segmented_journal.py tests/unit/test_reducer.py</automated></verify>
  <done>One scanner supplies trustworthy recovery health without implicit repair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement explicit locked quarantine and recovery continuation</name>
  <files>src/arw/recovery.py, src/arw/runtime.py, src/arw/journal.py, src/arw/models.py, src/arw/cli.py, src/arw/evidence.py, schemas/v1/recovery-request.schema.json, schemas/v1/recovery-receipt.schema.json, schemas/v1/event.schema.json, src/arw/schema_registry.py, tests/integration/test_recovery.py, tests/integration/test_recovery_crash.py</files>
  <read_first>Task 1 RED recovery/crash tests; Task 2 classifier/replay API; 02-CONTEXT.md D-05/D-06/D-07; 02-RESEARCH.md exact recovery transaction and crash order; RuntimeCommandService lock/identity/idempotency handling; manifests atomic write; evidence command recorder</read_first>
  <action>Add strict `arw recover --request` through the sole-writer service. Under `.journal.lock`, rescan and require request expected revision/head/original segment digest plus recoverable-tail health. Copy the complete unchanged damaged segment to `quarantine/<recovery_id>/segment.raw`, fsync it, write/fsync a canonical receipt with exact fault/operator/reason fields, verify both digests, and publish the next numeric segment atomically with one `recovery.completed` event as its first record. Bind the prior head/revision, original segment digest/count, quarantine raw/receipt hashes, fault offset/class, operator, command/event IDs, timestamp, and reason. Leave the damaged segment untouched. Make retries idempotent only on exact matching evidence/event identity; reject duplicate/conflicting recovery or any D-07 damage. Add deterministic test-only failpoints around each durable boundary and retain raw recovery evidence without promoting observations to authority. If recovery creates a coherent Passport boundary, use Plan 03's standard checkpoint path after recovery event acceptance, never a special mutable shortcut.</action>
  <acceptance_criteria>Recover appends exactly one bound event in exactly one next segment; original and quarantine raw bytes/digests match; fresh replay reaches the recovery revision and later legal transitions; duplicate retry adds nothing; every failpoint yields either wholly absent or wholly replayable recovery; forged/changed evidence and all D-07 cases remain blocked.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/integration/test_recovery.py tests/integration/test_recovery_crash.py tests/integration/test_journal_replay.py tests/integration/test_passport_lifecycle.py tests/integration/test_runtime_status.py</automated></verify>
  <done>Tail recovery is explicit, auditable, crash-safe, and never a silent canonical rewrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| malformed bytes to tail classifier | Accepted corruption must not be mislabeled disposable tail. |
| original segment to quarantine bundle | Evidence copy and receipt must represent exact bytes/fault. |
| quarantine bundle to recovery event | Forged or changed evidence must not authorize chain continuation. |
| interrupted recover process to next invocation | Compound crashes must not create two recoveries or skip bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-13 | Tampering | fault classification | mitigate | Final-suffix-only rule plus adversarial middle/later-chain fixtures. |
| T-02-14 | Repudiation/Tampering | quarantine evidence | mitigate | Exact full bytes, SHA-256, offset, receipt, operator/reason cross-binding. |
| T-02-15 | Elevation/Tampering | recovery continuation | mitigate | Explicit authorized command under lock; first-event and prior-head validation. |
| T-02-16 | Denial of Service/Race | crash during recovery | mitigate | Ordered fsync/atomic publication, idempotency, and failpoint oracle. |
</threat_model>

<verification>
Run scanner, corruption, recovery, crash, status, Passport, and Phase 1 forced-stop tests; inspect raw evidence cross-bindings. No unresolved HIGH threat may remain.
</verification>

<success_criteria>
RUN-04/RUN-06/RUN-07/RUN-08 and D-04 through D-07/D-15 are proven by byte-preserving recovery and fail-closed corruption behavior.
</success_criteria>

<output>Create `.planning/phases/02-durable-provenance-runtime/02-04-SUMMARY.md` when done.</output>
