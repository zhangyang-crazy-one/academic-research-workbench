---
phase: 02-durable-provenance-runtime
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/arw/models.py
  - src/arw/manifests.py
  - src/arw/runtime.py
  - src/arw/reducer.py
  - src/arw/status.py
  - src/arw/cli.py
  - schemas/v1/artifact-manifest.schema.json
  - schemas/v1/material-passport.schema.json
  - schemas/v1/passport-pointer.schema.json
  - schemas/v1/artifact-request.schema.json
  - schemas/v1/checkpoint-request.schema.json
  - schemas/v1/resume-request.schema.json
  - schemas/v1/event.schema.json
  - src/arw/schema_registry.py
  - tests/unit/test_manifests.py
  - tests/unit/test_reducer.py
  - tests/integration/test_passport_lifecycle.py
  - tests/integration/test_runtime_attempts.py
  - tests/integration/test_runtime_status.py
autonomous: true
requirements: [RUN-05, RUN-06, RUN-07]
must_haves:
  truths:
    - "D-08: Passport creation is legal only at a stage handoff, completed human decision, explicit checkpoint, or recovery boundary."
    - "D-09: Every Passport is immutable and content-addressed, becomes current only through `passport.accepted`, and `passport.json` is a rebuildable pointer."
    - "D-10: Parent and superseded Passport hashes remain auditable; stale or double resume is rejected with current Passport/revision and never creates an implicit branch."
    - "D-11: Accepted evidence timestamps/rules never mutate; freshness is computed dynamically and stale evidence blocks affected transitions until new evidence and Passport acceptance."
    - "D-14: Passport snapshots preserve stable pending decisions and active attempts needed for process-independent resume."
  artifacts:
    - path: "src/arw/manifests.py"
      provides: "Content-addressed artifact and Material Passport installation/validation"
    - path: "schemas/v1/material-passport.schema.json"
      provides: "Immutable checkpoint and freshness contract"
    - path: "tests/integration/test_passport_lifecycle.py"
      provides: "Acceptance, supersession, pointer loss, stale/double resume, and crash-boundary proofs"
  key_links:
    - from: "src/arw/runtime.py"
      to: "src/arw/manifests.py"
      via: "immutable file staged before one accepting event"
      pattern: "passport.accepted"
    - from: "src/arw/reducer.py"
      to: "passports/sha256"
      via: "accepted event hash selects current immutable Passport"
      pattern: "passport_sha256"
    - from: "passport.json"
      to: "src/arw/reducer.py"
      via: "pointer can be deleted and derived from replay"
      pattern: "accepted_revision"
---

<objective>
Implement immutable content-addressed artifact acceptance, coherent Material Passport checkpoints, dynamic freshness, and exact single-use resume.

Purpose: Make accepted work and resume boundaries reconstructible without mutable singleton state or conversation memory.
Output: Manifest stores/schemas, artifact/checkpoint/resume commands, atomic Passport pointer, reducer/status extensions, and lifecycle integration tests.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-durable-provenance-runtime/02-CONTEXT.md
@.planning/phases/02-durable-provenance-runtime/02-RESEARCH.md
@.planning/phases/02-durable-provenance-runtime/02-VALIDATION.md
@.planning/phases/02-durable-provenance-runtime/02-02-SUMMARY.md

<interfaces>
Artifact manifests identify content path/hash, kind/media type, producer/attempt, base revision, consumed input hashes, and creation metadata. Passport content binds run/workflow identity, based-on revision/head, stage/checkpoint kind, parent and superseded Passport hashes, accepted artifact/evidence manifest hashes, pending decisions, active attempts, and frozen freshness rules. Canonical files live at `manifests/artifacts/sha256/<digest>.json` and `passports/sha256/<digest>.json`; the latest pointer is `passport.json` and is never consulted as authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify immutable artifact, Passport, freshness, and resume behavior as RED tests</name>
  <files>tests/unit/test_manifests.py, tests/unit/test_reducer.py, tests/integration/test_passport_lifecycle.py, tests/integration/test_runtime_attempts.py, tests/integration/test_runtime_status.py</files>
  <read_first>02-02-SUMMARY.md runtime transaction and segment APIs; 02-CONTEXT.md D-08 through D-11 and D-14; 02-RESEARCH.md Immutable Artifacts and Material Passports and Required evidence assertions; vendor handoff_schemas.md Schema 9/reset/freshness sections; src/arw/evidence.py atomic writer; existing digest/symlink/path tests</read_first>
  <action>Write RED tests for canonical artifact manifest bytes and digest paths; path traversal/symlink/content hash/base revision/attempt freshness rejection; orphan manifest non-acceptance; one `artifact.accepted` event per valid command; coherent and incoherent checkpoint cases; Passport canonical hash, parent/superseded links, accepted revision/head, decision/attempt snapshot, and freshness rules; event-before-pointer authority; pointer deletion/corruption with unchanged replay status; stale evidence transition blocking without old Passport mutation; exact current resume success; stale hash, stale revision, double resume, superseded Passport, and expired-evidence rejection with current Passport/revision. Compare full canonical trees on every rejection and invert expected behavior failures after collection.</action>
  <acceptance_criteria>Tests collect and fail because immutable stores/checkpoint/resume APIs are absent; D-08/D-09/D-10/D-11/D-14 and RUN-05/RUN-06/RUN-07 are explicit byte/hash/state assertions.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest --collect-only -q tests/unit/test_manifests.py tests/integration/test_passport_lifecycle.py &amp;&amp; ! uv run --frozen pytest -q tests/unit/test_manifests.py tests/integration/test_passport_lifecycle.py</automated></verify>
  <done>Artifact/Passport/resume guarantees are observed RED before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement content-addressed artifact acceptance</name>
  <files>src/arw/models.py, src/arw/manifests.py, src/arw/runtime.py, src/arw/reducer.py, src/arw/cli.py, schemas/v1/artifact-manifest.schema.json, schemas/v1/artifact-request.schema.json, schemas/v1/event.schema.json, src/arw/schema_registry.py, tests/unit/test_manifests.py, tests/integration/test_runtime_attempts.py</files>
  <read_first>Task 1 RED artifact tests; 02-RESEARCH.md Artifact acceptance and safe commit order; 02-02 RuntimeCommandService, attempt state, journal lock/append; src/arw/canonical.py; src/arw/evidence.py `_write_bytes`; run-root immutable input path/symlink checks; current schema registry</read_first>
  <action>Implement strict `ArtifactManifest` and acceptance request/result models plus schemas. Validate normalized repository-relative content under the existing run root by walking every path component without symlinks, require a regular file, hash exact bytes, validate active attempt/base revision/consumed hashes when present, canonicalize the manifest, and install it exclusively at its SHA-256 path using temp write, file fsync, atomic rename/exclusive collision verification, and directory fsync. Under the same runtime lock, append one `artifact.accepted` event referencing manifest/content hashes. Treat installed but unreferenced files as non-canonical orphans; never infer acceptance from directory presence. Reject changed content, digest collision bytes, stale/closed attempts, and duplicate identities without journal or store mutation.</action>
  <acceptance_criteria>Accepted artifacts are selected only by `artifact.accepted` events and exact digest-addressed manifests; stale/invalid proposals append nothing; replay reconstructs accepted/superseded artifact state; artifact unit and attempt integration tests pass independently under Pydantic and JSON Schema validation.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/unit/test_manifests.py tests/integration/test_runtime_attempts.py tests/integration/test_runtime_transitions.py tests/schema/test_schema_drift.py</automated></verify>
  <done>Artifact existence and artifact acceptance are separate, auditable states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Passport checkpoint, pointer, freshness, and exact resume</name>
  <files>src/arw/models.py, src/arw/manifests.py, src/arw/runtime.py, src/arw/reducer.py, src/arw/status.py, src/arw/cli.py, schemas/v1/material-passport.schema.json, schemas/v1/passport-pointer.schema.json, schemas/v1/checkpoint-request.schema.json, schemas/v1/resume-request.schema.json, schemas/v1/event.schema.json, src/arw/schema_registry.py, tests/integration/test_passport_lifecycle.py, tests/integration/test_runtime_status.py</files>
  <read_first>Task 1 RED Passport/resume tests; Task 2 manifest store; 02-CONTEXT.md D-08/D-09/D-10/D-11/D-14; 02-RESEARCH.md Passport revision, status freshness, and crash order; ARS Schema 9 reset boundary/freshness rules; 02-02 reducer/runtime/status APIs</read_first>
  <action>Implement strict Material Passport, pointer, checkpoint, and resume models/schemas. Permit checkpoints only when workflow metadata marks stage handoff, a human decision just completed, the operator requests an explicit checkpoint, or recovery later supplies its boundary. Freeze accepted artifact/evidence hashes, timestamps/rules, pending decisions, active attempts, based-on revision/head, stage, parent and superseded hashes. Install Passport by content hash, append/fsync `passport.accepted`, then atomically replace/fsync `passport.json`; a failpoint between event and pointer must replay the event as authority. Compute freshness from caller-supplied `now` without modifying old bytes and expose stale blockers/legal transitions. Resume under the run lock only from the current unconsumed Passport and expected revision, append `resume.accepted` once, and reject stale/superseded/double/expired cases with current state. Provide an explicit derived-pointer rebuild helper/command if needed; never invoke it from status.</action>
  <acceptance_criteria>Every Passport is immutable and hash-addressed; event acceptance survives pointer deletion or stale pointer; supersession remains replay-visible; old bytes do not change when freshness expires; exact current resume appends once; all stale/double/branching cases append nothing and report current Passport/revision; status shows current Passport, blockers, attempts, decisions, and legal transitions.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/integration/test_passport_lifecycle.py tests/integration/test_runtime_status.py tests/integration/test_runtime_attempts.py tests/unit/test_reducer.py tests/schema/test_schema_drift.py</automated></verify>
  <done>Process-independent checkpoint/resume is grounded in immutable accepted Passport history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| artifact bytes to immutable manifest | A caller may swap, traverse to, or mis-hash content. |
| installed manifest/Passport to accepted state | Files must not become canonical without an event. |
| supplied Passport to resume | A stale/superseded/double-consumed boundary must not branch history. |
| current time to freshness projection | Time evaluation must not mutate historical evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-09 | Tampering/Information Disclosure | artifact path/content | mitigate | Relative regular-file checks, no symlinks, exact content/manifest hashes. |
| T-02-10 | Tampering/Repudiation | acceptance boundary | mitigate | Reducer follows accepting events only; orphan and pointer-loss tests. |
| T-02-11 | Replay/Elevation | resume | mitigate | Current hash/revision and unconsumed boundary checks under sole-writer lock. |
| T-02-12 | Tampering | freshness | mitigate | Frozen rules/timestamps, injected clock, dynamic blocker with immutable-byte assertions. |
</threat_model>

<verification>
Run artifact, Passport, attempt, status, schema, and pointer-loss/crash tests. Inspect that every accepted hash is linked by its event and no unresolved HIGH threat remains.
</verification>

<success_criteria>
RUN-05/RUN-06/RUN-07 and D-08 through D-11/D-14 are fully represented by immutable files, accepted events, replay state, and rejection tests.
</success_criteria>

<output>Create `.planning/phases/02-durable-provenance-runtime/02-03-SUMMARY.md` when done.</output>
