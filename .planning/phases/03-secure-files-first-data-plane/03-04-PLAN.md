---
phase: 03-secure-files-first-data-plane
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - vendor/patches/file-base/0005-phase3-research-retrieval.patch
  - vendor/source-manifest.json
  - src/arw/files.py
  - tests/integration/test_files_formats.py
  - tests/integration/test_file_generations.py
  - tests/integration/test_files_mcp.py
  - tests/schema/test_files_contracts.py
autonomous: true
requirements: [FILE-03, FILE-04, FILE-06, FILE-07]
must_haves:
  truths:
    - "D-07: Search, outline, and context may return selected-generation metadata, but stale documents never return snippets, outlines, context, or body."
    - "D-11: search_files requires explicit exact or full_text mode and a versioned deterministic ranking/tie-break contract, never raw FTS syntax."
    - "D-13: The MCP never parses raw PDF; only complete parent-registered UTF-8 extraction with provenance can be queried."
    - "D-14: Outlines are deterministic and format-aware for Markdown, LaTeX, and supported source formats; unsupported text returns no_structure."
    - "D-15: Context is anchored to a same-file, same-generation hit or explicit location and performs no semantic or cross-file expansion."
    - "D-16: Ordinary extraction failures degrade one document, while integrity failures block a generation and extractor versions force re-extraction."
  artifacts:
    - path: "vendor/patches/file-base/0005-phase3-research-retrieval.patch"
      provides: "Bounded exact/full-text search, deterministic format outlines, and anchored context implementations"
    - path: "tests/integration/test_files_formats.py"
      provides: "CJK, Markdown, LaTeX, BibTeX, source, plain-text, extraction, freshness, and ranking acceptance matrix"
    - path: "src/arw/files.py"
      provides: "Format/extractor-version generation metadata consumed by the native read-only plane"
  key_links:
    - from: "search_files"
      to: "read_file"
      via: "file_id plus expected_digest search-following read handshake"
      pattern: "expected_digest"
    - from: "get_context"
      to: "search_files"
      via: "same generation/file hit identity and explicit source location"
      pattern: "hit_id"
    - from: "extraction-registration.schema.json"
      to: "vendor/patches/file-base/0005-phase3-research-retrieval.patch"
      via: "eligible UTF-8 extracted text and provenance rows only"
      pattern: "extractor_version"
---

<objective>
Complete the research retrieval plane with bounded exact and CJK-capable full-text search, deterministic format-aware outlines, anchored context windows, and registered PDF extraction behavior.

Purpose: Provide the useful multilingual research operations promised by Phase 3 while preserving selected-generation consistency and preventing all stale or unregistered body-derived output.
Output: Ordered retrieval patch, explicit ranking/format semantics, and an exhaustive multilingual research-format acceptance suite.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-secure-files-first-data-plane/03-CONTEXT.md
@.planning/phases/03-secure-files-first-data-plane/03-RESEARCH.md
@.planning/phases/03-secure-files-first-data-plane/03-VALIDATION.md
@.planning/phases/03-secure-files-first-data-plane/03-03-SUMMARY.md
@generated/file-contracts.h

<interfaces>
`search_files` receives an explicit mode. Exact mode performs literal normalized content matching; full_text accepts plain text terms only, never SQL/FTS grammar, and uses a declared tokenizer/query compiler/ranking version plus stable file/location tie-breaks. Search results bind `generation_id`, `file_id`, digest, source location, freshness, and a hit identity. Before any snippet/outline/context is emitted, the implementation checks current live digest against the indexed digest. `get_outline` has deterministic parsers for declared formats and returns `no_structure` elsewhere. `get_context` accepts a hit identity or explicit same-generation file/location anchor and returns bounded adjacent lines/bytes only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify multilingual search, format, freshness, and extraction behavior as RED tests</name>
  <files>tests/integration/test_files_formats.py, tests/integration/test_file_generations.py, tests/integration/test_files_mcp.py, tests/schema/test_files_contracts.py</files>
  <read_first>03-CONTEXT.md D-06/D-07/D-08/D-11/D-13 through D-16; 03-RESEARCH.md Search Semantics, CJK strategy, Outline/Context, PDF boundary, freshness matrix, pagination/ranking; 03-03-SUMMARY.md; tests/fixtures/files-first/**; schemas/v1/files-search-request.schema.json; schemas/v1/files-outline-result.schema.json; schemas/v1/files-context-result.schema.json</read_first>
  <action>Remove the Plan 04 skip and define golden results for exact literal and full-text queries over Chinese, mixed CJK/Latin, Markdown, LaTeX, BibTeX, source code, and plain text. Assert normalization, tokenizer/query-compiler/ranking versions, deterministic score/location/file tie-breaks, pagination equivalence, plain-text-only queries, hard limits, malformed/operator-heavy input rejection, and timeout with no partial hits. For every operation, mutate/rename/delete a file after generation selection and require metadata-only stale results with no snippet/outline/context. Add deterministic Markdown heading, LaTeX section, and source symbol outline goldens; BibTeX/plain unsupported structure returns no_structure. Context tests bind a hit or explicit source location to the same file/generation and reject cross-file, stale, free-semantic, or cursor-replayed anchors. Cover valid, missing, old-version, failed, inaccessible, malformed, and raw-only PDF extraction registrations with provenance assertions and canary exclusion.</action>
  <acceptance_criteria>Tests fail only for absent search/outline/context behavior; every supported format has a positive and stale/degraded case; ranking goldens include ties and page boundaries; no test relies on implementation-defined SQLite scores without the versioned normalization contract.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 uv run --frozen pytest --collect-only -q tests/integration/test_files_formats.py &amp;&amp; ! UV_OFFLINE=1 uv run --frozen pytest -q tests/integration/test_files_formats.py</automated></verify>
  <done>All multilingual and research-format retrieval semantics are observed RED, including stale and extraction failure cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic exact/full-text search with freshness gates</name>
  <files>vendor/patches/file-base/0005-phase3-research-retrieval.patch, vendor/source-manifest.json, tests/integration/test_files_formats.py, tests/integration/test_files_mcp.py, tests/schema/test_files_contracts.py</files>
  <read_first>Task 1 search tests; 03-CONTEXT.md D-07/D-08/D-11/D-12; 03-RESEARCH.md FTS5/CJK tokenizer options, literal exact search, query compiler, ranking normalization, timeout/progress handler, stale-body gate; generated/file-contracts.h; patch 0003 index schema and patch 0004 dispatch/confinement primitives</read_first>
  <action>Create ordered patch `0005` against the pinned patched source. Implement exact mode as bounded literal normalized matching and full_text as a fixed plain-text tokenizer/query compiler that escapes/rejects raw FTS control syntax and supports the locked CJK token strategy. Return a declared ranking version and normalize scores into deterministic sortable values with explicit location/file tie-breakers; bind cursor to mode, normalized query, filters, ranking version, generation, and last full sort tuple. Execute against the startup-selected immutable generation with SQLite authorizer/query-only/progress ceilings. Before returning snippets, descriptor-open the current file and compare digest; stale entries may return indexed metadata/freshness only, with all body-derived fields structurally absent. Build a complete page before response and return no hits/cursor on timeout or budget abort.</action>
  <acceptance_criteria>Exact and full_text are behaviorally distinct and schema-valid; Chinese and mixed queries match goldens; raw FTS/SQL constructs cannot alter query structure; repeated/paginated ranking is stable; every stale/replaced/deleted case has zero snippet bytes; search tests and prior list/read tests pass.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 uv run --frozen pytest -q tests/integration/test_files_formats.py -k 'search or cjk or ranking or stale' tests/integration/test_files_mcp.py tests/schema/test_files_contracts.py</automated></verify>
  <done>Agents receive bounded deterministic search results without stale snippets or query-language injection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement deterministic outlines, anchored context, and registered extraction semantics</name>
  <files>vendor/patches/file-base/0005-phase3-research-retrieval.patch, vendor/source-manifest.json, src/arw/files.py, tests/integration/test_files_formats.py, tests/integration/test_file_generations.py</files>
  <read_first>Task 1 outline/context/PDF tests; Task 2 search hit identity; 03-CONTEXT.md D-13 through D-16; 03-RESEARCH.md format-aware structure extraction, context anchoring, parent PDF extraction registration, extractor invalidation; patch 0003 generation tables; patch 0004 live digest primitives</read_first>
  <action>Extend patch `0005` with deterministic, versioned parsers over selected-generation UTF-8 content for Markdown ATX/setext headings, LaTeX section commands under a declared non-expanding grammar, and supported source-language symbol shapes already represented by fixtures; do not use heuristic semantic headings. Return ordered bounded nodes with exact source spans, or typed no_structure. Implement context as bounded adjacent lines/bytes around a validated same-generation hit or explicit file/location; reject natural-language expansion, other files, stale hit IDs, and generation mismatch. Reuse the live digest gate before emitting any outline/context text. Consume PDF content only from complete accessible registrations whose source digest, extraction digest, extractor ID/version, encoding, and completion state match the closed generation; never open raw PDF for parsing. Make ordinary document/extraction failures visible as degraded metadata and keep integrity failures promotion-blocking. Update ordered patch/source identity.</action>
  <acceptance_criteria>All format goldens and no_structure cases pass; context never leaves its anchor file/generation; stale entries produce no outline/context text; raw or invalid PDF is unsearchable; valid registered extraction has full provenance; extractor-version changes invalidate old text; complete Phase 3 format and update suites pass.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 ./scripts/verify-sources &amp;&amp; UV_OFFLINE=1 uv run --frozen pytest -q tests/integration/test_files_formats.py tests/integration/test_file_generations.py tests/integration/test_files_mcp.py</automated></verify>
  <done>All five tools now provide bounded multilingual research retrieval with explicit format and provenance semantics.</done>
</task>

</tasks>

<threat_model>
| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T03-04 | mitigate | Live descriptor digest gate before every snippet/outline/context body field. |
| T03-05 | mitigate | Plain-text query compiler, SQLite authorizer/progress ceilings, complete-page-only responses. |
| T03-06 | mitigate | Registration/digest/version/completion checks; no raw PDF parser; private canary scans. |
| T03-07 | mitigate | Versioned tokenizer/ranking/parser contracts and ordered patch/source identity. |
</threat_model>

<verification>
Run the complete format/update/MCP suites, source verification, ranking pagination goldens, stale canary scans, and `git diff --check`. Verify projection deletion plus rebuild reproduces normalized results.
</verification>

<success_criteria>
FILE-03, FILE-04, FILE-06, and FILE-07 are complete across CJK, Markdown, LaTeX, BibTeX, source, plain text, and registered PDF extraction, with no stale body-derived output and no hidden semantic expansion.
</success_criteria>

<output>Create `.planning/phases/03-secure-files-first-data-plane/03-04-SUMMARY.md` when done.</output>
