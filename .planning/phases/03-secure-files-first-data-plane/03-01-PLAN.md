---
phase: 03-secure-files-first-data-plane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arw/file_models.py
  - src/arw/file_contracts.py
  - src/arw/schema_registry.py
  - schemas/v1/file-root.schema.json
  - schemas/v1/file-identity-manifest.schema.json
  - schemas/v1/file-generation-manifest.schema.json
  - schemas/v1/extraction-registration.schema.json
  - schemas/v1/file-admin-receipt.schema.json
  - schemas/v1/files-list-request.schema.json
  - schemas/v1/files-list-result.schema.json
  - schemas/v1/files-read-request.schema.json
  - schemas/v1/files-read-result.schema.json
  - schemas/v1/files-search-request.schema.json
  - schemas/v1/files-search-result.schema.json
  - schemas/v1/files-outline-request.schema.json
  - schemas/v1/files-outline-result.schema.json
  - schemas/v1/files-context-request.schema.json
  - schemas/v1/files-context-result.schema.json
  - scripts/generate-file-contract-header
  - generated/file-contracts.h
  - tests/schema/test_files_contracts.py
  - tests/unit/test_file_models.py
  - tests/unit/test_file_generations.py
  - tests/integration/test_files_admin.py
  - tests/integration/test_file_generations.py
  - tests/integration/test_files_mcp.py
  - tests/integration/test_files_formats.py
  - tests/integration/test_files_security.py
  - tests/fixtures/files-first/**
autonomous: true
requirements: [FILE-01, FILE-02, FILE-06, FILE-07]
must_haves:
  truths:
    - "D-05: A logical file_id survives only an unambiguous rename; duplicates remain distinct and ambiguous matching becomes delete/create."
    - "D-06: list_files and read_file use a live confined descriptor, while index-derived operations name an immutable selected generation."
    - "D-08: A search-following read binds file_id and expected_digest; replacement yields stale_conflict without body bytes."
    - "D-10 through D-12: read ranges, cursors, modes, ranking version, limits, and timeout outcomes are explicit strict contracts."
    - "D-16: Extraction state distinguishes ordinary degraded documents from generation-integrity failures and binds extractor version."
  artifacts:
    - path: "src/arw/file_models.py"
      provides: "Strict root, logical identity, generation, extraction, cursor, freshness, range, and receipt models"
    - path: "schemas/v1/files-read-result.schema.json"
      provides: "No-body stale-conflict and bounded continuation contract"
    - path: "generated/file-contracts.h"
      provides: "Deterministically generated native constants and schema digests"
    - path: "tests/fixtures/files-first/"
      provides: "Multilingual, duplicate, stale-canary, sensitive, source-format, and registered-extraction corpus"
  key_links:
    - from: "src/arw/file_models.py"
      to: "schemas/v1/file-generation-manifest.schema.json"
      via: "model/schema parity and canonical serialization tests"
      pattern: "generation_id"
    - from: "scripts/generate-file-contract-header"
      to: "generated/file-contracts.h"
      via: "checked schemas are the sole input to the embedded native contract"
      pattern: "schema_sha256"
    - from: "schemas/v1/files-read-request.schema.json"
      to: "schemas/v1/files-read-result.schema.json"
      via: "file_id plus expected_digest freshness handshake and query-bound continuation"
      pattern: "expected_digest"
---

<objective>
Freeze the complete files-first contract, canonical generation vocabulary, stable logical identity rules, and cross-language drift boundary before any administrative or MCP implementation.

Purpose: Give Python administration, patched native file-base, verification, and clients one strict interpretation of identity, freshness, pagination, extraction, and fail-closed outcomes.
Output: Versioned JSON Schemas, strict Python models, a reproducibly generated C header, a security-oriented fixture corpus, and collectable RED tests for every later Phase 3 slice.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-secure-files-first-data-plane/03-CONTEXT.md
@.planning/phases/03-secure-files-first-data-plane/03-RESEARCH.md
@.planning/phases/03-secure-files-first-data-plane/03-VALIDATION.md
@.planning/phases/02-durable-provenance-runtime/02-05-SUMMARY.md

<interfaces>
The process owns one configured root identified by a stable `root_id`. Logical `file_id` is independent of a relative path and never merges duplicate-content files. A generation is immutable after close and names canonical inventory, extraction registrations, index artifacts, their digests, tool/extractor versions, and a completeness verdict. Live operations return current digest/freshness; generation operations return selected generation identity and never expose stale body-derived fields. Cursors bind schema version, operation, normalized query/range, selected generation when relevant, last sort key, and hard expiry/size ceilings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the complete Phase 3 Wave 0 RED corpus</name>
  <files>tests/schema/test_files_contracts.py, tests/unit/test_file_models.py, tests/unit/test_file_generations.py, tests/integration/test_files_admin.py, tests/integration/test_file_generations.py, tests/integration/test_files_mcp.py, tests/integration/test_files_formats.py, tests/integration/test_files_security.py, tests/fixtures/files-first/**</files>
  <read_first>AGENTS.md; 03-CONTEXT.md D-01 through D-16; 03-RESEARCH.md Recommended Architecture, Contract Shape, Identity and Generation Semantics, Security Controls, and Validation Architecture; 03-VALIDATION.md; tests/schema/test_cross_language.py; tests/integration/test_mcp_confinement.py; tests/fixtures/private-canaries/canaries.json; schemas/v1/mcp-read-request.schema.json; schemas/v1/mcp-read-result.schema.json</read_first>
  <action>Create every missing test module named by 03-VALIDATION.md and a deterministic `tests/fixtures/files-first/` tree containing UTF-8 Chinese and mixed CJK text, Markdown headings, LaTeX sections, BibTeX entries, source code, plain text, duplicate-content files at distinct paths, rename candidates, ignored content, malformed UTF-8, private canaries, and a small PDF placeholder paired only with registered extracted UTF-8 text/provenance. Put reusable subprocess/root-tree snapshot/barrier helpers in the tests package rather than production code. In this task, fully implement only contract/model/generation assertions; later integration modules may be marked with an explicit module-level skip naming their owning plan so the complete suite collects without false green behavior. Require strict additional-property rejection, stable canonical bytes, identity ambiguity behavior, cursor tamper/operation/query/generation binding, no-body stale results, ceilings, extraction states, promotion validation, and integrity-versus-degradation classification. Demonstrate RED by collection plus focused failing behavior assertions; syntax/import failures do not count.</action>
  <acceptance_criteria>All Phase 3 test paths in 03-VALIDATION.md exist and collect; the three Plan 01 modules fail for missing production contracts only; fixtures contain no real private data and include expected digests/canary metadata; later-plan skips identify 03-02 through 03-05 explicitly.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 uv run --frozen pytest --collect-only -q tests/schema/test_files_contracts.py tests/unit/test_file_models.py tests/unit/test_file_generations.py tests/integration/test_files_admin.py tests/integration/test_file_generations.py tests/integration/test_files_mcp.py tests/integration/test_files_formats.py tests/integration/test_files_security.py &amp;&amp; ! UV_OFFLINE=1 uv run --frozen pytest -q tests/schema/test_files_contracts.py tests/unit/test_file_models.py tests/unit/test_file_generations.py</automated></verify>
  <done>The entire phase has a collectable security corpus and the contract foundation is observed RED before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict Python and JSON Schema file contracts</name>
  <files>src/arw/file_models.py, src/arw/file_contracts.py, src/arw/schema_registry.py, schemas/v1/file-root.schema.json, schemas/v1/file-identity-manifest.schema.json, schemas/v1/file-generation-manifest.schema.json, schemas/v1/extraction-registration.schema.json, schemas/v1/file-admin-receipt.schema.json, schemas/v1/files-list-request.schema.json, schemas/v1/files-list-result.schema.json, schemas/v1/files-read-request.schema.json, schemas/v1/files-read-result.schema.json, schemas/v1/files-search-request.schema.json, schemas/v1/files-search-result.schema.json, schemas/v1/files-outline-request.schema.json, schemas/v1/files-outline-result.schema.json, schemas/v1/files-context-request.schema.json, schemas/v1/files-context-result.schema.json, tests/schema/test_files_contracts.py, tests/unit/test_file_models.py, tests/unit/test_file_generations.py</files>
  <read_first>Task 1 RED tests; 03-CONTEXT.md D-03 through D-16; 03-RESEARCH.md generation layout, API contract, freshness, pagination, identity, limits, and extraction recommendations; src/arw/models.py strict-model conventions; src/arw/canonical.py; src/arw/schema_registry.py; schemas/v1/status.schema.json; tests/schema/test_schema_drift.py</read_first>
  <action>Implement frozen strict models and matching Draft 2020-12 schemas for one named root, identity inventory, immutable generation manifest, extraction registration, admin receipt, and all five MCP request/result families. Normalize relative paths without resolving outside root; keep digest and file_id distinct; encode unambiguous rename evidence but never infer identity from digest alone when multiple candidates exist. Define explicit `live`, `current`, `stale_metadata`, `stale_conflict`, `no_structure`, `degraded`, and blocking integrity states. Reads choose exactly one byte or line range with explicit encoding/truncation/next cursor. Search chooses exactly one `exact` or `full_text` mode, exposes ranking-version and freshness fields, and never accepts raw FTS syntax. Encode server ceilings as versioned constants; clients may lower but never raise them. Implement signed-or-MACed canonical opaque cursor envelopes using an injected secret and clock, with operation/query/range/generation binding and stable error codes. Register every schema without weakening existing Phase 1/2 names.</action>
  <acceptance_criteria>All new instances reject unknown fields and invalid state combinations; file_id rename/duplicate/ambiguity rules are deterministic; stale-conflict read results cannot contain content; exact/full_text are disjoint; cursor changes, cross-operation reuse, expiry, or generation changes reject; canonical manifest/receipt bytes are stable; Plan 01 model/schema tests pass.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 uv run --frozen pytest -q tests/schema/test_files_contracts.py tests/unit/test_file_models.py tests/unit/test_file_generations.py tests/schema/test_schema_drift.py</automated></verify>
  <done>Python and JSON Schema share one complete, strict files-first contract with executable identity, freshness, and generation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate and verify the native contract boundary</name>
  <files>scripts/generate-file-contract-header, generated/file-contracts.h, tests/schema/test_files_contracts.py, tests/unit/test_file_models.py</files>
  <read_first>Task 2 checked schemas and constants; 03-CONTEXT.md D-09 through D-12; 03-RESEARCH.md Cross-language schema drift and build recommendations; scripts/materialize-sources; scripts/build-file-base; tests/schema/test_cross_language.py; vendor/patches/file-base/0002-phase1-confined-read.patch</read_first>
  <action>Add an offline deterministic generator that reads the checked files request/result schemas and versioned constants, emits a compact C header with tool names, modes, result/status enums, hard ceilings, ranking version, schema digests, and a combined contract digest, and writes only when content changes. Do not hand-copy a second contract into C. Commit the generated header as a reviewed build input and add `--check` drift behavior that compares exact bytes without mutation. Extend tests to regenerate in a temporary path, compare digests/constants against Python/JSON Schema, and reject an intentionally edited header or schema. The generator must not read the network, locale-dependent data, wall-clock time, or absolute repository paths.</action>
  <acceptance_criteria>Two generations are byte-identical; `--check` is read-only and fails on drift; all five exact tool names and limits match Python/schema values; combined digest changes when a checked schema changes; existing source materialization remains untouched.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 ./scripts/generate-file-contract-header --check &amp;&amp; UV_OFFLINE=1 uv run --frozen pytest -q tests/schema/test_files_contracts.py tests/unit/test_file_models.py</automated></verify>
  <done>The patched native implementation can consume one generated, independently checked contract rather than a divergent handwritten copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted request to typed contract | Malformed, oversized, ambiguous, or cross-operation values must fail before filesystem or SQLite access. |
| mutable root to logical identity | Paths and duplicate bytes cannot silently rewrite file identity. |
| checked schemas to patched C | Generated constants and schema identities cannot drift from the Python control plane. |

## Threat Register

| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T03-03 | mitigate | Canonical closed-generation and receipt schemas forbid selecting partial state. |
| T03-04 | mitigate | Live digest and expected-digest handshake has a no-body conflict type. |
| T03-05 | mitigate | Strict request unions, opaque bound cursors, and hard contract ceilings reject early. |
| T03-06 | mitigate | Extraction registration and eligibility are explicit; fixture canaries test exclusion. |
| T03-07 | mitigate | Deterministic generated C header and independent schema/model drift tests. |
</threat_model>

<verification>
Run all three Plan 01 modules, schema drift tests, generator `--check`, and `git diff --check`. Confirm later integration modules collect and are explicitly assigned, not silently passing placeholders.
</verification>

<success_criteria>
FILE-01, FILE-02, FILE-06, and FILE-07 have one executable cross-language contract foundation, every later Phase 3 behavior has a Wave 0 test location, and no native implementation work can proceed with a stale contract header.
</success_criteria>

<output>Create `.planning/phases/03-secure-files-first-data-plane/03-01-SUMMARY.md` when done.</output>
