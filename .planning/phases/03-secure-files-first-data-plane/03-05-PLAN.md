---
phase: 03-secure-files-first-data-plane
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - vendor/patches/file-base/0006-phase3-security-barriers.patch
  - vendor/source-manifest.json
  - src/arw/evidence.py
  - src/arw/build_identity.py
  - src/arw/cli.py
  - scripts/stage-plugin
  - scripts/verify-phase-3
  - scripts/smoke-staged-plugin
  - .codex-plugin/plugin.json
  - .mcp.json
  - skills/academic-research-workbench/SKILL.md
  - hooks/hooks.json
  - schemas/v1/build-identity.schema.json
  - schemas/v1/version-report.schema.json
  - tests/integration/test_files_security.py
  - tests/integration/test_version_report.py
  - tests/integration/test_digest_drift.py
  - tests/staged/test_mcp_launcher.py
  - tests/staged/test_manifest_install.py
  - tests/staged/test_private_exclusions.py
  - tests/staged/test_supply_chain_inventory.py
  - docs/runtime/files-first-data-plane.md
autonomous: true
requirements: [FILE-01, FILE-02, FILE-03, FILE-04, FILE-06, FILE-07, FILE-08, VER-03]
must_haves:
  truths:
    - "VER-03: Traversal, link escape, deterministic replacement races, sensitive paths, malformed input, and every output budget fail closed before content leakage."
    - "D-01 through D-16 each have an independent machine-readable verdict bound to retained raw evidence from exact staged bytes."
    - "The staged plugin carries the exact five-tool native profile, generated contract identity, ordered source/patch identity, and no private root, cache, index, extraction, or test canary."
    - "Queries leave research root, control state, selected generation, SQLite files, pointer, and cache byte-identical; administration remains parent-only."
    - "Deleting projections and rebuilding from registered source truth reproduces normalized query behavior."
  artifacts:
    - path: "tests/integration/test_files_security.py"
      provides: "Deterministic VER-03 adversarial and no-leakage matrix"
    - path: "scripts/verify-phase-3"
      provides: "Owned-root clean staged qualification and raw-evidence-bound top verdict"
    - path: "docs/runtime/files-first-data-plane.md"
      provides: "Operator contract for root registration, extraction, sync, one-root launch, freshness, and recovery"
    - path: "vendor/source-manifest.json"
      provides: "Final ordered patches, generated contract, native binary, and patched-tree identity"
  key_links:
    - from: "scripts/verify-phase-3"
      to: "build/evidence/phase-03"
      via: "retained raw commands, JSON-RPC transcripts, tree manifests, canary scans, and verdict digests"
      pattern: "D-16"
    - from: "src/arw/build_identity.py"
      to: "vendor/source-manifest.json"
      via: "version report binds source archive, ordered patches, generated contract, tree, and binary"
      pattern: "file_contract_sha256"
    - from: "scripts/stage-plugin"
      to: "scripts/file-base-mcp"
      via: "exact staged launcher/profile/binary and exclusion inventory"
      pattern: "file-base-mcp"
---

<objective>
Close Phase 3 with deterministic adversarial security evidence, exact staged packaging, source/binary/contract identity, and a top verdict proving every requirement and locked design decision.

Purpose: Demonstrate that the useful files-first data plane remains confined, read-only, bounded, reproducible, and private-data-free under malformed inputs, race schedules, packaging, and rebuild.
Output: Final security patch/barriers, VER-03 suite, staged verifier/evidence bundle, package/version updates, operator documentation, and full-regression qualification.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-secure-files-first-data-plane/03-CONTEXT.md
@.planning/phases/03-secure-files-first-data-plane/03-RESEARCH.md
@.planning/phases/03-secure-files-first-data-plane/03-VALIDATION.md
@.planning/phases/03-secure-files-first-data-plane/03-04-SUMMARY.md
@.planning/phases/02-durable-provenance-runtime/02-05-SUMMARY.md

<interfaces>
The Phase 3 verifier creates all roots/control/generations/evidence under an owned temporary or explicit evidence root, stages the installable plugin from tracked inputs, invokes only staged launchers/binaries, and records exact command, environment allowlist, versions, input/output digests, JSON-RPC transcripts, deterministic barrier schedule, before/after tree manifests, canary scans, normalized rebuild comparison, and per-requirement/per-decision verdicts. It must distinguish technical PASS from the repository's separate SUP-04 legal evidence gate and must never turn an unavailable release authorization into a false technical failure or vice versa.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete deterministic VER-03 adversarial and race qualification</name>
  <files>vendor/patches/file-base/0006-phase3-security-barriers.patch, vendor/source-manifest.json, tests/integration/test_files_security.py, tests/integration/test_files_mcp.py, tests/integration/test_files_formats.py</files>
  <read_first>03-VALIDATION.md Threat Register and Full-Phase Sign-Off Gates; 03-CONTEXT.md D-01 through D-16; 03-RESEARCH.md adversarial matrix, race barriers, resource budgets, sensitive-path/no-write testing; 03-04-SUMMARY.md; tests/integration/test_mcp_confinement.py; tests/fixtures/private-canaries/canaries.json; native patches 0002-0005</read_first>
  <action>Remove the Plan 05 skip and implement the complete VER-03 matrix against direct native JSON-RPC and administration. Cover absolute/relative traversal, separators/encoding variants, symlink chains/loops, hardlink or junction-like platform cases where available, root replacement, path-to-inode swaps at each deterministic barrier, selected-pointer/generation replacement, sensitive names/locations, malformed JSON/schema/UTF-8/cursors/queries, huge numeric/string/depth values, page/read/snippet/outline/context ceilings, SQLite progress timeout, and client attempts to raise limits. Snapshot root/control/generation/cache/temp descriptors and scan unique canaries after each call. Add ordered patch `0006` only for auditable test barriers/failpoints and final hardening needed by failures; barriers must require a test-build capability unavailable in release mode and never alter result semantics. Classify skips only for genuinely absent platform primitives, with equivalent Linux coverage mandatory.</action>
  <acceptance_criteria>Every VER-03 class has a deterministic assertion; all rejected/raced/timeout cases return zero protected body bytes and no partial page; query tree manifests are exact; canary scan is zero; no release binary exposes test controls; source verification and all Phase 3 integration tests pass.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 ./scripts/verify-sources &amp;&amp; UV_OFFLINE=1 uv run --frozen pytest -q tests/integration/test_files_security.py tests/integration/test_files_mcp.py tests/integration/test_files_formats.py tests/integration/test_mcp_confinement.py</automated></verify>
  <done>VER-03 is executable and fail-closed across confinement, races, sensitive data, malformed input, and budgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage exact files-first artifacts and bind source, contract, and binary identity</name>
  <files>src/arw/build_identity.py, src/arw/cli.py, scripts/stage-plugin, scripts/smoke-staged-plugin, .codex-plugin/plugin.json, .mcp.json, skills/academic-research-workbench/SKILL.md, hooks/hooks.json, schemas/v1/build-identity.schema.json, schemas/v1/version-report.schema.json, tests/integration/test_version_report.py, tests/integration/test_digest_drift.py, tests/staged/test_mcp_launcher.py, tests/staged/test_manifest_install.py, tests/staged/test_private_exclusions.py, tests/staged/test_supply_chain_inventory.py, docs/runtime/files-first-data-plane.md</files>
  <read_first>Task 1 final source identity; Phase 1/2 staging and build identity implementation; scripts/stage-plugin; scripts/smoke-staged-plugin; plugin manifest/launchers; vendor/source-manifest.json; generated/file-contracts.h; tests/staged/test_supply_chain_inventory.py; tests/staged/test_private_exclusions.py; 03-RESEARCH.md packaging/private-data and operator workflow recommendations</read_first>
  <action>Extend version/build identity with ordered file-base patches 0001-0006, patched source tree digest, generated file-contract digest/schema digests, compile profile, native binary digest, tokenizer/ranking/outline versions, and parent Python package identity. Stage only required schemas, generated/header-derived metadata, release native binary, safe launchers, Python runtime, docs, notices, and plugin manifest. Ensure launchers resolve state supplied by the parent at runtime rather than packaging any root/control/cache/generation. Expand exclusion/SBOM/inventory tests to reject database/index/extracted text/receipt/evidence/temp/private-canary/test-barrier artifacts and stale duplicate binaries. Add staged smoke for parent registration/sync followed by exact five-tool launch from paths containing spaces. Document root ownership, extraction registration, explicit sync, stale outcomes, one-process-per-root, rebuild/repair, hard ceilings, and the absence of raw PDF parsing without exposing internal-only instructions.</action>
  <acceptance_criteria>`arw version --json` and staged version validate and match exact tracked/source/binary/contract identities; staged launcher advertises exactly five tools and works from relocated paths; package inventory has no private/query-state/test controls; SBOM/notices include native dependencies and patches; existing Phase 1/2 staged tests remain green.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 uv run --frozen pytest -q tests/integration/test_version_report.py tests/integration/test_digest_drift.py tests/staged/test_mcp_launcher.py tests/staged/test_manifest_install.py tests/staged/test_private_exclusions.py tests/staged/test_supply_chain_inventory.py &amp;&amp; UV_OFFLINE=1 ./scripts/smoke-staged-plugin</automated></verify>
  <done>The installable plugin contains the exact qualified files-first implementation and no private or mutable runtime state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the Phase 3 verifier and close all technical gates</name>
  <files>src/arw/evidence.py, scripts/verify-phase-3, tests/integration/test_files_security.py, docs/runtime/files-first-data-plane.md, .planning/phases/03-secure-files-first-data-plane/03-VALIDATION.md</files>
  <read_first>03-VALIDATION.md complete map; 03-CONTEXT.md D-01 through D-16; ROADMAP Phase 3 success criteria; scripts/verify-phase-1; scripts/verify-phase-2; src/arw/evidence.py; tests/integration/test_phase2_verifier_safety.py; Plans 03-01 through 03-04 summaries; Task 1 and Task 2 evidence paths</read_first>
  <action>Implement `scripts/verify-phase-3 --clean --evidence-root` using the existing owned-root evidence framework. It must start from clean source materialization and staging, create a synthetic multilingual root/registrations, execute admin update/promotion/failure/rebuild matrices, run direct staged five-tool JSON-RPC including every deterministic race and budget class, compare before/after query trees, scan stale/private canaries, delete projections and rebuild, and compare normalized results. Retain raw stdout/stderr/transcripts/manifests/digests/tool lists/barrier schedules/version reports and generate independently checked verdict JSON for FILE-01/02/03/04/06/07/08, VER-03, D-01 through D-16, and all four ROADMAP criteria. Refuse symlinked/non-owned evidence roots, stale artifacts, missing raw evidence, digest mismatch, or partial execution. Run frozen full pytest plus source and prior-phase verifiers; report technical PASS separately from SUP-04 release BLOCKED. Reconcile 03-VALIDATION.md task statuses/frontmatter only after exact commands pass.</action>
  <acceptance_criteria>A clean verifier run exits zero only when every Phase 3 technical verdict is true and raw-evidence-bound; tampering/deleting evidence, substituting a binary, leaking a canary, or forcing one skipped required check makes it nonzero; full offline pytest, source verifier, Phase 1, Phase 2, and Phase 3 verifier pass; SUP-04 remains accurately separate.</acceptance_criteria>
  <verify><automated>UV_OFFLINE=1 ./scripts/verify-phase-3 --clean --evidence-root build/evidence/phase-03 &amp;&amp; UV_OFFLINE=1 uv run --frozen pytest -q &amp;&amp; UV_OFFLINE=1 ./scripts/verify-sources &amp;&amp; UV_OFFLINE=1 ./scripts/verify-phase-1 --clean --evidence-root build/evidence/phase-01-regression &amp;&amp; UV_OFFLINE=1 ./scripts/verify-phase-2 --clean --evidence-root build/evidence/phase-02-regression</automated></verify>
  <done>Phase 3 has a reproducible technical PASS backed by exact staged bytes and retained machine-verifiable evidence.</done>
</task>

</tasks>

<threat_model>
| Threat Ref | Disposition | Mitigation Plan |
|---|---|---|
| T03-01 | verify/mitigate | Exhaustive descriptor/link/traversal/replacement matrix and protected canaries. |
| T03-02 | verify/mitigate | Exact five-tool staged discovery, no-write tree manifests, admin negative tests. |
| T03-03 | verify/mitigate | Barrier-scheduled publication failures and old-pointer/result equality. |
| T03-04 | verify/mitigate | Replacement barriers plus zero stale body/snippet/outline/context scans. |
| T03-05 | verify/mitigate | Schema fuzz cases, hard ceilings, progress timeout, no partial response. |
| T03-06 | verify/mitigate | Extraction matrix and package/evidence/private-canary exclusions. |
| T03-07 | verify/mitigate | Clean materialization, generated-contract drift, binary/version/stage digest binding. |
</threat_model>

<verification>
The task 3 automated command is the mandatory technical gate. Also run `git diff --check`, inspect the final requirement/decision verdict, and run independent code, security, and validation reviews before updating roadmap completion.
</verification>

<success_criteria>
Every Phase 3 requirement and D-01 through D-16 passes from exact staged artifacts; the four ROADMAP success criteria are machine-verifiable; query behavior is confined/read-only/bounded/stale-safe; projection rebuild is reproducible; the package is private-data-free; release authorization remains separately truthful.
</success_criteria>

<output>Create `.planning/phases/03-secure-files-first-data-plane/03-05-SUMMARY.md` when done.</output>
