---
quick_id: 260825-vd9
status: ready
mode: quick-full
must_haves:
  truths:
    - Bundled ARS is pinned to upstream academic-research-skills v3.21.1 at 127ff85e4bbfcdd10b95040537b6c6bd7ad17aeb through adapter 0.1.27.
    - All ARW-owned control-plane, hook, MCP, patch, Codex-overlay, and project files remain semantically unchanged except authoritative version/provenance documentation required by the sync.
    - experiment-agent remains at e291e7dc7ca268b2de7e1a9cf23bc2eef5dc0651 and file-base remains at ee68144af5453addda995a27cce8142999f318fb.
    - Existing hooks remain observational and pass contract, parity, staged, and host-canary checks.
    - The full local verification matrix passes, or every environment-only skip/blocker is recorded with exact evidence.
  artifacts:
    - skills/academic-research-suite/ars/
    - skills/academic-research-suite/VERSION
    - skills/academic-research-suite/SKILL.md
    - skills/academic-research-suite/manifest.json
    - vendor/source-manifest.json
    - README.md
    - MODIFICATIONS.md
  key_links:
    - vendor/source-manifest.json pins the same ARS commit represented by skills/academic-research-suite/manifest.json.
    - skills/academic-research-suite/VERSION, SKILL.md metadata, manifest.json adapter_version, README.md, and MODIFICATIONS.md agree on adapter 0.1.27.
    - Stage integration-lock and host canary bind the refreshed adapter tree without changing file-base patch or binary digests.
---

# Quick Task 260825-vd9: Lossless bundled ARS v3.21.1 update

## Scope lock

- Treat `skills/academic-research-suite/ars/**` as the upstream refresh surface.
- Preserve `src/arw/**`, `hooks/**`, `scripts/**`, `tests/**`, `packaging/**`, `schemas/**`, `vendor/patches/**`, `vendor/mcp-manifest.json`, `skills/academic-research-suite/codex/**`, `skills/academic-research-suite/agents/**`, `skills/academic-research-suite/evals/**`, and `skills/academic-research-workbench/**` byte-for-byte unless a test proves a narrowly required compatibility fix.
- Do not upgrade experiment-agent or file-base.
- Keep the ARW core compatibility floor at Codex CLI 0.144.4. Treat the upstream subscription transport as an optional capability requiring Codex CLI >=0.147.0; do not silently raise the whole plugin floor.
- Before mutation, record the clean pre-update commit and authoritative pins under ignored `build/evidence/ars-sync-0.1.27/pre-update.json`; rollback is an explicit `git revert` of the eventual atomic update commit, never a destructive reset/force checkout.

## Task 1 — Refresh only the upstream-owned ARS surface

**files**
- `skills/academic-research-suite/ars/**`
- `vendor/source-manifest.json`
- `skills/academic-research-suite/{VERSION,SKILL.md,manifest.json}`
- `README.md`
- `MODIFICATIONS.md`

**action**
1. Record the clean base commit and old pins, then verify upstream tag/commit identities for `academic-research-skills` v3.21.1 (`127ff85e4bbfcdd10b95040537b6c6bd7ad17aeb`) and Codex adapter v0.1.27 (`7600799a41266379ed5e7716eef963a7161277c0`).
2. Materialize the target source in an isolated temporary directory and mechanically refresh only the upstream-owned ARS bundle.
3. Reconcile these seven recorded conflicts against the current tree and the v0.1.27 Codex mirror: `ars/commands/ars-revision-coach.md`, `ars/scripts/check_pipeline_boundary_semantics.py`, `ars/scripts/check_human_subjects_output_contract.py`, `ars/scripts/test_check_committee_correspondence.py`, `ars/scripts/test_resolve_human_subjects_authority.py`, `ars/scripts/check_human_subjects_reference_migration.py`, and `ars/scripts/test_check_human_subjects_reference_migration.py`. `MODIFICATIONS.md` names no additional path-level ARW overlay inside `ars/**`; any non-mirror delta must therefore be individually justified.
4. Update source tree hashes, commit pins, adapter version, README, and MODIFICATIONS. Use the v0.1.27 mirror manifest as the current provenance source (`ars_codex_base.commit=7600799a...`, `adapter_version=0.1.27`, `ars_commit=127ff85e...`) while retaining the old 0.1.24→0.1.26 provenance as history rather than silently discarding it; update the manual-conflict and inactive-script lists to the target mirror. Keep experiment-agent/file-base pins and file-base patch/binary hashes unchanged.

**verify**
- Manifest/schema checks and `./scripts/verify-sources --inputs-only` pass after materialization.
- `git diff --name-only` contains no forbidden ARW-owned path; `hooks/hooks.json`, `hooks/arw_hook.py`, `vendor/mcp-manifest.json`, all file-base patches/binaries, and experiment-agent remain byte-identical to the recorded base.
- Exact consistency checks cover: `skills/academic-research-suite/VERSION`; `SKILL.md` metadata version; `manifest.json` adapter version, ARS source commit, current provenance, runtime notes, manual conflicts, and inactive scripts; `vendor/source-manifest.json` ARS revision/version/git tree/tree hash; `README.md`; and `MODIFICATIONS.md`.
- Core floor remains 0.144.4, while any subscription-transport route is explicitly capability-gated at detected Codex CLI >=0.147.0.

**done**
- The vendored tree is a reviewable v3.21.1 refresh with all ARW overlays preserved and no unrelated project changes.

## Task 2 — Review provenance and regression boundary

**files**
- All Task 1 changes
- Existing manual-conflict list and integration manifests

**action**
1. Compare the final tree against both pre-update HEAD and upstream v0.1.27.
2. Classify every non-upstream delta as an intentional ARW overlay.
3. Confirm no secret, license, patch, binary, experiment-agent, or file-base drift.

**verify**
- Independent reviewer reports no high-severity preservation/provenance issue.
- License gate, source integrity tests, and adapter consistency checks pass.

**done**
- A reviewer can explain every changed file and confirm the ARW-owned boundary is intact.

## Task 3 — Run the full validation matrix and regenerate evidence

**files**
- Test/runtime outputs under ignored `build/**`, `.file-base/**`, and isolated temporary homes only

**action**
1. Run frozen dependency/source baseline and full Python unit/schema/property/integration/eval suites, followed by `scripts/verify-phase-1` through `scripts/verify-phase-7` in dependency order using fresh evidence roots.
2. Run bundled ARS self-tests, Hook contract/parity/Claude-wrapper tests, MCP/file-base security/native/upstream tests (including sanitizer profiles where supported), license/SBOM checks, and package-boundary builds.
3. Build a clean stage, run staged tests and every smoke mode (`--version`, `--mcp`, `--route`, `--install-cli`), create Codex and Claude marketplaces, install from the generated marketplace into an isolated fresh Codex home, and exercise the installed route/MCP.
4. Qualify a real authenticated Codex host with isolated homes, prepare the qualified stage, and verify the resulting integration lock/canary. Record exact pass/fail/skip counts and environment-only blockers; fix only regressions caused by this update.

**verify**
- All available non-host suites pass with zero unexpected skips.
- Existing Hook events (`SessionStart`, `SubagentStop`, `Stop`, plus the packaged Claude guard path) pass their current authoritative tests; `git diff` confirms the ARW hook implementation/configuration is unchanged and no new event is invented as part of an upstream refresh.
- Marketplace install, isolated installed route/MCP, host canary, and staged route pass on an authenticated Codex host; integration-lock binds adapter 0.1.27 and leaves experiment-agent/file-base hashes unchanged.
- `git status` after tests contains only intentional Task 1/2 source changes; generated evidence remains ignored. The recorded base commit and eventual atomic commit make `git revert <update-commit>` a verifiable rollback path.

**done**
- The update is technically qualified, auditable, and ready for atomic commit, with any pre-existing CC BY-NC release-policy blocker reported separately from technical test status.
