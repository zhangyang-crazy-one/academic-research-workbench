---
phase: quick-260826-9p7-research-integrity-bridge
quick_task: 260826-9p7
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/arw/integration_lock.py
  - src/arw/cli.py
  - src/arw/schema_registry.py
  - src/arw/research_integrity.py
  - schemas/v1/research-integrity-contracts.schema.json
  - scripts/stage-plugin
  - skills/academic-research-suite/codex/scripts/ars_codex_quality_gates.py
  - skills/academic-research-suite/ars/scripts/adapters/folder_scan.py
  - skills/academic-research-suite/ars/scripts/adapters/tests/test_folder_scan.py
  - skills/academic-research-suite/ars/scripts/adapters/examples/folder_scan/expected_passport.yaml
  - skills/academic-research-suite/ars/scripts/adapters/examples/folder_scan/expected_rejection_log.yaml
  - skills/academic-research-suite/ars/scripts/adapters/README.md
  - tests/unit/test_integration_lock.py
  - tests/unit/test_research_integrity.py
  - SBOM.cdx.json
  - MODIFICATIONS.md
must_haves:
  truths:
    - "Operator can distinguish missing inputs, malformed/non-canonical lock bytes, staged ARW/ARS/file-base drift, host drift, root-hook drift, hook-evidence drift, legal-state drift, and final exact-lock drift without route or hook observations gaining authority."
    - "An ARS literature-corpus entry can be converted deterministically into a digest-bound ARW research-source manifest and linked through evidence spans to claims using strict, installed JSON Schemas."
    - "Unicode/CJK filenames following the documented family/year conventions are accepted without losing the original author/title text, while generated citation keys remain deterministic, collision-safe, and valid under the ARS ASCII citation-key schema."
    - "Technical qualification and legal release qualification remain independent: experiments stay disabled and unresolved CC BY-NC/intended-use/accountable-approval blockers stay BLOCKED."
  artifacts:
    - path: "schemas/v1/research-integrity-contracts.schema.json"
      provides: "One discriminated schema bundle for integration diagnostics, research sources, evidence spans, and claim links"
    - path: "src/arw/research_integrity.py"
      provides: "Strict models, pure builders, canonical digest helpers, and whole-chain validator"
    - path: "skills/academic-research-suite/ars/scripts/adapters/folder_scan.py"
      provides: "Unicode-aware filename ingestion preserving CJK metadata"
  key_links:
    - from: "bin/arw"
      to: "src/arw/cli.py"
      via: "existing route forwarding with an additive --diagnostics JSON mode after locked-wheel bootstrap"
      pattern: "route.*diagnostics"
    - from: "src/arw/cli.py"
      to: "src/arw/integration_lock.py"
      via: "shared input discovery plus read-only diagnose_integration_lock call"
      pattern: "diagnose_integration_lock"
    - from: "src/arw/research_integrity.py"
      to: "src/arw/canonical.py"
      via: "builders and chain validator recompute canonical SHA-256 over every supplied upstream document"
      pattern: "canonical_json_bytes|sha256_hex|validate_research_integrity_chain"
    - from: "src/arw/schema_registry.py"
      to: "scripts/stage-plugin"
      via: "registered schemas regenerated into share/arw/schemas and included in the exact staged allowlist/build identity"
      pattern: "RESEARCH_INTEGRITY_SCHEMA_NAME|research-integrity-contracts"
    - from: "skills/academic-research-suite/ars/scripts/adapters/folder_scan.py"
      to: "skills/academic-research-suite/ars/shared/contracts/passport/literature_corpus_entry.schema.json"
      via: "Unicode-preserving display fields plus folder-local NFKC/hash-backed ASCII citation-key derivation"
      pattern: "citation_key|NFKC"
---

<objective>
Implement the research-integrity bridge and close the diagnosed installation/adapter defects in one reviewable feature PR.

Purpose: Make local ARW failures explainable at the exact integration layer, make ARS bibliographic material usable as digest-bound ARW evidence without widening authority, and remove the artificial ASCII-only ingestion failure for CJK research libraries.

Output: Layered diagnostic mode and root-hook supply-chain gate; an installed discriminated schema bundle plus pure builders/chain validator; CJK-capable folder ingestion; focused tests, adapter documentation, exact stage inventory, and SBOM updates.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@src/arw/integration_lock.py
@src/arw/cli.py
@src/arw/contracts.py
@src/arw/models.py
@src/arw/schema_registry.py
@src/arw/canonical.py
@src/arw/manifests.py
@src/arw/runtime.py
@scripts/stage-plugin
@hooks/hooks.json
@skills/academic-research-suite/codex/hooks/hooks.json
@skills/academic-research-suite/codex/scripts/ars_codex_quality_gates.py
@skills/academic-research-suite/ars/shared/contracts/passport/literature_corpus_entry.schema.json
@skills/academic-research-suite/ars/scripts/adapters/folder_scan.py
@supply-chain/license-verdict.json

<interfaces>
Existing authority and integration contracts to preserve:

- `load_and_verify_integration_lock(path, *, stage_root, codex_launcher, codex_native_binary, host_canary_evidence) -> IntegrationVerification` is the formal PASS boundary. Diagnostics may explain its inputs and validators but must not replace, relax, or partially satisfy it.
- `installed_route(verification=None, blocked_reason=...) -> RouteResult` remains fail-closed; only a complete `IntegrationVerification` selects `inline-role-prompts`. `release_qualification` remains `BLOCKED`, `experiment_execution` remains `disabled`, and `paper_ast_export` remains `deferred-v2`.
- `ArtifactManifest` and `ArtifactAcceptanceRequest` are the parent-owned admission path. `MaterialPassport` carries only `accepted_artifact_manifest_sha256`; bridge documents do not add mutable bibliography/evidence fields to the Passport.
- Root `hooks/hooks.json` is the installed observational Codex hook definition. The nested ARS Codex hook pack is separately disabled by default and is not a substitute supply-chain witness for the root hook.
- ARS adapter output is a user-owned `passport.yaml` with `literature_corpus[]` plus `rejection_log.yaml`; the bridge accepts an already-parsed entry and performs no network calls, YAML loading, source-file reads, ledger writes, retries, or evidence admission.
</interfaces>
</context>

<environment_preflight>
Repository reality observed while planning:

- `/home/zhangyangrui/.local/bin/uv` exists and reports exactly `uv 0.11.28`.
- `.python-version` pins `3.14.6`.
- `.venv/bin/python` and `.venv/bin/pytest` are currently absent in this fresh worktree.
- `scripts/stage-plugin` already pins `PYTHON="$PROJECT_ROOT/.venv/bin/python"`; it must not fall back to a system interpreter.

Before writing any RED test, execute this frozen environment preflight from the repository root:

<automated>`test "$(cat .python-version)" = "3.14.6" &amp;&amp; test "$(/home/zhangyangrui/.local/bin/uv --version)" = "uv 0.11.28 (x86_64-unknown-linux-gnu)" &amp;&amp; ARW_Q9P7_UV_CACHE=/tmp/arw-q9p7-uv-cache &amp;&amp; mkdir -p "$ARW_Q9P7_UV_CACHE" &amp;&amp; test -x .venv/bin/python &amp;&amp; UV_CACHE_DIR="$ARW_Q9P7_UV_CACHE" UV_OFFLINE=1 /home/zhangyangrui/.local/bin/uv pip install --python .venv/bin/python --no-index --find-links vendor/python/wheelhouse 'pytest==9.1.1' 'PyYAML==6.0.3' 'hatchling==1.31.0' 'jsonschema==4.26.0' 'portalocker==3.2.0' 'pydantic==2.13.4' &amp;&amp; .venv/bin/python -c 'import sys; assert sys.version_info[:3] == (3, 14, 6)' &amp;&amp; .venv/bin/python -c 'import jsonschema, pydantic, pytest, yaml' &amp;&amp; PYTHONPATH=src PYTHONNOUSERSITE=1 .venv/bin/python -m pytest --collect-only -q tests/unit/test_integration_lock.py tests/staged/test_supply_chain_inventory.py skills/academic-research-suite/codex/tests/test_full_runtime_adapter.py skills/academic-research-suite/ars/scripts/adapters/tests/test_folder_scan.py &amp;&amp; grep -F 'PYTHON="$PROJECT_ROOT/.venv/bin/python"' scripts/stage-plugin`</automated>

Any nonzero preflight result is an environment BLOCKER. It is not TDD RED, must not be repaired by an unlocked install or system Python, and must be recorded in the SUMMARY before execution stops.
</environment_preflight>

<tdd_evidence_contract>
For every task, first run its existing baseline selection GREEN with `PYTHONPATH=src PYTHONNOUSERSITE=1 .venv/bin/python -m pytest`. After adding only the new tests, run `--collect-only` and require exit 0, then run the named RED selection and require exit 1 from the planned assertion. Exit 2/3/4/5, `SyntaxError`, `ImportError`, `ModuleNotFoundError`, fixture setup failure, collection failure, dependency failure, or interpreter/bootstrap failure is invalid evidence and must be fixed before RED is claimed.

The task SUMMARY must record for each RED cycle: the exact `.venv/bin/python -m pytest` command, named test/node ID, expected assertion and why current behavior violates it, actual exit code, and a short output excerpt proving assertion failure rather than environment/collection failure. It must also record the corresponding GREEN command and exit 0. Tests for not-yet-existing public functions must import their module and first assert `hasattr(...)` so absence produces an assertion failure, not a collection error.
</tdd_evidence_contract>

<scope_exception>
The user explicitly requires one feat PR that implements the research-integrity plan and the diagnosed lock/hook/CJK defects together. The plan is reduced from 30 to 16 modified files by using one discriminated JSON-Schema bundle, reusing existing tests where no edit is required, placing citekey hardening inside `folder_scan.py`, and retaining only the adapter README, required golden fixtures, and mandatory downstream modification disclosure. Fewer files would either leave false user-facing adapter documentation, omit the CC BY-NC overlay notice, remove the two existing golden contracts, skip installed schema/stage wiring, or omit negative tests. This is a known quick-task scope exception requiring the orchestrator to force continuation after plan validation; it is not hidden as ordinary quick scope.

Execution is strictly serial and each task must finish GREEN as its own atomic commit: Task 1 -> Task 2 -> Task 3. Shared `schema_registry.py`, `research-integrity-contracts.schema.json`, `stage-plugin`, and `SBOM.cdx.json` are cohesive feature-PR integration surfaces; Task 1 creates the diagnostic portion, Task 2 extends the same discriminated contract with source/span/link definitions, and Task 3 refreshes the final ARS-tree digests after its vendored adapter change. Never stage files from a later task in an earlier task commit.
</scope_exception>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact layered integration diagnostics and root-hook supply-chain gating</name>
  <depends_on>Environment preflight only; this is atomic commit 1.</depends_on>
  <files>src/arw/integration_lock.py, src/arw/cli.py, src/arw/schema_registry.py, schemas/v1/research-integrity-contracts.schema.json, scripts/stage-plugin, skills/academic-research-suite/codex/scripts/ars_codex_quality_gates.py, tests/unit/test_integration_lock.py, SBOM.cdx.json</files>
  <behavior>
    - A complete exact fixture returns an `arw.integration-diagnostic.v1` report whose ordered layers all PASS and whose final lock SHA-256 matches `IntegrationVerification`.
    - Missing/incomplete inputs, non-canonical lock JSON, mutated root `hooks/hooks.json`, mutated `hooks/arw_hook.py`, ARS tree drift, file-base drift, host tuple drift, hook-canary drift, and legal-policy drift each BLOCK at their named layer; downstream layers are `NOT_EVALUATED`, not falsely PASS.
    - A blocked diagnostic emits only closed reason codes, bounded safe detail, and expected/observed digests where available; it never emits credentials, raw canary streams, private content, or unredacted absolute paths.
    - `arw route --diagnostics --json` always emits schema-valid canonical JSON; it exits 0 only for a complete technical PASS and 65 for BLOCKED, while plain `arw route --json` retains its existing fail-closed response contract and exact keys.
    - The ARS Codex quality-gate suite contains a `root-hook-supply-chain` gate that validates both root hook files as unique SBOM components with exact SHA-256 values and verifies the root command pack remains observational; the separately disabled nested hook pack cannot satisfy this gate.
  </behavior>
  <action>Confirm the existing Task 1 baseline is GREEN, then write named RED tests in `tests/unit/test_integration_lock.py` using module-level `hasattr` guards so collection succeeds. The required RED nodes are `test_route_diagnostics_reports_noncanonical_lock_at_lock_document` and `test_root_hook_supply_chain_gate_rejects_digest_substitution`; each must exit 1 on its specific assertion, not on import/parser/setup failure, and be recorded per the TDD evidence contract. Introduce frozen strict diagnostic models and one read-only `diagnose_integration_lock` path that evaluates the same existing validators in fixed order: `inputs`, `lock_document`, `staged_arw`, `ars_bundle`, `file_base`, `codex_host`, `hook_definition`, `hook_execution_evidence`, `legal_state`, `exact_lock`. Reuse/extract route input discovery so defaults cannot drift. Keep final `load_and_verify_integration_lock` as the sole technical PASS boundary; partial layers never construct `IntegrationVerification`. Add `--diagnostics` to the existing Python `route` parser so unchanged `bin/arw` forwarding suffices. Create the diagnostic branch of the single discriminated `research-integrity-contracts.schema.json`, register it, and stage it; do not add a per-schema CycloneDX component because staged schema bytes are already build-identity covered. Error serialization uses closed reason codes and bounded redacted detail. Add a distinct `root-hook-supply-chain` gate that hashes root `hooks/hooks.json` and `hooks/arw_hook.py`, requires unique exact SBOM components, validates observational command shape, and cannot be satisfied by the nested disabled hook pack. Because the gate script is inside `skills/academic-research-suite`, recompute `artifact:skills/academic-research-suite` in `SBOM.cdx.json` in this same task, run supply-chain inventory before commit, and leave the `.../ars` subtree digest unchanged. Do not edit an installation cache, publish a lock, infer permission, or change authority. When GREEN, create atomic commit 1 containing only this task's files.</action>
  <verify>
    <automated>UV_OFFLINE=1 PYTHONPATH=src PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/unit/test_integration_lock.py skills/academic-research-suite/codex/tests/test_full_runtime_adapter.py tests/staged/test_supply_chain_inventory.py</automated>
  </verify>
  <done>Each supported integration failure is assigned to one stable diagnostic layer with schema-valid output; complete bytes still require the original exact verifier; the root hook definition and immediate ARS-suite SBOM digest are independently gated; focused tests and unchanged supply-chain inventory exit 0; atomic commit 1 contains no Task 2/3 files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the ARS-to-ARW research-integrity contract bridge</name>
  <depends_on>Task 1 GREEN and atomic commit 1; this is atomic commit 2.</depends_on>
  <files>src/arw/research_integrity.py, src/arw/schema_registry.py, schemas/v1/research-integrity-contracts.schema.json, scripts/stage-plugin, tests/unit/test_research_integrity.py</files>
  <behavior>
    - `research_source_from_ars_entry` accepts a valid already-parsed ARS literature entry plus caller-supplied source digest/import identity, preserves Unicode title/author metadata, and deterministically binds the complete canonical ARS entry as `bibliographic_sha256`.
    - A source manifest requires `source_id`, ASCII-schema-valid `citation_key`, bibliographic fields, `source_pointer`, `source_sha256`, `bibliographic_sha256`, adapter identity when present, `imported_at`, and `imported_by`; missing source bytes/digest or malformed bibliographic identity fails closed.
    - An evidence span binds one research-source-manifest digest, the same source-content digest, one extraction-registration digest, a typed bounded locator, and extracted-text digest; it carries no quoted/full source text.
    - A claim-evidence link binds one claim ID/digest to a non-empty canonical unique tuple of evidence-span digests and one relation from `supports`, `contradicts`, or `contextualizes`; duplicates, non-canonical order, and unknown fields are rejected.
    - Pure `build_research_source_manifest`, `build_evidence_span`, and `build_claim_evidence_link` functions derive downstream digest fields from the exact supplied upstream documents instead of accepting caller-asserted upstream hashes; `source_sha256` is propagated unchanged from source manifest through every evidence span.
    - `validate_research_integrity_chain` recomputes canonical bytes/digests at every hop and rejects missing upstream documents, same-ID document replacement, post-build mutation, mismatched `source_sha256`, substituted span bytes, broken claim-link references, or non-canonical digest ordering.
    - Model-generated definitions equal the source/span/link branches of the single checked schema bundle byte-for-semantics, registry validation accepts representative documents and rejects mutations, and stage output contains that bundle under `share/arw/schemas/` in build identity.
  </behavior>
  <action>Confirm the post-Task-1 baseline is GREEN. First create an importable, behavior-free `src/arw/research_integrity.py` skeleton. Then add `tests/unit/test_research_integrity.py` with `hasattr` assertions inside concrete test functions, require collect-only exit 0, and capture assertion-only RED exit 1 for `test_chain_validator_rejects_upstream_document_substitution` and `test_evidence_builder_recomputes_and_propagates_source_sha256`; the skeleton itself is part of the implementation task and the RED commit is not published separately. Implement frozen Pydantic models following `StrictModel`, `StableRuntimeId`, `ActorId`, `Sha256`, `UtcTimestamp`, and `canonical_json_bytes`. Implement pure builders `build_research_source_manifest`, `build_evidence_span`, and `build_claim_evidence_link`; each accepts exact upstream model/document objects, recomputes their canonical digest internally, and never trusts a separately supplied upstream digest. Source builder binds the complete validated ARS entry as `bibliographic_sha256`; evidence builder copies `source_sha256` from the supplied source manifest; claim builder hashes exact span documents. Implement `validate_research_integrity_chain` over explicit source/span/link collections: recompute every document digest, build ID-to-digest maps, require every downstream reference to resolve to those exact bytes, enforce source digest propagation and canonical unique ordering, and reject replacement/mutation even when IDs stay equal. Provide canonical bytes/digest helpers but no YAML parser, network/file fetch, artifact admission, run mutation, retry, graph write, or Passport constructor. Extend the already registered discriminated schema bundle with source/span/link branches and update the exact stage list. Model docstrings and schema descriptions must state the parent-only `ArtifactAcceptanceRequest` -> `ArtifactManifest` -> accepted Passport-hash authority path. Run unchanged supply-chain inventory; this task does not touch the ARS tree or SBOM. When GREEN, create atomic commit 2 containing only Task 2 files.</action>
  <verify>
    <automated>UV_OFFLINE=1 PYTHONPATH=src PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/unit/test_research_integrity.py tests/schema/test_schema_drift.py tests/staged/test_supply_chain_inventory.py</automated>
  </verify>
  <done>Pure builders and whole-chain validation prove the canonical bibliography -> source -> span -> claim digest chain, reject replacement/mutation, and preserve source hashes; the installed schema bundle and stage inventory are GREEN; atomic commit 2 contains no Task 3 files and introduces no new writer/hook authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Support CJK literature filenames and refresh reviewable provenance inventories</name>
  <depends_on>Task 2 GREEN and atomic commit 2; this is atomic commit 3.</depends_on>
  <files>skills/academic-research-suite/ars/scripts/adapters/folder_scan.py, skills/academic-research-suite/ars/scripts/adapters/tests/test_folder_scan.py, skills/academic-research-suite/ars/scripts/adapters/examples/folder_scan/expected_passport.yaml, skills/academic-research-suite/ars/scripts/adapters/examples/folder_scan/expected_rejection_log.yaml, skills/academic-research-suite/ars/scripts/adapters/README.md, SBOM.cdx.json, MODIFICATIONS.md</files>
  <behavior>
    - `中文檔名_2024.pdf` and equivalent documented separator forms are accepted as Unicode author/title metadata instead of rejected solely for non-ASCII characters.
    - A parsed Unicode family token contains at least one Unicode `Letter` code point and contains only Unicode letters/combining marks plus documented apostrophe/hyphen punctuation; control, separator, surrogate/private-use, symbol, and emoji-only tokens are rejected rather than treated as authors.
    - Latin conventions keep their current citation keys; Unicode-only family/title material receives a deterministic ASCII key beginning with a letter, incorporating year and an NFKC-normalized digest token, then the existing alpha collision suffix policy.
    - Canonically equivalent Unicode spellings produce the same base key, two distinct Unicode records do not silently collapse, and every emitted key validates against `^[A-Za-z][A-Za-z0-9_:-]*$`.
    - Passport display `authors[].family` and `title` preserve the exact original filename text; NFKC is applied only to a private copy used as citation-key hash input.
    - Missing year/family remains a categorical rejection; symlink confinement, URI encoding, deterministic ordering, venue `unknown/unknown`, and fail-soft behavior do not regress.
    - Documentation, golden examples, local-modification disclosure, and CycloneDX digests describe the exact final vendored ARS/root hook/schema bytes; upstream source commit identities and unresolved license verdicts are not rewritten.
  </behavior>
  <action>Confirm the post-Task-2 baseline is GREEN, add CJK/security assertions to the already importable folder test module, require collect-only exit 0, then capture assertion-only RED exit 1 for `test_cjk_family_year_filename_is_accepted_with_original_display_text` and `test_unicode_family_rejects_control_separator_and_emoji_only_tokens`. Remove the blanket non-ASCII rejection only inside `folder_scan.py`. Parse Unicode families solely through explicit `{Family}_{Year}_{title}` / documented separator conventions. A family must contain at least one `unicodedata.category(ch).startswith("L")` code point and every character must be a Unicode letter, combining mark, apostrophe, or hyphen; reject `C*`, `Z*`, `S*`, emoji-only, empty, and other punctuation-only tokens. Preserve the original family and title byte-for-text in display output. For citekey derivation only, create an NFKC-normalized private string, derive a deterministic ASCII `ref{year}{sha256-prefix}` fallback when existing Latin logic cannot produce a schema-valid key, and apply the existing alpha suffix collision policy locally without changing shared `_common.py`; Latin keys must remain identical. Test NFKC-equivalent spelling, distinct Unicode keys, collision suffixes, ASCII regex validity, malformed family rejection, symlink/URI behavior, and existing Latin fixtures. Bump only `folder_scan.py`'s adapter version, update the two required golden files and the one directly false adapter README statement. Record the downstream CC BY-NC overlay in `MODIFICATIONS.md`, including the CJK filename grammar, deterministic citekey fallback, adapter version bump, unchanged upstream commit identity, and unresolved license restrictions. Because this task changes the vendored `ars` subtree, recompute both `artifact:skills/academic-research-suite/ars` and enclosing `artifact:skills/academic-research-suite` SBOM hashes in this same atomic task, add no new per-file SBOM rows, run supply-chain inventory, and keep upstream source commits plus legal/release fields unchanged. Never touch the install cache or claim a new upstream commit. When GREEN, create atomic commit 3 containing only Task 3 files.</action>
  <verify>
    <automated>UV_OFFLINE=1 PYTHONPATH=src PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q skills/academic-research-suite/ars/scripts/adapters/tests/test_folder_scan.py tests/staged/test_supply_chain_inventory.py</automated>
  </verify>
  <done>CJK fixtures are accepted only under the explicit safe family grammar, display text is unchanged, citekeys are normalized/hash-derived and contract-valid, Latin/security behavior stays GREEN, final ARS/enclosing SBOM digests and supply-chain inventory are GREEN, legal blockers are unchanged, and atomic commit 3 contains only Task 3 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installed/staged plugin bytes -> integration diagnostics | Untrusted or drifted files must be explained but never admitted as formal PASS without exact lock verification. |
| ARS user-owned entry -> ARW bridge contract | Bibliographic metadata and source pointers are untrusted input; the parent supplies and later admits exact digests. |
| Unicode filename -> ARS passport | Confusable/normalization/collision input must not create invalid or silently aliased citation identities. |
| Hook observation -> parent control plane | Hook output is optional observation with no canonical state, evidence-admission, retry, provenance, or gate authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q9P7-01 | Tampering | integration lock/stage/hooks | mitigate | Per-layer exact hashes, checked schema, root-hook SBOM gate, final unchanged `load_and_verify_integration_lock` comparison. |
| T-Q9P7-02 | Information disclosure | diagnostic error output | mitigate | Closed reason codes, bounded redacted detail, hashes instead of raw canary streams/private paths/credentials. |
| T-Q9P7-03 | Elevation of privilege | hook/bridge outputs | mitigate | Hooks remain observational; bridge is pure and cannot write ledger/Passport or accept evidence; only parent artifact admission is documented and tested. |
| T-Q9P7-04 | Spoofing/Tampering | ARS source and claim links | mitigate | Bind complete ARS entry, source content, extraction registration, evidence span, and claim bytes by separate SHA-256 fields with strict cross-field validation. |
| T-Q9P7-05 | Spoofing/Denial of service | Unicode filenames/citekey collisions | mitigate | NFKC normalization, deterministic ASCII digest fallback, bounded input/schema patterns, and explicit collision suffixing. |
| T-Q9P7-SC | Tampering | dependency/supply-chain changes | mitigate | Add no packages; retain offline pinned wheelhouse, source commits, SBOM conventions, and existing legal BLOCKED verdict. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Constraint | Task | Status | Notes
-------|----|--------------------|------|--------|------
GOAL | QUICK-260826-9p7 | One reviewable feat PR implementing the ARW research-integrity plan and diagnosed defects | 1-3 | COVERED | Code, tests, docs, installed schemas, inventory updates included.
REQ | DIAG | Precise layered integration-lock diagnosis | 1 | COVERED | Named layers, safe reason codes, unchanged formal verifier.
REQ | HOOK | Root hooks supply-chain gate | 1 | COVERED | Root and nested hook packs remain distinct; SBOM digests enforced.
REQ | BRIDGE | ARS to ARW manifest/evidence/link contracts | 2 | COVERED | Pure adapter plus source/span/link schemas and parent admission wiring.
REQ | CJK | CJK literature filename support | 3 | COVERED | Unicode metadata preserved; ASCII citekey contract retained.
CONTEXT | AUTHORITY | Hooks observational; parent owns canonical state, admission, retries, provenance, gates | 1-3 | COVERED | Explicit negative authority tests/docs; no alternate writer.
CONTEXT | LEGAL | Do not fabricate permission or clear unresolved release blockers | 1,3 | COVERED | Technical/release verdicts stay separate and BLOCKED where required.
CONTEXT | EXECUTION | Do not enable experiment execution | 1-3 | COVERED | Route contract and controlled execution unchanged.
CONTEXT | CACHE | Do not modify installed plugin cache | 1-3 | COVERED | Source/stage-only changes and tests.
</source_audit>

<verification>
1. Pass `<environment_preflight>` first. Run each task's baseline/collect-only/RED/GREEN sequence under `.venv/bin/python`, retain assertion-only RED evidence, and record the exact commands, node IDs, expected assertions, excerpts, and exit codes in SUMMARY. Environment, dependency, syntax, import, collection, or fixture failures never count as RED.
2. Run the combined contract and adapter regression set:
   `UV_OFFLINE=1 PYTHONPATH=src PYTHONNOUSERSITE=1 .venv/bin/python -m pytest -q tests/unit/test_integration_lock.py tests/unit/test_research_integrity.py tests/schema/test_schema_drift.py tests/staged/test_cli_launcher.py tests/staged/test_supply_chain_inventory.py skills/academic-research-suite/codex/tests/test_full_runtime_adapter.py skills/academic-research-suite/ars/scripts/adapters/tests/test_folder_scan.py`
3. Run the complete static ARS adapter gate:
   `PYTHONNOUSERSITE=1 .venv/bin/python skills/academic-research-suite/codex/scripts/ars_codex_quality_gates.py all --json`
4. Build and validate an isolated exact stage without touching the installation cache:
   `test -x .venv/bin/python &amp;&amp; grep -F 'PYTHON="$PROJECT_ROOT/.venv/bin/python"' scripts/stage-plugin &amp;&amp; ARW_Q9P7_TMP=$(mktemp -d) &amp;&amp; UV_OFFLINE=1 PYTHONNOUSERSITE=1 ARW_STAGE_TMP_ROOT="$ARW_Q9P7_TMP/build" ./scripts/stage-plugin --clean --stage-root "$ARW_Q9P7_TMP/stage/academic-research-workbench" &amp;&amp; UV_OFFLINE=1 PYTHONNOUSERSITE=1 ARW_STAGE_TMP_ROOT="$ARW_Q9P7_TMP/build" ./scripts/stage-plugin --stage-root "$ARW_Q9P7_TMP/stage/academic-research-workbench" --validate-only`
5. Confirm exactly three task commits in dependency order, `git diff --check`, and a 16-file implementation diff. Inspect it for install-cache paths, generated private evidence, raw research text, legal approval, experiment enablement, or alternate canonical writers.
</verification>

<success_criteria>
- `arw route --diagnostics --json` identifies the exact failing integration layer and cannot return technical PASS without the existing complete integration verification receipt; plain route output is unchanged.
- Root hook config/handler bytes are directly SBOM-gated; nested ARS hook safety remains a separate disabled-by-default check.
- One strict discriminated schema bundle validates diagnostic, research-source, evidence-span, and claim-link documents through model generation, registry validation, and staged installation.
- Pure builders plus whole-chain validation form and independently recompute the complete bibliography entry -> source manifest -> evidence span -> claim link digest chain, propagate source hashes, and reject same-ID replacement or post-build mutation; parent artifact acceptance remains the only route into canonical state and Passport hashes.
- CJK filenames pass adapter/schema tests with preserved Unicode metadata and stable ASCII citekeys; previous Latin, symlink, URI, deterministic-order, rejection, and venue behaviors remain green.
- Each ARS-tree-changing task refreshes its SBOM digest and passes supply-chain inventory before its atomic commit; full focused regression, quality gates, isolated stage validation, and diff hygiene pass; release qualification remains BLOCKED and experiment execution remains disabled.
</success_criteria>

<output>
Create `.planning/quick/260826-9p7-implement-arw-research-integrity-bridge-/260826-9p7-SUMMARY.md` after execution. Include the environment preflight result, each task's baseline/collect-only/RED/GREEN commands and exit codes, expected assertion witnesses, three atomic commit hashes, final 16-file inventory, SBOM digest changes, and verification commands. The main agent owns push and feat PR creation.
</output>
