# file-base compatibility provider

The existing shell entrypoint delegates to `bin/provider` here. Its native branch
still requires explicit allowed-root/cache configuration. The qualified native
binary, source snapshot, patch, schema and error taxonomy are unchanged. Native
compatibility and the store-backed files route retain their separate contracts.
The continuing native exposure is why its qualification gate remains mandatory.
