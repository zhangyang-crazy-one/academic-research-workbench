# ARS workflow adapter

`arw_ars.ARSAdapter` implements the existing WorkflowProvider contract. The
composition root is its only production import site. Bundled ARS skill/source
bytes, license inventory and digest-qualified assets are unchanged. See
`docs/runtime/extension-formats.md` for migration and compatibility evidence.
