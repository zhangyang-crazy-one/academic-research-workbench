# Files-first Phase 3 fixture

This corpus is synthetic and repository-owned. It contains no real paper,
credential, run, or private cache data. `fixture-manifest.json` binds the bytes
that exercise duplicate identity, registered PDF extraction, malformed UTF-8,
and stale/private canary suppression.

